/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/**
 * TransferScheduleSummaryRetirementContribution - The contribution info for a retirement account
 */
public class TransferScheduleSummaryRetirementContribution {

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("tax_year")
  private Optional<Integer> taxYear;

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("temporal_tax_year")
  private Optional<? extends TransferScheduleSummaryTemporalTaxYear> temporalTaxYear;

  /** The type of retirement contribution. */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("type")
  private Optional<? extends TransferScheduleSummaryType> type;

  @JsonCreator
  public TransferScheduleSummaryRetirementContribution(
      @JsonProperty("tax_year") Optional<Integer> taxYear,
      @JsonProperty("temporal_tax_year")
          Optional<? extends TransferScheduleSummaryTemporalTaxYear> temporalTaxYear,
      @JsonProperty("type") Optional<? extends TransferScheduleSummaryType> type) {
    Utils.checkNotNull(taxYear, "taxYear");
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    Utils.checkNotNull(type, "type");
    this.taxYear = taxYear;
    this.temporalTaxYear = temporalTaxYear;
    this.type = type;
  }

  public TransferScheduleSummaryRetirementContribution() {
    this(Optional.empty(), Optional.empty(), Optional.empty());
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  @JsonIgnore
  public Optional<Integer> taxYear() {
    return taxYear;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<TransferScheduleSummaryTemporalTaxYear> temporalTaxYear() {
    return (Optional<TransferScheduleSummaryTemporalTaxYear>) temporalTaxYear;
  }

  /** The type of retirement contribution. */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<TransferScheduleSummaryType> type() {
    return (Optional<TransferScheduleSummaryType>) type;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  public TransferScheduleSummaryRetirementContribution withTaxYear(int taxYear) {
    Utils.checkNotNull(taxYear, "taxYear");
    this.taxYear = Optional.ofNullable(taxYear);
    return this;
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  public TransferScheduleSummaryRetirementContribution withTaxYear(Optional<Integer> taxYear) {
    Utils.checkNotNull(taxYear, "taxYear");
    this.taxYear = taxYear;
    return this;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  public TransferScheduleSummaryRetirementContribution withTemporalTaxYear(
      TransferScheduleSummaryTemporalTaxYear temporalTaxYear) {
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    this.temporalTaxYear = Optional.ofNullable(temporalTaxYear);
    return this;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  public TransferScheduleSummaryRetirementContribution withTemporalTaxYear(
      Optional<? extends TransferScheduleSummaryTemporalTaxYear> temporalTaxYear) {
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    this.temporalTaxYear = temporalTaxYear;
    return this;
  }

  /** The type of retirement contribution. */
  public TransferScheduleSummaryRetirementContribution withType(TransferScheduleSummaryType type) {
    Utils.checkNotNull(type, "type");
    this.type = Optional.ofNullable(type);
    return this;
  }

  /** The type of retirement contribution. */
  public TransferScheduleSummaryRetirementContribution withType(
      Optional<? extends TransferScheduleSummaryType> type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TransferScheduleSummaryRetirementContribution other =
        (TransferScheduleSummaryRetirementContribution) o;
    return Objects.deepEquals(this.taxYear, other.taxYear)
        && Objects.deepEquals(this.temporalTaxYear, other.temporalTaxYear)
        && Objects.deepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Objects.hash(taxYear, temporalTaxYear, type);
  }

  @Override
  public String toString() {
    return Utils.toString(
        TransferScheduleSummaryRetirementContribution.class,
        "taxYear",
        taxYear,
        "temporalTaxYear",
        temporalTaxYear,
        "type",
        type);
  }

  public static final class Builder {

    private Optional<Integer> taxYear = Optional.empty();

    private Optional<? extends TransferScheduleSummaryTemporalTaxYear> temporalTaxYear =
        Optional.empty();

    private Optional<? extends TransferScheduleSummaryType> type = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * An explicit tax year value. The current year is always valid; and the prior year is valid
     * only before the tax deadline. Must be in "YYYY" format.
     */
    public Builder taxYear(int taxYear) {
      Utils.checkNotNull(taxYear, "taxYear");
      this.taxYear = Optional.ofNullable(taxYear);
      return this;
    }

    /**
     * An explicit tax year value. The current year is always valid; and the prior year is valid
     * only before the tax deadline. Must be in "YYYY" format.
     */
    public Builder taxYear(Optional<Integer> taxYear) {
      Utils.checkNotNull(taxYear, "taxYear");
      this.taxYear = taxYear;
      return this;
    }

    /**
     * A temporal tax year value. This will always evaluate to a year based on the date the transfer
     * was initiated.
     */
    public Builder temporalTaxYear(TransferScheduleSummaryTemporalTaxYear temporalTaxYear) {
      Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
      this.temporalTaxYear = Optional.ofNullable(temporalTaxYear);
      return this;
    }

    /**
     * A temporal tax year value. This will always evaluate to a year based on the date the transfer
     * was initiated.
     */
    public Builder temporalTaxYear(
        Optional<? extends TransferScheduleSummaryTemporalTaxYear> temporalTaxYear) {
      Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
      this.temporalTaxYear = temporalTaxYear;
      return this;
    }

    /** The type of retirement contribution. */
    public Builder type(TransferScheduleSummaryType type) {
      Utils.checkNotNull(type, "type");
      this.type = Optional.ofNullable(type);
      return this;
    }

    /** The type of retirement contribution. */
    public Builder type(Optional<? extends TransferScheduleSummaryType> type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public TransferScheduleSummaryRetirementContribution build() {
      return new TransferScheduleSummaryRetirementContribution(taxYear, temporalTaxYear, type);
    }
  }
}
