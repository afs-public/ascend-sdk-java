/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/** ReportingEligibility - Tax reporting eligibility. */
@JsonDeserialize(using = ReportingEligibility._Deserializer.class)
@JsonSerialize(using = ReportingEligibility._Serializer.class)
public class ReportingEligibility {

  public static final ReportingEligibility TAX_REPORTING_ELIGIBILITY_UNSPECIFIED =
      new ReportingEligibility("TAX_REPORTING_ELIGIBILITY_UNSPECIFIED");
  public static final ReportingEligibility ELIGIBLE = new ReportingEligibility("ELIGIBLE");
  public static final ReportingEligibility INELIGIBLE = new ReportingEligibility("INELIGIBLE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, ReportingEligibility> values = createValuesMap();
  private static final Map<String, ReportingEligibilityEnum> enums = createEnumsMap();

  private final String value;

  private ReportingEligibility(String value) {
    this.value = value;
  }

  /**
   * Returns a ReportingEligibility with the given value. For a specific value the returned object
   * will always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as ReportingEligibility
   */
  public static ReportingEligibility of(String value) {
    synchronized (ReportingEligibility.class) {
      return values.computeIfAbsent(value, v -> new ReportingEligibility(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ReportingEligibilityEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    ReportingEligibility other = (ReportingEligibility) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "ReportingEligibility [value=" + value + "]";
  }

  // return an array just like an enum
  public static ReportingEligibility[] values() {
    synchronized (ReportingEligibility.class) {
      return values.values().toArray(new ReportingEligibility[] {});
    }
  }

  private static final Map<String, ReportingEligibility> createValuesMap() {
    Map<String, ReportingEligibility> map = new LinkedHashMap<>();
    map.put("TAX_REPORTING_ELIGIBILITY_UNSPECIFIED", TAX_REPORTING_ELIGIBILITY_UNSPECIFIED);
    map.put("ELIGIBLE", ELIGIBLE);
    map.put("INELIGIBLE", INELIGIBLE);
    return map;
  }

  private static final Map<String, ReportingEligibilityEnum> createEnumsMap() {
    Map<String, ReportingEligibilityEnum> map = new HashMap<>();
    map.put(
        "TAX_REPORTING_ELIGIBILITY_UNSPECIFIED",
        ReportingEligibilityEnum.TAX_REPORTING_ELIGIBILITY_UNSPECIFIED);
    map.put("ELIGIBLE", ReportingEligibilityEnum.ELIGIBLE);
    map.put("INELIGIBLE", ReportingEligibilityEnum.INELIGIBLE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<ReportingEligibility> {

    protected _Serializer() {
      super(ReportingEligibility.class);
    }

    @Override
    public void serialize(ReportingEligibility value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<ReportingEligibility> {

    protected _Deserializer() {
      super(ReportingEligibility.class);
    }

    @Override
    public ReportingEligibility deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return ReportingEligibility.of(v);
    }
  }

  public enum ReportingEligibilityEnum {
    TAX_REPORTING_ELIGIBILITY_UNSPECIFIED("TAX_REPORTING_ELIGIBILITY_UNSPECIFIED"),
    ELIGIBLE("ELIGIBLE"),
    INELIGIBLE("INELIGIBLE"),
    ;

    private final String value;

    private ReportingEligibilityEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
