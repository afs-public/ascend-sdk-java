/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * CouponFrequency
 *
 * <p>Frequency of payments
 */
@JsonDeserialize(using = CouponFrequency._Deserializer.class)
@JsonSerialize(using = CouponFrequency._Serializer.class)
public class CouponFrequency {

  public static final CouponFrequency COUPON_FREQUENCY_UNSPECIFIED =
      new CouponFrequency("COUPON_FREQUENCY_UNSPECIFIED");
  public static final CouponFrequency SEMI_ANNUAL = new CouponFrequency("SEMI_ANNUAL");
  public static final CouponFrequency MONTHLY = new CouponFrequency("MONTHLY");
  public static final CouponFrequency AT_MATURITY = new CouponFrequency("AT_MATURITY");
  public static final CouponFrequency ZERO = new CouponFrequency("ZERO");
  public static final CouponFrequency QUARTERLY = new CouponFrequency("QUARTERLY");
  public static final CouponFrequency ANNUAL = new CouponFrequency("ANNUAL");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, CouponFrequency> values = createValuesMap();
  private static final Map<String, CouponFrequencyEnum> enums = createEnumsMap();

  private final String value;

  private CouponFrequency(String value) {
    this.value = value;
  }

  /**
   * Returns a CouponFrequency with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as CouponFrequency
   */
  public static CouponFrequency of(String value) {
    synchronized (CouponFrequency.class) {
      return values.computeIfAbsent(value, v -> new CouponFrequency(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<CouponFrequencyEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    CouponFrequency other = (CouponFrequency) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "CouponFrequency [value=" + value + "]";
  }

  // return an array just like an enum
  public static CouponFrequency[] values() {
    synchronized (CouponFrequency.class) {
      return values.values().toArray(new CouponFrequency[] {});
    }
  }

  private static final Map<String, CouponFrequency> createValuesMap() {
    Map<String, CouponFrequency> map = new LinkedHashMap<>();
    map.put("COUPON_FREQUENCY_UNSPECIFIED", COUPON_FREQUENCY_UNSPECIFIED);
    map.put("SEMI_ANNUAL", SEMI_ANNUAL);
    map.put("MONTHLY", MONTHLY);
    map.put("AT_MATURITY", AT_MATURITY);
    map.put("ZERO", ZERO);
    map.put("QUARTERLY", QUARTERLY);
    map.put("ANNUAL", ANNUAL);
    return map;
  }

  private static final Map<String, CouponFrequencyEnum> createEnumsMap() {
    Map<String, CouponFrequencyEnum> map = new HashMap<>();
    map.put("COUPON_FREQUENCY_UNSPECIFIED", CouponFrequencyEnum.COUPON_FREQUENCY_UNSPECIFIED);
    map.put("SEMI_ANNUAL", CouponFrequencyEnum.SEMI_ANNUAL);
    map.put("MONTHLY", CouponFrequencyEnum.MONTHLY);
    map.put("AT_MATURITY", CouponFrequencyEnum.AT_MATURITY);
    map.put("ZERO", CouponFrequencyEnum.ZERO);
    map.put("QUARTERLY", CouponFrequencyEnum.QUARTERLY);
    map.put("ANNUAL", CouponFrequencyEnum.ANNUAL);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<CouponFrequency> {

    protected _Serializer() {
      super(CouponFrequency.class);
    }

    @Override
    public void serialize(CouponFrequency value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<CouponFrequency> {

    protected _Deserializer() {
      super(CouponFrequency.class);
    }

    @Override
    public CouponFrequency deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return CouponFrequency.of(v);
    }
  }

  public enum CouponFrequencyEnum {
    COUPON_FREQUENCY_UNSPECIFIED("COUPON_FREQUENCY_UNSPECIFIED"),
    SEMI_ANNUAL("SEMI_ANNUAL"),
    MONTHLY("MONTHLY"),
    AT_MATURITY("AT_MATURITY"),
    ZERO("ZERO"),
    QUARTERLY("QUARTERLY"),
    ANNUAL("ANNUAL"),
    ;

    private final String value;

    private CouponFrequencyEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
