/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * ReserveClass
 *
 * <p>The Reserve Class associated with the account
 */
@JsonDeserialize(using = ReserveClass._Deserializer.class)
@JsonSerialize(using = ReserveClass._Serializer.class)
public class ReserveClass {

  public static final ReserveClass RESERVE_CLASS_UNSPECIFIED =
      new ReserveClass("RESERVE_CLASS_UNSPECIFIED");
  public static final ReserveClass CUSTOMER = new ReserveClass("CUSTOMER");
  public static final ReserveClass PAB = new ReserveClass("PAB");
  public static final ReserveClass FIRM = new ReserveClass("FIRM");
  public static final ReserveClass STREET = new ReserveClass("STREET");
  public static final ReserveClass GL = new ReserveClass("G_L");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, ReserveClass> values = createValuesMap();
  private static final Map<String, ReserveClassEnum> enums = createEnumsMap();

  private final String value;

  private ReserveClass(String value) {
    this.value = value;
  }

  /**
   * Returns a ReserveClass with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as ReserveClass
   */
  public static ReserveClass of(String value) {
    synchronized (ReserveClass.class) {
      return values.computeIfAbsent(value, v -> new ReserveClass(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ReserveClassEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    ReserveClass other = (ReserveClass) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "ReserveClass [value=" + value + "]";
  }

  // return an array just like an enum
  public static ReserveClass[] values() {
    synchronized (ReserveClass.class) {
      return values.values().toArray(new ReserveClass[] {});
    }
  }

  private static final Map<String, ReserveClass> createValuesMap() {
    Map<String, ReserveClass> map = new LinkedHashMap<>();
    map.put("RESERVE_CLASS_UNSPECIFIED", RESERVE_CLASS_UNSPECIFIED);
    map.put("CUSTOMER", CUSTOMER);
    map.put("PAB", PAB);
    map.put("FIRM", FIRM);
    map.put("STREET", STREET);
    map.put("G_L", GL);
    return map;
  }

  private static final Map<String, ReserveClassEnum> createEnumsMap() {
    Map<String, ReserveClassEnum> map = new HashMap<>();
    map.put("RESERVE_CLASS_UNSPECIFIED", ReserveClassEnum.RESERVE_CLASS_UNSPECIFIED);
    map.put("CUSTOMER", ReserveClassEnum.CUSTOMER);
    map.put("PAB", ReserveClassEnum.PAB);
    map.put("FIRM", ReserveClassEnum.FIRM);
    map.put("STREET", ReserveClassEnum.STREET);
    map.put("G_L", ReserveClassEnum.GL);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<ReserveClass> {

    protected _Serializer() {
      super(ReserveClass.class);
    }

    @Override
    public void serialize(ReserveClass value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<ReserveClass> {

    protected _Deserializer() {
      super(ReserveClass.class);
    }

    @Override
    public ReserveClass deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return ReserveClass.of(v);
    }
  }

  public enum ReserveClassEnum {
    RESERVE_CLASS_UNSPECIFIED("RESERVE_CLASS_UNSPECIFIED"),
    CUSTOMER("CUSTOMER"),
    PAB("PAB"),
    FIRM("FIRM"),
    STREET("STREET"),
    GL("G_L"),
    ;

    private final String value;

    private ReserveClassEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
