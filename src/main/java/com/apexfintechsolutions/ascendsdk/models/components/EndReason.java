/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/** EndReason - The end reason of the LTID. */
@JsonDeserialize(using = EndReason._Deserializer.class)
@JsonSerialize(using = EndReason._Serializer.class)
public class EndReason {

  public static final EndReason REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED =
      new EndReason("REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED");
  public static final EndReason EVENT_REASON_CREATED = new EndReason("EVENT_REASON_CREATED");
  public static final EndReason EVENT_REASON_CORRECTION = new EndReason("EVENT_REASON_CORRECTION");
  public static final EndReason EVENT_REASON_ENDED = new EndReason("EVENT_REASON_ENDED");
  public static final EndReason EVENT_REASON_REPLACED = new EndReason("EVENT_REASON_REPLACED");
  public static final EndReason EVENT_REASON_TRANSFER = new EndReason("EVENT_REASON_TRANSFER");
  public static final EndReason EVENT_REASON_OTHER = new EndReason("EVENT_REASON_OTHER");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, EndReason> values = createValuesMap();
  private static final Map<String, EndReasonEnum> enums = createEnumsMap();

  private final String value;

  private EndReason(String value) {
    this.value = value;
  }

  /**
   * Returns a EndReason with the given value. For a specific value the returned object will always
   * be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as EndReason
   */
  public static EndReason of(String value) {
    synchronized (EndReason.class) {
      return values.computeIfAbsent(value, v -> new EndReason(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<EndReasonEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    EndReason other = (EndReason) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "EndReason [value=" + value + "]";
  }

  // return an array just like an enum
  public static EndReason[] values() {
    synchronized (EndReason.class) {
      return values.values().toArray(new EndReason[] {});
    }
  }

  private static final Map<String, EndReason> createValuesMap() {
    Map<String, EndReason> map = new LinkedHashMap<>();
    map.put(
        "REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED", REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED);
    map.put("EVENT_REASON_CREATED", EVENT_REASON_CREATED);
    map.put("EVENT_REASON_CORRECTION", EVENT_REASON_CORRECTION);
    map.put("EVENT_REASON_ENDED", EVENT_REASON_ENDED);
    map.put("EVENT_REASON_REPLACED", EVENT_REASON_REPLACED);
    map.put("EVENT_REASON_TRANSFER", EVENT_REASON_TRANSFER);
    map.put("EVENT_REASON_OTHER", EVENT_REASON_OTHER);
    return map;
  }

  private static final Map<String, EndReasonEnum> createEnumsMap() {
    Map<String, EndReasonEnum> map = new HashMap<>();
    map.put(
        "REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED",
        EndReasonEnum.REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED);
    map.put("EVENT_REASON_CREATED", EndReasonEnum.EVENT_REASON_CREATED);
    map.put("EVENT_REASON_CORRECTION", EndReasonEnum.EVENT_REASON_CORRECTION);
    map.put("EVENT_REASON_ENDED", EndReasonEnum.EVENT_REASON_ENDED);
    map.put("EVENT_REASON_REPLACED", EndReasonEnum.EVENT_REASON_REPLACED);
    map.put("EVENT_REASON_TRANSFER", EndReasonEnum.EVENT_REASON_TRANSFER);
    map.put("EVENT_REASON_OTHER", EndReasonEnum.EVENT_REASON_OTHER);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<EndReason> {

    protected _Serializer() {
      super(EndReason.class);
    }

    @Override
    public void serialize(EndReason value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<EndReason> {

    protected _Deserializer() {
      super(EndReason.class);
    }

    @Override
    public EndReason deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return EndReason.of(v);
    }
  }

  public enum EndReasonEnum {
    REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED("REPORTABLE_ACCOUNT_EVENT_REASON_UNSPECIFIED"),
    EVENT_REASON_CREATED("EVENT_REASON_CREATED"),
    EVENT_REASON_CORRECTION("EVENT_REASON_CORRECTION"),
    EVENT_REASON_ENDED("EVENT_REASON_ENDED"),
    EVENT_REASON_REPLACED("EVENT_REASON_REPLACED"),
    EVENT_REASON_TRANSFER("EVENT_REASON_TRANSFER"),
    EVENT_REASON_OTHER("EVENT_REASON_OTHER"),
    ;

    private final String value;

    private EndReasonEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
