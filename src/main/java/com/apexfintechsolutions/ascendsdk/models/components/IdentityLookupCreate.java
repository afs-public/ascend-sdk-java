/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonProperty;

/**
 * IdentityLookupCreate
 *
 * <p>Represents an identity lookup resource
 */
public class IdentityLookupCreate {
  /**
   * End-user device metadata for fraud detection and compliance All fields must represent the
   * actual end-user's device, not any intermediate proxy or client server through which requests
   * may be routed
   */
  @JsonProperty("device_metadata")
  private DeviceMetadataCreate deviceMetadata;

  /** Represents an identification document */
  @JsonProperty("identification")
  private IdentificationCreate identification;

  /**
   * An object representing a phone number, suitable as an API wire format.
   *
   * <p>This representation:
   *
   * <p>- should not be used for locale-specific formatting of a phone number, such as "+1 (650)
   * 253-0000 ext. 123"
   *
   * <p>- is not designed for efficient storage - may not be suitable for dialing - specialized
   * libraries (see references) should be used to parse the number for that purpose
   *
   * <p>To do something meaningful with this number, such as format it for various use-cases,
   * convert it to an `i18n.phonenumbers.PhoneNumber` object first.
   *
   * <p>For instance, in Java this would be:
   *
   * <p>com.google.type.PhoneNumber wireProto = com.google.type.PhoneNumber.newBuilder().build();
   * com.google.i18n.phonenumbers.Phonenumber.PhoneNumber phoneNumber =
   * PhoneNumberUtil.getInstance().parse(wireProto.getE164Number(), "ZZ"); if
   * (!wireProto.getExtension().isEmpty()) { phoneNumber.setExtension(wireProto.getExtension()); }
   *
   * <p>Reference(s): - https://github.com/google/libphonenumber
   */
  @JsonProperty("phone_number")
  private PhoneNumberCreate phoneNumber;

  /** Verify User has consented to an identity lookup */
  @JsonProperty("user_consent")
  private boolean userConsent;

  @JsonCreator
  public IdentityLookupCreate(
      @JsonProperty("device_metadata") DeviceMetadataCreate deviceMetadata,
      @JsonProperty("identification") IdentificationCreate identification,
      @JsonProperty("phone_number") PhoneNumberCreate phoneNumber,
      @JsonProperty("user_consent") boolean userConsent) {
    Utils.checkNotNull(deviceMetadata, "deviceMetadata");
    Utils.checkNotNull(identification, "identification");
    Utils.checkNotNull(phoneNumber, "phoneNumber");
    Utils.checkNotNull(userConsent, "userConsent");
    this.deviceMetadata = deviceMetadata;
    this.identification = identification;
    this.phoneNumber = phoneNumber;
    this.userConsent = userConsent;
  }

  /**
   * End-user device metadata for fraud detection and compliance All fields must represent the
   * actual end-user's device, not any intermediate proxy or client server through which requests
   * may be routed
   */
  @JsonIgnore
  public DeviceMetadataCreate deviceMetadata() {
    return deviceMetadata;
  }

  /** Represents an identification document */
  @JsonIgnore
  public IdentificationCreate identification() {
    return identification;
  }

  /**
   * An object representing a phone number, suitable as an API wire format.
   *
   * <p>This representation:
   *
   * <p>- should not be used for locale-specific formatting of a phone number, such as "+1 (650)
   * 253-0000 ext. 123"
   *
   * <p>- is not designed for efficient storage - may not be suitable for dialing - specialized
   * libraries (see references) should be used to parse the number for that purpose
   *
   * <p>To do something meaningful with this number, such as format it for various use-cases,
   * convert it to an `i18n.phonenumbers.PhoneNumber` object first.
   *
   * <p>For instance, in Java this would be:
   *
   * <p>com.google.type.PhoneNumber wireProto = com.google.type.PhoneNumber.newBuilder().build();
   * com.google.i18n.phonenumbers.Phonenumber.PhoneNumber phoneNumber =
   * PhoneNumberUtil.getInstance().parse(wireProto.getE164Number(), "ZZ"); if
   * (!wireProto.getExtension().isEmpty()) { phoneNumber.setExtension(wireProto.getExtension()); }
   *
   * <p>Reference(s): - https://github.com/google/libphonenumber
   */
  @JsonIgnore
  public PhoneNumberCreate phoneNumber() {
    return phoneNumber;
  }

  /** Verify User has consented to an identity lookup */
  @JsonIgnore
  public boolean userConsent() {
    return userConsent;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * End-user device metadata for fraud detection and compliance All fields must represent the
   * actual end-user's device, not any intermediate proxy or client server through which requests
   * may be routed
   */
  public IdentityLookupCreate withDeviceMetadata(DeviceMetadataCreate deviceMetadata) {
    Utils.checkNotNull(deviceMetadata, "deviceMetadata");
    this.deviceMetadata = deviceMetadata;
    return this;
  }

  /** Represents an identification document */
  public IdentityLookupCreate withIdentification(IdentificationCreate identification) {
    Utils.checkNotNull(identification, "identification");
    this.identification = identification;
    return this;
  }

  /**
   * An object representing a phone number, suitable as an API wire format.
   *
   * <p>This representation:
   *
   * <p>- should not be used for locale-specific formatting of a phone number, such as "+1 (650)
   * 253-0000 ext. 123"
   *
   * <p>- is not designed for efficient storage - may not be suitable for dialing - specialized
   * libraries (see references) should be used to parse the number for that purpose
   *
   * <p>To do something meaningful with this number, such as format it for various use-cases,
   * convert it to an `i18n.phonenumbers.PhoneNumber` object first.
   *
   * <p>For instance, in Java this would be:
   *
   * <p>com.google.type.PhoneNumber wireProto = com.google.type.PhoneNumber.newBuilder().build();
   * com.google.i18n.phonenumbers.Phonenumber.PhoneNumber phoneNumber =
   * PhoneNumberUtil.getInstance().parse(wireProto.getE164Number(), "ZZ"); if
   * (!wireProto.getExtension().isEmpty()) { phoneNumber.setExtension(wireProto.getExtension()); }
   *
   * <p>Reference(s): - https://github.com/google/libphonenumber
   */
  public IdentityLookupCreate withPhoneNumber(PhoneNumberCreate phoneNumber) {
    Utils.checkNotNull(phoneNumber, "phoneNumber");
    this.phoneNumber = phoneNumber;
    return this;
  }

  /** Verify User has consented to an identity lookup */
  public IdentityLookupCreate withUserConsent(boolean userConsent) {
    Utils.checkNotNull(userConsent, "userConsent");
    this.userConsent = userConsent;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IdentityLookupCreate other = (IdentityLookupCreate) o;
    return Utils.enhancedDeepEquals(this.deviceMetadata, other.deviceMetadata)
        && Utils.enhancedDeepEquals(this.identification, other.identification)
        && Utils.enhancedDeepEquals(this.phoneNumber, other.phoneNumber)
        && Utils.enhancedDeepEquals(this.userConsent, other.userConsent);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(deviceMetadata, identification, phoneNumber, userConsent);
  }

  @Override
  public String toString() {
    return Utils.toString(
        IdentityLookupCreate.class,
        "deviceMetadata",
        deviceMetadata,
        "identification",
        identification,
        "phoneNumber",
        phoneNumber,
        "userConsent",
        userConsent);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private DeviceMetadataCreate deviceMetadata;

    private IdentificationCreate identification;

    private PhoneNumberCreate phoneNumber;

    private Boolean userConsent;

    private Builder() {
      // force use of static builder() method
    }

    /**
     * End-user device metadata for fraud detection and compliance All fields must represent the
     * actual end-user's device, not any intermediate proxy or client server through which requests
     * may be routed
     */
    public Builder deviceMetadata(DeviceMetadataCreate deviceMetadata) {
      Utils.checkNotNull(deviceMetadata, "deviceMetadata");
      this.deviceMetadata = deviceMetadata;
      return this;
    }

    /** Represents an identification document */
    public Builder identification(IdentificationCreate identification) {
      Utils.checkNotNull(identification, "identification");
      this.identification = identification;
      return this;
    }

    /**
     * An object representing a phone number, suitable as an API wire format.
     *
     * <p>This representation:
     *
     * <p>- should not be used for locale-specific formatting of a phone number, such as "+1 (650)
     * 253-0000 ext. 123"
     *
     * <p>- is not designed for efficient storage - may not be suitable for dialing - specialized
     * libraries (see references) should be used to parse the number for that purpose
     *
     * <p>To do something meaningful with this number, such as format it for various use-cases,
     * convert it to an `i18n.phonenumbers.PhoneNumber` object first.
     *
     * <p>For instance, in Java this would be:
     *
     * <p>com.google.type.PhoneNumber wireProto = com.google.type.PhoneNumber.newBuilder().build();
     * com.google.i18n.phonenumbers.Phonenumber.PhoneNumber phoneNumber =
     * PhoneNumberUtil.getInstance().parse(wireProto.getE164Number(), "ZZ"); if
     * (!wireProto.getExtension().isEmpty()) { phoneNumber.setExtension(wireProto.getExtension()); }
     *
     * <p>Reference(s): - https://github.com/google/libphonenumber
     */
    public Builder phoneNumber(PhoneNumberCreate phoneNumber) {
      Utils.checkNotNull(phoneNumber, "phoneNumber");
      this.phoneNumber = phoneNumber;
      return this;
    }

    /** Verify User has consented to an identity lookup */
    public Builder userConsent(boolean userConsent) {
      Utils.checkNotNull(userConsent, "userConsent");
      this.userConsent = userConsent;
      return this;
    }

    public IdentityLookupCreate build() {

      return new IdentityLookupCreate(deviceMetadata, identification, phoneNumber, userConsent);
    }
  }
}
