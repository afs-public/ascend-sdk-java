/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * PartyNameSuffix - The suffix of a natural person; A suffix in a name is any part of the name that
 * comes after the last name
 */
@JsonDeserialize(using = PartyNameSuffix._Deserializer.class)
@JsonSerialize(using = PartyNameSuffix._Serializer.class)
public class PartyNameSuffix {

  public static final PartyNameSuffix NAME_SUFFIX_UNSPECIFIED =
      new PartyNameSuffix("NAME_SUFFIX_UNSPECIFIED");
  public static final PartyNameSuffix SR = new PartyNameSuffix("SR");
  public static final PartyNameSuffix JR = new PartyNameSuffix("JR");
  public static final PartyNameSuffix III = new PartyNameSuffix("III");
  public static final PartyNameSuffix IV = new PartyNameSuffix("IV");
  public static final PartyNameSuffix V = new PartyNameSuffix("V");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, PartyNameSuffix> values = createValuesMap();
  private static final Map<String, PartyNameSuffixEnum> enums = createEnumsMap();

  private final String value;

  private PartyNameSuffix(String value) {
    this.value = value;
  }

  /**
   * Returns a PartyNameSuffix with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as PartyNameSuffix
   */
  public static PartyNameSuffix of(String value) {
    synchronized (PartyNameSuffix.class) {
      return values.computeIfAbsent(value, v -> new PartyNameSuffix(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<PartyNameSuffixEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    PartyNameSuffix other = (PartyNameSuffix) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "PartyNameSuffix [value=" + value + "]";
  }

  // return an array just like an enum
  public static PartyNameSuffix[] values() {
    synchronized (PartyNameSuffix.class) {
      return values.values().toArray(new PartyNameSuffix[] {});
    }
  }

  private static final Map<String, PartyNameSuffix> createValuesMap() {
    Map<String, PartyNameSuffix> map = new LinkedHashMap<>();
    map.put("NAME_SUFFIX_UNSPECIFIED", NAME_SUFFIX_UNSPECIFIED);
    map.put("SR", SR);
    map.put("JR", JR);
    map.put("III", III);
    map.put("IV", IV);
    map.put("V", V);
    return map;
  }

  private static final Map<String, PartyNameSuffixEnum> createEnumsMap() {
    Map<String, PartyNameSuffixEnum> map = new HashMap<>();
    map.put("NAME_SUFFIX_UNSPECIFIED", PartyNameSuffixEnum.NAME_SUFFIX_UNSPECIFIED);
    map.put("SR", PartyNameSuffixEnum.SR);
    map.put("JR", PartyNameSuffixEnum.JR);
    map.put("III", PartyNameSuffixEnum.III);
    map.put("IV", PartyNameSuffixEnum.IV);
    map.put("V", PartyNameSuffixEnum.V);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<PartyNameSuffix> {

    protected _Serializer() {
      super(PartyNameSuffix.class);
    }

    @Override
    public void serialize(PartyNameSuffix value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<PartyNameSuffix> {

    protected _Deserializer() {
      super(PartyNameSuffix.class);
    }

    @Override
    public PartyNameSuffix deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return PartyNameSuffix.of(v);
    }
  }

  public enum PartyNameSuffixEnum {
    NAME_SUFFIX_UNSPECIFIED("NAME_SUFFIX_UNSPECIFIED"),
    SR("SR"),
    JR("JR"),
    III("III"),
    IV("IV"),
    V("V"),
    ;

    private final String value;

    private PartyNameSuffixEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
