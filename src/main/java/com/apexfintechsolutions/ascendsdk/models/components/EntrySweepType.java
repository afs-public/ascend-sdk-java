/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/** EntrySweepType - Sweep program that cash is being swept to/ from */
@JsonDeserialize(using = EntrySweepType._Deserializer.class)
@JsonSerialize(using = EntrySweepType._Serializer.class)
public class EntrySweepType {

  public static final EntrySweepType SWEEP_TYPE_UNSPECIFIED =
      new EntrySweepType("SWEEP_TYPE_UNSPECIFIED");
  public static final EntrySweepType FDIC = new EntrySweepType("FDIC");
  public static final EntrySweepType MONEY_MARKET = new EntrySweepType("MONEY_MARKET");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, EntrySweepType> values = createValuesMap();
  private static final Map<String, EntrySweepTypeEnum> enums = createEnumsMap();

  private final String value;

  private EntrySweepType(String value) {
    this.value = value;
  }

  /**
   * Returns a EntrySweepType with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as EntrySweepType
   */
  public static EntrySweepType of(String value) {
    synchronized (EntrySweepType.class) {
      return values.computeIfAbsent(value, v -> new EntrySweepType(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<EntrySweepTypeEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    EntrySweepType other = (EntrySweepType) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "EntrySweepType [value=" + value + "]";
  }

  // return an array just like an enum
  public static EntrySweepType[] values() {
    synchronized (EntrySweepType.class) {
      return values.values().toArray(new EntrySweepType[] {});
    }
  }

  private static final Map<String, EntrySweepType> createValuesMap() {
    Map<String, EntrySweepType> map = new LinkedHashMap<>();
    map.put("SWEEP_TYPE_UNSPECIFIED", SWEEP_TYPE_UNSPECIFIED);
    map.put("FDIC", FDIC);
    map.put("MONEY_MARKET", MONEY_MARKET);
    return map;
  }

  private static final Map<String, EntrySweepTypeEnum> createEnumsMap() {
    Map<String, EntrySweepTypeEnum> map = new HashMap<>();
    map.put("SWEEP_TYPE_UNSPECIFIED", EntrySweepTypeEnum.SWEEP_TYPE_UNSPECIFIED);
    map.put("FDIC", EntrySweepTypeEnum.FDIC);
    map.put("MONEY_MARKET", EntrySweepTypeEnum.MONEY_MARKET);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<EntrySweepType> {

    protected _Serializer() {
      super(EntrySweepType.class);
    }

    @Override
    public void serialize(EntrySweepType value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<EntrySweepType> {

    protected _Deserializer() {
      super(EntrySweepType.class);
    }

    @Override
    public EntrySweepType deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return EntrySweepType.of(v);
    }
  }

  public enum EntrySweepTypeEnum {
    SWEEP_TYPE_UNSPECIFIED("SWEEP_TYPE_UNSPECIFIED"),
    FDIC("FDIC"),
    MONEY_MARKET("MONEY_MARKET"),
    ;

    private final String value;

    private EntrySweepTypeEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
