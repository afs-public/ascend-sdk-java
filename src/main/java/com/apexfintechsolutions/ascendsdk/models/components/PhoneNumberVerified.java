/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/** PhoneNumberVerified - Whether or not the customer phone number was verified */
@JsonDeserialize(using = PhoneNumberVerified._Deserializer.class)
@JsonSerialize(using = PhoneNumberVerified._Serializer.class)
public class PhoneNumberVerified {

  public static final PhoneNumberVerified VERIFICATION_STATE_UNSPECIFIED =
      new PhoneNumberVerified("VERIFICATION_STATE_UNSPECIFIED");
  public static final PhoneNumberVerified NOT_IN_SCOPE = new PhoneNumberVerified("NOT_IN_SCOPE");
  public static final PhoneNumberVerified VERIFIED = new PhoneNumberVerified("VERIFIED");
  public static final PhoneNumberVerified UNVERIFIED = new PhoneNumberVerified("UNVERIFIED");
  public static final PhoneNumberVerified INCONCLUSIVE = new PhoneNumberVerified("INCONCLUSIVE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, PhoneNumberVerified> values = createValuesMap();
  private static final Map<String, PhoneNumberVerifiedEnum> enums = createEnumsMap();

  private final String value;

  private PhoneNumberVerified(String value) {
    this.value = value;
  }

  /**
   * Returns a PhoneNumberVerified with the given value. For a specific value the returned object
   * will always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as PhoneNumberVerified
   */
  public static PhoneNumberVerified of(String value) {
    synchronized (PhoneNumberVerified.class) {
      return values.computeIfAbsent(value, v -> new PhoneNumberVerified(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<PhoneNumberVerifiedEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    PhoneNumberVerified other = (PhoneNumberVerified) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "PhoneNumberVerified [value=" + value + "]";
  }

  // return an array just like an enum
  public static PhoneNumberVerified[] values() {
    synchronized (PhoneNumberVerified.class) {
      return values.values().toArray(new PhoneNumberVerified[] {});
    }
  }

  private static final Map<String, PhoneNumberVerified> createValuesMap() {
    Map<String, PhoneNumberVerified> map = new LinkedHashMap<>();
    map.put("VERIFICATION_STATE_UNSPECIFIED", VERIFICATION_STATE_UNSPECIFIED);
    map.put("NOT_IN_SCOPE", NOT_IN_SCOPE);
    map.put("VERIFIED", VERIFIED);
    map.put("UNVERIFIED", UNVERIFIED);
    map.put("INCONCLUSIVE", INCONCLUSIVE);
    return map;
  }

  private static final Map<String, PhoneNumberVerifiedEnum> createEnumsMap() {
    Map<String, PhoneNumberVerifiedEnum> map = new HashMap<>();
    map.put(
        "VERIFICATION_STATE_UNSPECIFIED", PhoneNumberVerifiedEnum.VERIFICATION_STATE_UNSPECIFIED);
    map.put("NOT_IN_SCOPE", PhoneNumberVerifiedEnum.NOT_IN_SCOPE);
    map.put("VERIFIED", PhoneNumberVerifiedEnum.VERIFIED);
    map.put("UNVERIFIED", PhoneNumberVerifiedEnum.UNVERIFIED);
    map.put("INCONCLUSIVE", PhoneNumberVerifiedEnum.INCONCLUSIVE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<PhoneNumberVerified> {

    protected _Serializer() {
      super(PhoneNumberVerified.class);
    }

    @Override
    public void serialize(PhoneNumberVerified value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<PhoneNumberVerified> {

    protected _Deserializer() {
      super(PhoneNumberVerified.class);
    }

    @Override
    public PhoneNumberVerified deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return PhoneNumberVerified.of(v);
    }
  }

  public enum PhoneNumberVerifiedEnum {
    VERIFICATION_STATE_UNSPECIFIED("VERIFICATION_STATE_UNSPECIFIED"),
    NOT_IN_SCOPE("NOT_IN_SCOPE"),
    VERIFIED("VERIFIED"),
    UNVERIFIED("UNVERIFIED"),
    INCONCLUSIVE("INCONCLUSIVE"),
    ;

    private final String value;

    private PhoneNumberVerifiedEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
