/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * AchWithdrawalScheduleState
 *
 * <p>The state of the represented schedule
 */
@JsonDeserialize(using = AchWithdrawalScheduleState._Deserializer.class)
@JsonSerialize(using = AchWithdrawalScheduleState._Serializer.class)
public class AchWithdrawalScheduleState {

  public static final AchWithdrawalScheduleState ACTIVE = new AchWithdrawalScheduleState("ACTIVE");
  public static final AchWithdrawalScheduleState CANCELED =
      new AchWithdrawalScheduleState("CANCELED");
  public static final AchWithdrawalScheduleState COMPLETED =
      new AchWithdrawalScheduleState("COMPLETED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, AchWithdrawalScheduleState> values = createValuesMap();
  private static final Map<String, AchWithdrawalScheduleStateEnum> enums = createEnumsMap();

  private final String value;

  private AchWithdrawalScheduleState(String value) {
    this.value = value;
  }

  /**
   * Returns a AchWithdrawalScheduleState with the given value. For a specific value the returned
   * object will always be a singleton so reference equality is satisfied when the values are the
   * same.
   *
   * @param value value to be wrapped as AchWithdrawalScheduleState
   */
  public static AchWithdrawalScheduleState of(String value) {
    synchronized (AchWithdrawalScheduleState.class) {
      return values.computeIfAbsent(value, v -> new AchWithdrawalScheduleState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<AchWithdrawalScheduleStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    AchWithdrawalScheduleState other = (AchWithdrawalScheduleState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "AchWithdrawalScheduleState [value=" + value + "]";
  }

  // return an array just like an enum
  public static AchWithdrawalScheduleState[] values() {
    synchronized (AchWithdrawalScheduleState.class) {
      return values.values().toArray(new AchWithdrawalScheduleState[] {});
    }
  }

  private static final Map<String, AchWithdrawalScheduleState> createValuesMap() {
    Map<String, AchWithdrawalScheduleState> map = new LinkedHashMap<>();
    map.put("ACTIVE", ACTIVE);
    map.put("CANCELED", CANCELED);
    map.put("COMPLETED", COMPLETED);
    return map;
  }

  private static final Map<String, AchWithdrawalScheduleStateEnum> createEnumsMap() {
    Map<String, AchWithdrawalScheduleStateEnum> map = new HashMap<>();
    map.put("ACTIVE", AchWithdrawalScheduleStateEnum.ACTIVE);
    map.put("CANCELED", AchWithdrawalScheduleStateEnum.CANCELED);
    map.put("COMPLETED", AchWithdrawalScheduleStateEnum.COMPLETED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<AchWithdrawalScheduleState> {

    protected _Serializer() {
      super(AchWithdrawalScheduleState.class);
    }

    @Override
    public void serialize(
        AchWithdrawalScheduleState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<AchWithdrawalScheduleState> {

    protected _Deserializer() {
      super(AchWithdrawalScheduleState.class);
    }

    @Override
    public AchWithdrawalScheduleState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return AchWithdrawalScheduleState.of(v);
    }
  }

  public enum AchWithdrawalScheduleStateEnum {
    ACTIVE("ACTIVE"),
    CANCELED("CANCELED"),
    COMPLETED("COMPLETED"),
    ;

    private final String value;

    private AchWithdrawalScheduleStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
