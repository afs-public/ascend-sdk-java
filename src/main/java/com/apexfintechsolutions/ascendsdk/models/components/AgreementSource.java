/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * AgreementSource - An internal indicator from where the agreement was generated; Typically
 * `ACCOUNTS_SERVICE` if accessing our public APIs
 */
@JsonDeserialize(using = AgreementSource._Deserializer.class)
@JsonSerialize(using = AgreementSource._Serializer.class)
public class AgreementSource {

  public static final AgreementSource AGREEMENT_SOURCE_UNSPECIFIED =
      new AgreementSource("AGREEMENT_SOURCE_UNSPECIFIED");
  public static final AgreementSource ATLAS_FORM = new AgreementSource("ATLAS_FORM");
  public static final AgreementSource OTHER_SOURCE = new AgreementSource("OTHER_SOURCE");
  public static final AgreementSource ACCOUNTS_SERVICE = new AgreementSource("ACCOUNTS_SERVICE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, AgreementSource> values = createValuesMap();
  private static final Map<String, AgreementSourceEnum> enums = createEnumsMap();

  private final String value;

  private AgreementSource(String value) {
    this.value = value;
  }

  /**
   * Returns a AgreementSource with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as AgreementSource
   */
  public static AgreementSource of(String value) {
    synchronized (AgreementSource.class) {
      return values.computeIfAbsent(value, v -> new AgreementSource(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<AgreementSourceEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    AgreementSource other = (AgreementSource) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "AgreementSource [value=" + value + "]";
  }

  // return an array just like an enum
  public static AgreementSource[] values() {
    synchronized (AgreementSource.class) {
      return values.values().toArray(new AgreementSource[] {});
    }
  }

  private static final Map<String, AgreementSource> createValuesMap() {
    Map<String, AgreementSource> map = new LinkedHashMap<>();
    map.put("AGREEMENT_SOURCE_UNSPECIFIED", AGREEMENT_SOURCE_UNSPECIFIED);
    map.put("ATLAS_FORM", ATLAS_FORM);
    map.put("OTHER_SOURCE", OTHER_SOURCE);
    map.put("ACCOUNTS_SERVICE", ACCOUNTS_SERVICE);
    return map;
  }

  private static final Map<String, AgreementSourceEnum> createEnumsMap() {
    Map<String, AgreementSourceEnum> map = new HashMap<>();
    map.put("AGREEMENT_SOURCE_UNSPECIFIED", AgreementSourceEnum.AGREEMENT_SOURCE_UNSPECIFIED);
    map.put("ATLAS_FORM", AgreementSourceEnum.ATLAS_FORM);
    map.put("OTHER_SOURCE", AgreementSourceEnum.OTHER_SOURCE);
    map.put("ACCOUNTS_SERVICE", AgreementSourceEnum.ACCOUNTS_SERVICE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<AgreementSource> {

    protected _Serializer() {
      super(AgreementSource.class);
    }

    @Override
    public void serialize(AgreementSource value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<AgreementSource> {

    protected _Deserializer() {
      super(AgreementSource.class);
    }

    @Override
    public AgreementSource deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return AgreementSource.of(v);
    }
  }

  public enum AgreementSourceEnum {
    AGREEMENT_SOURCE_UNSPECIFIED("AGREEMENT_SOURCE_UNSPECIFIED"),
    ATLAS_FORM("ATLAS_FORM"),
    OTHER_SOURCE("OTHER_SOURCE"),
    ACCOUNTS_SERVICE("ACCOUNTS_SERVICE"),
    ;

    private final String value;

    private AgreementSourceEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
