/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/** Result - The outcome of the delivery */
@JsonDeserialize(using = Result._Deserializer.class)
@JsonSerialize(using = Result._Serializer.class)
public class Result {

  public static final Result PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED =
      new Result("PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED");
  public static final Result SUCCEEDED = new Result("SUCCEEDED");
  public static final Result FAILED = new Result("FAILED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, Result> values = createValuesMap();
  private static final Map<String, ResultEnum> enums = createEnumsMap();

  private final String value;

  private Result(String value) {
    this.value = value;
  }

  /**
   * Returns a Result with the given value. For a specific value the returned object will always be
   * a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as Result
   */
  public static Result of(String value) {
    synchronized (Result.class) {
      return values.computeIfAbsent(value, v -> new Result(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ResultEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    Result other = (Result) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "Result [value=" + value + "]";
  }

  // return an array just like an enum
  public static Result[] values() {
    synchronized (Result.class) {
      return values.values().toArray(new Result[] {});
    }
  }

  private static final Map<String, Result> createValuesMap() {
    Map<String, Result> map = new LinkedHashMap<>();
    map.put(
        "PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED",
        PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED);
    map.put("SUCCEEDED", SUCCEEDED);
    map.put("FAILED", FAILED);
    return map;
  }

  private static final Map<String, ResultEnum> createEnumsMap() {
    Map<String, ResultEnum> map = new HashMap<>();
    map.put(
        "PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED",
        ResultEnum.PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED);
    map.put("SUCCEEDED", ResultEnum.SUCCEEDED);
    map.put("FAILED", ResultEnum.FAILED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<Result> {

    protected _Serializer() {
      super(Result.class);
    }

    @Override
    public void serialize(Result value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<Result> {

    protected _Deserializer() {
      super(Result.class);
    }

    @Override
    public Result deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return Result.of(v);
    }
  }

  public enum ResultEnum {
    PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED("PUSH_SUBSCRIPTION_DELIVERY_RESULT_UNSPECIFIED"),
    SUCCEEDED("SUCCEEDED"),
    FAILED("FAILED"),
    ;

    private final String value;

    private ResultEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
