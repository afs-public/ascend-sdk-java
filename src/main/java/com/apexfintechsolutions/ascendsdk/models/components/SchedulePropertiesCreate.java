/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/** SchedulePropertiesCreate - Properties common to all transfer schedules */
public class SchedulePropertiesCreate {

  /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("occurrences")
  private Optional<Integer> occurrences;

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @JsonProperty("start_date")
  private DateCreate startDate;

  /**
   * The time unit used to calculate the interval between transfers. The time period between
   * transfers in a scheduled series is the unit of time times the multiplier
   */
  @JsonProperty("time_unit")
  private TimeUnit timeUnit;

  /**
   * The multiplier used to determine the length of the interval between transfers. The time period
   * between transfers in a scheduled series is the unit of time times the multiplier
   */
  @JsonProperty("unit_multiplier")
  private int unitMultiplier;

  @JsonCreator
  public SchedulePropertiesCreate(
      @JsonProperty("occurrences") Optional<Integer> occurrences,
      @JsonProperty("start_date") DateCreate startDate,
      @JsonProperty("time_unit") TimeUnit timeUnit,
      @JsonProperty("unit_multiplier") int unitMultiplier) {
    Utils.checkNotNull(occurrences, "occurrences");
    Utils.checkNotNull(startDate, "startDate");
    Utils.checkNotNull(timeUnit, "timeUnit");
    Utils.checkNotNull(unitMultiplier, "unitMultiplier");
    this.occurrences = occurrences;
    this.startDate = startDate;
    this.timeUnit = timeUnit;
    this.unitMultiplier = unitMultiplier;
  }

  public SchedulePropertiesCreate(DateCreate startDate, TimeUnit timeUnit, int unitMultiplier) {
    this(Optional.empty(), startDate, timeUnit, unitMultiplier);
  }

  /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
  @JsonIgnore
  public Optional<Integer> occurrences() {
    return occurrences;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @JsonIgnore
  public DateCreate startDate() {
    return startDate;
  }

  /**
   * The time unit used to calculate the interval between transfers. The time period between
   * transfers in a scheduled series is the unit of time times the multiplier
   */
  @JsonIgnore
  public TimeUnit timeUnit() {
    return timeUnit;
  }

  /**
   * The multiplier used to determine the length of the interval between transfers. The time period
   * between transfers in a scheduled series is the unit of time times the multiplier
   */
  @JsonIgnore
  public int unitMultiplier() {
    return unitMultiplier;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
  public SchedulePropertiesCreate withOccurrences(int occurrences) {
    Utils.checkNotNull(occurrences, "occurrences");
    this.occurrences = Optional.ofNullable(occurrences);
    return this;
  }

  /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
  public SchedulePropertiesCreate withOccurrences(Optional<Integer> occurrences) {
    Utils.checkNotNull(occurrences, "occurrences");
    this.occurrences = occurrences;
    return this;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public SchedulePropertiesCreate withStartDate(DateCreate startDate) {
    Utils.checkNotNull(startDate, "startDate");
    this.startDate = startDate;
    return this;
  }

  /**
   * The time unit used to calculate the interval between transfers. The time period between
   * transfers in a scheduled series is the unit of time times the multiplier
   */
  public SchedulePropertiesCreate withTimeUnit(TimeUnit timeUnit) {
    Utils.checkNotNull(timeUnit, "timeUnit");
    this.timeUnit = timeUnit;
    return this;
  }

  /**
   * The multiplier used to determine the length of the interval between transfers. The time period
   * between transfers in a scheduled series is the unit of time times the multiplier
   */
  public SchedulePropertiesCreate withUnitMultiplier(int unitMultiplier) {
    Utils.checkNotNull(unitMultiplier, "unitMultiplier");
    this.unitMultiplier = unitMultiplier;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SchedulePropertiesCreate other = (SchedulePropertiesCreate) o;
    return Objects.deepEquals(this.occurrences, other.occurrences)
        && Objects.deepEquals(this.startDate, other.startDate)
        && Objects.deepEquals(this.timeUnit, other.timeUnit)
        && Objects.deepEquals(this.unitMultiplier, other.unitMultiplier);
  }

  @Override
  public int hashCode() {
    return Objects.hash(occurrences, startDate, timeUnit, unitMultiplier);
  }

  @Override
  public String toString() {
    return Utils.toString(
        SchedulePropertiesCreate.class,
        "occurrences",
        occurrences,
        "startDate",
        startDate,
        "timeUnit",
        timeUnit,
        "unitMultiplier",
        unitMultiplier);
  }

  public static final class Builder {

    private Optional<Integer> occurrences = Optional.empty();

    private DateCreate startDate;

    private TimeUnit timeUnit;

    private Integer unitMultiplier;

    private Builder() {
      // force use of static builder() method
    }

    /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
    public Builder occurrences(int occurrences) {
      Utils.checkNotNull(occurrences, "occurrences");
      this.occurrences = Optional.ofNullable(occurrences);
      return this;
    }

    /** The number of occurrences (empty or 0 indicates unlimited occurrences) */
    public Builder occurrences(Optional<Integer> occurrences) {
      Utils.checkNotNull(occurrences, "occurrences");
      this.occurrences = occurrences;
      return this;
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder startDate(DateCreate startDate) {
      Utils.checkNotNull(startDate, "startDate");
      this.startDate = startDate;
      return this;
    }

    /**
     * The time unit used to calculate the interval between transfers. The time period between
     * transfers in a scheduled series is the unit of time times the multiplier
     */
    public Builder timeUnit(TimeUnit timeUnit) {
      Utils.checkNotNull(timeUnit, "timeUnit");
      this.timeUnit = timeUnit;
      return this;
    }

    /**
     * The multiplier used to determine the length of the interval between transfers. The time
     * period between transfers in a scheduled series is the unit of time times the multiplier
     */
    public Builder unitMultiplier(int unitMultiplier) {
      Utils.checkNotNull(unitMultiplier, "unitMultiplier");
      this.unitMultiplier = unitMultiplier;
      return this;
    }

    public SchedulePropertiesCreate build() {
      return new SchedulePropertiesCreate(occurrences, startDate, timeUnit, unitMultiplier);
    }
  }
}
