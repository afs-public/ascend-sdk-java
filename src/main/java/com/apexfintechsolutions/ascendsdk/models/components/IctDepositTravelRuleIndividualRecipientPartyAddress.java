/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import java.util.Objects;
import java.util.Optional;

/** IctDepositTravelRuleIndividualRecipientPartyAddress - The address of the party */
public class IctDepositTravelRuleIndividualRecipientPartyAddress {

  /**
   * Unstructured address lines describing the lower levels of an address.
   *
   * <p>Because values in address_lines do not have type information and may sometimes contain
   * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
   * clear. The order of address lines should be "envelope order" for the country/region of the
   * address. In places where this can vary (e.g. Japan), address_language is used to make it
   * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). This
   * way, the most specific line of an address can be selected based on the language.
   *
   * <p>The minimum permitted structural representation of an address consists of a region_code with
   * all remaining information placed in the address_lines. It would be possible to format such an
   * address very approximately without geocoding, but no semantic reasoning could be made about any
   * of the address components until it was at least partially resolved.
   *
   * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
   * the recommended way to handle completely unstructured addresses (as opposed to guessing which
   * parts of the address should be localities or administrative areas).
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("address_lines")
  private Optional<? extends List<String>> addressLines;

  /**
   * Optional. Highest administrative subdivision which is used for postal addresses of a country or
   * region. For example, this can be a state, a province, an oblast, or a prefecture. Specifically,
   * for Spain this is the province and not the autonomous community (e.g. "Barcelona" and not
   * "Catalonia"). Many countries don't use an administrative area in postal addresses. E.g. in
   * Switzerland this should be left unpopulated.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("administrative_area")
  private Optional<String> administrativeArea;

  /**
   * Optional. BCP-47 language code of the contents of this address (if known). This is often the UI
   * language of the input form or is expected to match one of the languages used in the address'
   * country/region, or their transliterated equivalents. This can affect formatting in certain
   * countries, but is not critical to the correctness of the data and will never affect any
   * validation or other non-formatting related operations.
   *
   * <p>If this value is not known, it should be omitted (rather than specifying a possibly
   * incorrect default).
   *
   * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("language_code")
  private Optional<String> languageCode;

  /**
   * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
   * comune, UK post town. In regions of the world where localities are not well defined or do not
   * fit into this structure well, leave locality empty and use address_lines.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("locality")
  private Optional<String> locality;

  /** Optional. The name of the organization at the address. */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("organization")
  private Optional<String> organization;

  /**
   * Optional. Postal code of the address. Not all countries use or require postal codes to be
   * present, but where they are used, they may trigger additional validation with other parts of
   * the address (e.g. state/zip validation in the U.S.A.).
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("postal_code")
  private Optional<String> postalCode;

  /**
   * Optional. The recipient at the address. This field may, under certain circumstances, contain
   * multiline information. For example, it might contain "care of" information.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("recipients")
  private Optional<? extends List<String>> recipients;

  /**
   * Required. CLDR region code of the country/region of the address. This is never inferred and it
   * is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
   * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
   * Example: "CH" for Switzerland.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("region_code")
  private Optional<String> regionCode;

  /**
   * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
   * revision.
   *
   * <p>All new revisions **must** be backward compatible with old revisions.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("revision")
  private Optional<Integer> revision;

  /**
   * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
   * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
   * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
   * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("sorting_code")
  private Optional<String> sortingCode;

  /**
   * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
   * districts.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("sublocality")
  private Optional<String> sublocality;

  @JsonCreator
  public IctDepositTravelRuleIndividualRecipientPartyAddress(
      @JsonProperty("address_lines") Optional<? extends List<String>> addressLines,
      @JsonProperty("administrative_area") Optional<String> administrativeArea,
      @JsonProperty("language_code") Optional<String> languageCode,
      @JsonProperty("locality") Optional<String> locality,
      @JsonProperty("organization") Optional<String> organization,
      @JsonProperty("postal_code") Optional<String> postalCode,
      @JsonProperty("recipients") Optional<? extends List<String>> recipients,
      @JsonProperty("region_code") Optional<String> regionCode,
      @JsonProperty("revision") Optional<Integer> revision,
      @JsonProperty("sorting_code") Optional<String> sortingCode,
      @JsonProperty("sublocality") Optional<String> sublocality) {
    Utils.checkNotNull(addressLines, "addressLines");
    Utils.checkNotNull(administrativeArea, "administrativeArea");
    Utils.checkNotNull(languageCode, "languageCode");
    Utils.checkNotNull(locality, "locality");
    Utils.checkNotNull(organization, "organization");
    Utils.checkNotNull(postalCode, "postalCode");
    Utils.checkNotNull(recipients, "recipients");
    Utils.checkNotNull(regionCode, "regionCode");
    Utils.checkNotNull(revision, "revision");
    Utils.checkNotNull(sortingCode, "sortingCode");
    Utils.checkNotNull(sublocality, "sublocality");
    this.addressLines = addressLines;
    this.administrativeArea = administrativeArea;
    this.languageCode = languageCode;
    this.locality = locality;
    this.organization = organization;
    this.postalCode = postalCode;
    this.recipients = recipients;
    this.regionCode = regionCode;
    this.revision = revision;
    this.sortingCode = sortingCode;
    this.sublocality = sublocality;
  }

  public IctDepositTravelRuleIndividualRecipientPartyAddress() {
    this(
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty());
  }

  /**
   * Unstructured address lines describing the lower levels of an address.
   *
   * <p>Because values in address_lines do not have type information and may sometimes contain
   * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
   * clear. The order of address lines should be "envelope order" for the country/region of the
   * address. In places where this can vary (e.g. Japan), address_language is used to make it
   * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). This
   * way, the most specific line of an address can be selected based on the language.
   *
   * <p>The minimum permitted structural representation of an address consists of a region_code with
   * all remaining information placed in the address_lines. It would be possible to format such an
   * address very approximately without geocoding, but no semantic reasoning could be made about any
   * of the address components until it was at least partially resolved.
   *
   * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
   * the recommended way to handle completely unstructured addresses (as opposed to guessing which
   * parts of the address should be localities or administrative areas).
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<List<String>> addressLines() {
    return (Optional<List<String>>) addressLines;
  }

  /**
   * Optional. Highest administrative subdivision which is used for postal addresses of a country or
   * region. For example, this can be a state, a province, an oblast, or a prefecture. Specifically,
   * for Spain this is the province and not the autonomous community (e.g. "Barcelona" and not
   * "Catalonia"). Many countries don't use an administrative area in postal addresses. E.g. in
   * Switzerland this should be left unpopulated.
   */
  @JsonIgnore
  public Optional<String> administrativeArea() {
    return administrativeArea;
  }

  /**
   * Optional. BCP-47 language code of the contents of this address (if known). This is often the UI
   * language of the input form or is expected to match one of the languages used in the address'
   * country/region, or their transliterated equivalents. This can affect formatting in certain
   * countries, but is not critical to the correctness of the data and will never affect any
   * validation or other non-formatting related operations.
   *
   * <p>If this value is not known, it should be omitted (rather than specifying a possibly
   * incorrect default).
   *
   * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
   */
  @JsonIgnore
  public Optional<String> languageCode() {
    return languageCode;
  }

  /**
   * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
   * comune, UK post town. In regions of the world where localities are not well defined or do not
   * fit into this structure well, leave locality empty and use address_lines.
   */
  @JsonIgnore
  public Optional<String> locality() {
    return locality;
  }

  /** Optional. The name of the organization at the address. */
  @JsonIgnore
  public Optional<String> organization() {
    return organization;
  }

  /**
   * Optional. Postal code of the address. Not all countries use or require postal codes to be
   * present, but where they are used, they may trigger additional validation with other parts of
   * the address (e.g. state/zip validation in the U.S.A.).
   */
  @JsonIgnore
  public Optional<String> postalCode() {
    return postalCode;
  }

  /**
   * Optional. The recipient at the address. This field may, under certain circumstances, contain
   * multiline information. For example, it might contain "care of" information.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<List<String>> recipients() {
    return (Optional<List<String>>) recipients;
  }

  /**
   * Required. CLDR region code of the country/region of the address. This is never inferred and it
   * is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
   * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
   * Example: "CH" for Switzerland.
   */
  @JsonIgnore
  public Optional<String> regionCode() {
    return regionCode;
  }

  /**
   * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
   * revision.
   *
   * <p>All new revisions **must** be backward compatible with old revisions.
   */
  @JsonIgnore
  public Optional<Integer> revision() {
    return revision;
  }

  /**
   * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
   * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
   * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
   * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
   */
  @JsonIgnore
  public Optional<String> sortingCode() {
    return sortingCode;
  }

  /**
   * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
   * districts.
   */
  @JsonIgnore
  public Optional<String> sublocality() {
    return sublocality;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * Unstructured address lines describing the lower levels of an address.
   *
   * <p>Because values in address_lines do not have type information and may sometimes contain
   * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
   * clear. The order of address lines should be "envelope order" for the country/region of the
   * address. In places where this can vary (e.g. Japan), address_language is used to make it
   * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). This
   * way, the most specific line of an address can be selected based on the language.
   *
   * <p>The minimum permitted structural representation of an address consists of a region_code with
   * all remaining information placed in the address_lines. It would be possible to format such an
   * address very approximately without geocoding, but no semantic reasoning could be made about any
   * of the address components until it was at least partially resolved.
   *
   * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
   * the recommended way to handle completely unstructured addresses (as opposed to guessing which
   * parts of the address should be localities or administrative areas).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withAddressLines(
      List<String> addressLines) {
    Utils.checkNotNull(addressLines, "addressLines");
    this.addressLines = Optional.ofNullable(addressLines);
    return this;
  }

  /**
   * Unstructured address lines describing the lower levels of an address.
   *
   * <p>Because values in address_lines do not have type information and may sometimes contain
   * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
   * clear. The order of address lines should be "envelope order" for the country/region of the
   * address. In places where this can vary (e.g. Japan), address_language is used to make it
   * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). This
   * way, the most specific line of an address can be selected based on the language.
   *
   * <p>The minimum permitted structural representation of an address consists of a region_code with
   * all remaining information placed in the address_lines. It would be possible to format such an
   * address very approximately without geocoding, but no semantic reasoning could be made about any
   * of the address components until it was at least partially resolved.
   *
   * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
   * the recommended way to handle completely unstructured addresses (as opposed to guessing which
   * parts of the address should be localities or administrative areas).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withAddressLines(
      Optional<? extends List<String>> addressLines) {
    Utils.checkNotNull(addressLines, "addressLines");
    this.addressLines = addressLines;
    return this;
  }

  /**
   * Optional. Highest administrative subdivision which is used for postal addresses of a country or
   * region. For example, this can be a state, a province, an oblast, or a prefecture. Specifically,
   * for Spain this is the province and not the autonomous community (e.g. "Barcelona" and not
   * "Catalonia"). Many countries don't use an administrative area in postal addresses. E.g. in
   * Switzerland this should be left unpopulated.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withAdministrativeArea(
      String administrativeArea) {
    Utils.checkNotNull(administrativeArea, "administrativeArea");
    this.administrativeArea = Optional.ofNullable(administrativeArea);
    return this;
  }

  /**
   * Optional. Highest administrative subdivision which is used for postal addresses of a country or
   * region. For example, this can be a state, a province, an oblast, or a prefecture. Specifically,
   * for Spain this is the province and not the autonomous community (e.g. "Barcelona" and not
   * "Catalonia"). Many countries don't use an administrative area in postal addresses. E.g. in
   * Switzerland this should be left unpopulated.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withAdministrativeArea(
      Optional<String> administrativeArea) {
    Utils.checkNotNull(administrativeArea, "administrativeArea");
    this.administrativeArea = administrativeArea;
    return this;
  }

  /**
   * Optional. BCP-47 language code of the contents of this address (if known). This is often the UI
   * language of the input form or is expected to match one of the languages used in the address'
   * country/region, or their transliterated equivalents. This can affect formatting in certain
   * countries, but is not critical to the correctness of the data and will never affect any
   * validation or other non-formatting related operations.
   *
   * <p>If this value is not known, it should be omitted (rather than specifying a possibly
   * incorrect default).
   *
   * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withLanguageCode(String languageCode) {
    Utils.checkNotNull(languageCode, "languageCode");
    this.languageCode = Optional.ofNullable(languageCode);
    return this;
  }

  /**
   * Optional. BCP-47 language code of the contents of this address (if known). This is often the UI
   * language of the input form or is expected to match one of the languages used in the address'
   * country/region, or their transliterated equivalents. This can affect formatting in certain
   * countries, but is not critical to the correctness of the data and will never affect any
   * validation or other non-formatting related operations.
   *
   * <p>If this value is not known, it should be omitted (rather than specifying a possibly
   * incorrect default).
   *
   * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withLanguageCode(
      Optional<String> languageCode) {
    Utils.checkNotNull(languageCode, "languageCode");
    this.languageCode = languageCode;
    return this;
  }

  /**
   * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
   * comune, UK post town. In regions of the world where localities are not well defined or do not
   * fit into this structure well, leave locality empty and use address_lines.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withLocality(String locality) {
    Utils.checkNotNull(locality, "locality");
    this.locality = Optional.ofNullable(locality);
    return this;
  }

  /**
   * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
   * comune, UK post town. In regions of the world where localities are not well defined or do not
   * fit into this structure well, leave locality empty and use address_lines.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withLocality(
      Optional<String> locality) {
    Utils.checkNotNull(locality, "locality");
    this.locality = locality;
    return this;
  }

  /** Optional. The name of the organization at the address. */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withOrganization(String organization) {
    Utils.checkNotNull(organization, "organization");
    this.organization = Optional.ofNullable(organization);
    return this;
  }

  /** Optional. The name of the organization at the address. */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withOrganization(
      Optional<String> organization) {
    Utils.checkNotNull(organization, "organization");
    this.organization = organization;
    return this;
  }

  /**
   * Optional. Postal code of the address. Not all countries use or require postal codes to be
   * present, but where they are used, they may trigger additional validation with other parts of
   * the address (e.g. state/zip validation in the U.S.A.).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withPostalCode(String postalCode) {
    Utils.checkNotNull(postalCode, "postalCode");
    this.postalCode = Optional.ofNullable(postalCode);
    return this;
  }

  /**
   * Optional. Postal code of the address. Not all countries use or require postal codes to be
   * present, but where they are used, they may trigger additional validation with other parts of
   * the address (e.g. state/zip validation in the U.S.A.).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withPostalCode(
      Optional<String> postalCode) {
    Utils.checkNotNull(postalCode, "postalCode");
    this.postalCode = postalCode;
    return this;
  }

  /**
   * Optional. The recipient at the address. This field may, under certain circumstances, contain
   * multiline information. For example, it might contain "care of" information.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRecipients(
      List<String> recipients) {
    Utils.checkNotNull(recipients, "recipients");
    this.recipients = Optional.ofNullable(recipients);
    return this;
  }

  /**
   * Optional. The recipient at the address. This field may, under certain circumstances, contain
   * multiline information. For example, it might contain "care of" information.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRecipients(
      Optional<? extends List<String>> recipients) {
    Utils.checkNotNull(recipients, "recipients");
    this.recipients = recipients;
    return this;
  }

  /**
   * Required. CLDR region code of the country/region of the address. This is never inferred and it
   * is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
   * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
   * Example: "CH" for Switzerland.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRegionCode(String regionCode) {
    Utils.checkNotNull(regionCode, "regionCode");
    this.regionCode = Optional.ofNullable(regionCode);
    return this;
  }

  /**
   * Required. CLDR region code of the country/region of the address. This is never inferred and it
   * is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
   * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
   * Example: "CH" for Switzerland.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRegionCode(
      Optional<String> regionCode) {
    Utils.checkNotNull(regionCode, "regionCode");
    this.regionCode = regionCode;
    return this;
  }

  /**
   * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
   * revision.
   *
   * <p>All new revisions **must** be backward compatible with old revisions.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRevision(int revision) {
    Utils.checkNotNull(revision, "revision");
    this.revision = Optional.ofNullable(revision);
    return this;
  }

  /**
   * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
   * revision.
   *
   * <p>All new revisions **must** be backward compatible with old revisions.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withRevision(
      Optional<Integer> revision) {
    Utils.checkNotNull(revision, "revision");
    this.revision = revision;
    return this;
  }

  /**
   * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
   * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
   * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
   * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withSortingCode(String sortingCode) {
    Utils.checkNotNull(sortingCode, "sortingCode");
    this.sortingCode = Optional.ofNullable(sortingCode);
    return this;
  }

  /**
   * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
   * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
   * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
   * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withSortingCode(
      Optional<String> sortingCode) {
    Utils.checkNotNull(sortingCode, "sortingCode");
    this.sortingCode = sortingCode;
    return this;
  }

  /**
   * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
   * districts.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withSublocality(String sublocality) {
    Utils.checkNotNull(sublocality, "sublocality");
    this.sublocality = Optional.ofNullable(sublocality);
    return this;
  }

  /**
   * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
   * districts.
   */
  public IctDepositTravelRuleIndividualRecipientPartyAddress withSublocality(
      Optional<String> sublocality) {
    Utils.checkNotNull(sublocality, "sublocality");
    this.sublocality = sublocality;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IctDepositTravelRuleIndividualRecipientPartyAddress other =
        (IctDepositTravelRuleIndividualRecipientPartyAddress) o;
    return Objects.deepEquals(this.addressLines, other.addressLines)
        && Objects.deepEquals(this.administrativeArea, other.administrativeArea)
        && Objects.deepEquals(this.languageCode, other.languageCode)
        && Objects.deepEquals(this.locality, other.locality)
        && Objects.deepEquals(this.organization, other.organization)
        && Objects.deepEquals(this.postalCode, other.postalCode)
        && Objects.deepEquals(this.recipients, other.recipients)
        && Objects.deepEquals(this.regionCode, other.regionCode)
        && Objects.deepEquals(this.revision, other.revision)
        && Objects.deepEquals(this.sortingCode, other.sortingCode)
        && Objects.deepEquals(this.sublocality, other.sublocality);
  }

  @Override
  public int hashCode() {
    return Objects.hash(
        addressLines,
        administrativeArea,
        languageCode,
        locality,
        organization,
        postalCode,
        recipients,
        regionCode,
        revision,
        sortingCode,
        sublocality);
  }

  @Override
  public String toString() {
    return Utils.toString(
        IctDepositTravelRuleIndividualRecipientPartyAddress.class,
        "addressLines",
        addressLines,
        "administrativeArea",
        administrativeArea,
        "languageCode",
        languageCode,
        "locality",
        locality,
        "organization",
        organization,
        "postalCode",
        postalCode,
        "recipients",
        recipients,
        "regionCode",
        regionCode,
        "revision",
        revision,
        "sortingCode",
        sortingCode,
        "sublocality",
        sublocality);
  }

  public static final class Builder {

    private Optional<? extends List<String>> addressLines = Optional.empty();

    private Optional<String> administrativeArea = Optional.empty();

    private Optional<String> languageCode = Optional.empty();

    private Optional<String> locality = Optional.empty();

    private Optional<String> organization = Optional.empty();

    private Optional<String> postalCode = Optional.empty();

    private Optional<? extends List<String>> recipients = Optional.empty();

    private Optional<String> regionCode = Optional.empty();

    private Optional<Integer> revision = Optional.empty();

    private Optional<String> sortingCode = Optional.empty();

    private Optional<String> sublocality = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * Unstructured address lines describing the lower levels of an address.
     *
     * <p>Because values in address_lines do not have type information and may sometimes contain
     * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
     * clear. The order of address lines should be "envelope order" for the country/region of the
     * address. In places where this can vary (e.g. Japan), address_language is used to make it
     * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large).
     * This way, the most specific line of an address can be selected based on the language.
     *
     * <p>The minimum permitted structural representation of an address consists of a region_code
     * with all remaining information placed in the address_lines. It would be possible to format
     * such an address very approximately without geocoding, but no semantic reasoning could be made
     * about any of the address components until it was at least partially resolved.
     *
     * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
     * the recommended way to handle completely unstructured addresses (as opposed to guessing which
     * parts of the address should be localities or administrative areas).
     */
    public Builder addressLines(List<String> addressLines) {
      Utils.checkNotNull(addressLines, "addressLines");
      this.addressLines = Optional.ofNullable(addressLines);
      return this;
    }

    /**
     * Unstructured address lines describing the lower levels of an address.
     *
     * <p>Because values in address_lines do not have type information and may sometimes contain
     * multiple values in a single field (e.g. "Austin, TX"), it is important that the line order is
     * clear. The order of address lines should be "envelope order" for the country/region of the
     * address. In places where this can vary (e.g. Japan), address_language is used to make it
     * explicit (e.g. "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large).
     * This way, the most specific line of an address can be selected based on the language.
     *
     * <p>The minimum permitted structural representation of an address consists of a region_code
     * with all remaining information placed in the address_lines. It would be possible to format
     * such an address very approximately without geocoding, but no semantic reasoning could be made
     * about any of the address components until it was at least partially resolved.
     *
     * <p>Creating an address only containing a region_code and address_lines, and then geocoding is
     * the recommended way to handle completely unstructured addresses (as opposed to guessing which
     * parts of the address should be localities or administrative areas).
     */
    public Builder addressLines(Optional<? extends List<String>> addressLines) {
      Utils.checkNotNull(addressLines, "addressLines");
      this.addressLines = addressLines;
      return this;
    }

    /**
     * Optional. Highest administrative subdivision which is used for postal addresses of a country
     * or region. For example, this can be a state, a province, an oblast, or a prefecture.
     * Specifically, for Spain this is the province and not the autonomous community (e.g.
     * "Barcelona" and not "Catalonia"). Many countries don't use an administrative area in postal
     * addresses. E.g. in Switzerland this should be left unpopulated.
     */
    public Builder administrativeArea(String administrativeArea) {
      Utils.checkNotNull(administrativeArea, "administrativeArea");
      this.administrativeArea = Optional.ofNullable(administrativeArea);
      return this;
    }

    /**
     * Optional. Highest administrative subdivision which is used for postal addresses of a country
     * or region. For example, this can be a state, a province, an oblast, or a prefecture.
     * Specifically, for Spain this is the province and not the autonomous community (e.g.
     * "Barcelona" and not "Catalonia"). Many countries don't use an administrative area in postal
     * addresses. E.g. in Switzerland this should be left unpopulated.
     */
    public Builder administrativeArea(Optional<String> administrativeArea) {
      Utils.checkNotNull(administrativeArea, "administrativeArea");
      this.administrativeArea = administrativeArea;
      return this;
    }

    /**
     * Optional. BCP-47 language code of the contents of this address (if known). This is often the
     * UI language of the input form or is expected to match one of the languages used in the
     * address' country/region, or their transliterated equivalents. This can affect formatting in
     * certain countries, but is not critical to the correctness of the data and will never affect
     * any validation or other non-formatting related operations.
     *
     * <p>If this value is not known, it should be omitted (rather than specifying a possibly
     * incorrect default).
     *
     * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
     */
    public Builder languageCode(String languageCode) {
      Utils.checkNotNull(languageCode, "languageCode");
      this.languageCode = Optional.ofNullable(languageCode);
      return this;
    }

    /**
     * Optional. BCP-47 language code of the contents of this address (if known). This is often the
     * UI language of the input form or is expected to match one of the languages used in the
     * address' country/region, or their transliterated equivalents. This can affect formatting in
     * certain countries, but is not critical to the correctness of the data and will never affect
     * any validation or other non-formatting related operations.
     *
     * <p>If this value is not known, it should be omitted (rather than specifying a possibly
     * incorrect default).
     *
     * <p>Examples: "zh-Hant", "ja", "ja-Latn", "en".
     */
    public Builder languageCode(Optional<String> languageCode) {
      Utils.checkNotNull(languageCode, "languageCode");
      this.languageCode = languageCode;
      return this;
    }

    /**
     * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
     * comune, UK post town. In regions of the world where localities are not well defined or do not
     * fit into this structure well, leave locality empty and use address_lines.
     */
    public Builder locality(String locality) {
      Utils.checkNotNull(locality, "locality");
      this.locality = Optional.ofNullable(locality);
      return this;
    }

    /**
     * Optional. Generally refers to the city/town portion of the address. Examples: US city, IT
     * comune, UK post town. In regions of the world where localities are not well defined or do not
     * fit into this structure well, leave locality empty and use address_lines.
     */
    public Builder locality(Optional<String> locality) {
      Utils.checkNotNull(locality, "locality");
      this.locality = locality;
      return this;
    }

    /** Optional. The name of the organization at the address. */
    public Builder organization(String organization) {
      Utils.checkNotNull(organization, "organization");
      this.organization = Optional.ofNullable(organization);
      return this;
    }

    /** Optional. The name of the organization at the address. */
    public Builder organization(Optional<String> organization) {
      Utils.checkNotNull(organization, "organization");
      this.organization = organization;
      return this;
    }

    /**
     * Optional. Postal code of the address. Not all countries use or require postal codes to be
     * present, but where they are used, they may trigger additional validation with other parts of
     * the address (e.g. state/zip validation in the U.S.A.).
     */
    public Builder postalCode(String postalCode) {
      Utils.checkNotNull(postalCode, "postalCode");
      this.postalCode = Optional.ofNullable(postalCode);
      return this;
    }

    /**
     * Optional. Postal code of the address. Not all countries use or require postal codes to be
     * present, but where they are used, they may trigger additional validation with other parts of
     * the address (e.g. state/zip validation in the U.S.A.).
     */
    public Builder postalCode(Optional<String> postalCode) {
      Utils.checkNotNull(postalCode, "postalCode");
      this.postalCode = postalCode;
      return this;
    }

    /**
     * Optional. The recipient at the address. This field may, under certain circumstances, contain
     * multiline information. For example, it might contain "care of" information.
     */
    public Builder recipients(List<String> recipients) {
      Utils.checkNotNull(recipients, "recipients");
      this.recipients = Optional.ofNullable(recipients);
      return this;
    }

    /**
     * Optional. The recipient at the address. This field may, under certain circumstances, contain
     * multiline information. For example, it might contain "care of" information.
     */
    public Builder recipients(Optional<? extends List<String>> recipients) {
      Utils.checkNotNull(recipients, "recipients");
      this.recipients = recipients;
      return this;
    }

    /**
     * Required. CLDR region code of the country/region of the address. This is never inferred and
     * it is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
     * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
     * Example: "CH" for Switzerland.
     */
    public Builder regionCode(String regionCode) {
      Utils.checkNotNull(regionCode, "regionCode");
      this.regionCode = Optional.ofNullable(regionCode);
      return this;
    }

    /**
     * Required. CLDR region code of the country/region of the address. This is never inferred and
     * it is up to the user to ensure the value is correct. See http://cldr.unicode.org/ and
     * http://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details.
     * Example: "CH" for Switzerland.
     */
    public Builder regionCode(Optional<String> regionCode) {
      Utils.checkNotNull(regionCode, "regionCode");
      this.regionCode = regionCode;
      return this;
    }

    /**
     * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
     * revision.
     *
     * <p>All new revisions **must** be backward compatible with old revisions.
     */
    public Builder revision(int revision) {
      Utils.checkNotNull(revision, "revision");
      this.revision = Optional.ofNullable(revision);
      return this;
    }

    /**
     * The schema revision of the `PostalAddress`. This must be set to 0, which is the latest
     * revision.
     *
     * <p>All new revisions **must** be backward compatible with old revisions.
     */
    public Builder revision(Optional<Integer> revision) {
      Utils.checkNotNull(revision, "revision");
      this.revision = revision;
      return this;
    }

    /**
     * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
     * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
     * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
     * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
     */
    public Builder sortingCode(String sortingCode) {
      Utils.checkNotNull(sortingCode, "sortingCode");
      this.sortingCode = Optional.ofNullable(sortingCode);
      return this;
    }

    /**
     * Optional. Additional, country-specific, sorting code. This is not used in most regions. Where
     * it is used, the value is either a string like "CEDEX", optionally followed by a number (e.g.
     * "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area
     * indicator" (Malawi) or "post office indicator" (e.g. Côte d'Ivoire).
     */
    public Builder sortingCode(Optional<String> sortingCode) {
      Utils.checkNotNull(sortingCode, "sortingCode");
      this.sortingCode = sortingCode;
      return this;
    }

    /**
     * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
     * districts.
     */
    public Builder sublocality(String sublocality) {
      Utils.checkNotNull(sublocality, "sublocality");
      this.sublocality = Optional.ofNullable(sublocality);
      return this;
    }

    /**
     * Optional. Sublocality of the address. For example, this can be neighborhoods, boroughs,
     * districts.
     */
    public Builder sublocality(Optional<String> sublocality) {
      Utils.checkNotNull(sublocality, "sublocality");
      this.sublocality = sublocality;
      return this;
    }

    public IctDepositTravelRuleIndividualRecipientPartyAddress build() {
      return new IctDepositTravelRuleIndividualRecipientPartyAddress(
          addressLines,
          administrativeArea,
          languageCode,
          locality,
          organization,
          postalCode,
          recipients,
          regionCode,
          revision,
          sortingCode,
          sublocality);
    }
  }
}
