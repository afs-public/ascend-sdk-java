/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * RedemptionFrequency
 *
 * <p>Indicates how frequently an account can redeem the asset.
 */
@JsonDeserialize(using = RedemptionFrequency._Deserializer.class)
@JsonSerialize(using = RedemptionFrequency._Serializer.class)
public class RedemptionFrequency {

  public static final RedemptionFrequency FREQUENCY_UNSPECIFIED =
      new RedemptionFrequency("FREQUENCY_UNSPECIFIED");
  public static final RedemptionFrequency DAILY = new RedemptionFrequency("DAILY");
  public static final RedemptionFrequency WEEKLY = new RedemptionFrequency("WEEKLY");
  public static final RedemptionFrequency BI_WEEKLY = new RedemptionFrequency("BI_WEEKLY");
  public static final RedemptionFrequency MONTHLY = new RedemptionFrequency("MONTHLY");
  public static final RedemptionFrequency QUARTERLY = new RedemptionFrequency("QUARTERLY");
  public static final RedemptionFrequency BI_ANNUALLY = new RedemptionFrequency("BI_ANNUALLY");
  public static final RedemptionFrequency ANNUALLY = new RedemptionFrequency("ANNUALLY");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, RedemptionFrequency> values = createValuesMap();
  private static final Map<String, RedemptionFrequencyEnum> enums = createEnumsMap();

  private final String value;

  private RedemptionFrequency(String value) {
    this.value = value;
  }

  /**
   * Returns a RedemptionFrequency with the given value. For a specific value the returned object
   * will always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as RedemptionFrequency
   */
  public static RedemptionFrequency of(String value) {
    synchronized (RedemptionFrequency.class) {
      return values.computeIfAbsent(value, v -> new RedemptionFrequency(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<RedemptionFrequencyEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    RedemptionFrequency other = (RedemptionFrequency) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "RedemptionFrequency [value=" + value + "]";
  }

  // return an array just like an enum
  public static RedemptionFrequency[] values() {
    synchronized (RedemptionFrequency.class) {
      return values.values().toArray(new RedemptionFrequency[] {});
    }
  }

  private static final Map<String, RedemptionFrequency> createValuesMap() {
    Map<String, RedemptionFrequency> map = new LinkedHashMap<>();
    map.put("FREQUENCY_UNSPECIFIED", FREQUENCY_UNSPECIFIED);
    map.put("DAILY", DAILY);
    map.put("WEEKLY", WEEKLY);
    map.put("BI_WEEKLY", BI_WEEKLY);
    map.put("MONTHLY", MONTHLY);
    map.put("QUARTERLY", QUARTERLY);
    map.put("BI_ANNUALLY", BI_ANNUALLY);
    map.put("ANNUALLY", ANNUALLY);
    return map;
  }

  private static final Map<String, RedemptionFrequencyEnum> createEnumsMap() {
    Map<String, RedemptionFrequencyEnum> map = new HashMap<>();
    map.put("FREQUENCY_UNSPECIFIED", RedemptionFrequencyEnum.FREQUENCY_UNSPECIFIED);
    map.put("DAILY", RedemptionFrequencyEnum.DAILY);
    map.put("WEEKLY", RedemptionFrequencyEnum.WEEKLY);
    map.put("BI_WEEKLY", RedemptionFrequencyEnum.BI_WEEKLY);
    map.put("MONTHLY", RedemptionFrequencyEnum.MONTHLY);
    map.put("QUARTERLY", RedemptionFrequencyEnum.QUARTERLY);
    map.put("BI_ANNUALLY", RedemptionFrequencyEnum.BI_ANNUALLY);
    map.put("ANNUALLY", RedemptionFrequencyEnum.ANNUALLY);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<RedemptionFrequency> {

    protected _Serializer() {
      super(RedemptionFrequency.class);
    }

    @Override
    public void serialize(RedemptionFrequency value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<RedemptionFrequency> {

    protected _Deserializer() {
      super(RedemptionFrequency.class);
    }

    @Override
    public RedemptionFrequency deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return RedemptionFrequency.of(v);
    }
  }

  public enum RedemptionFrequencyEnum {
    FREQUENCY_UNSPECIFIED("FREQUENCY_UNSPECIFIED"),
    DAILY("DAILY"),
    WEEKLY("WEEKLY"),
    BI_WEEKLY("BI_WEEKLY"),
    MONTHLY("MONTHLY"),
    QUARTERLY("QUARTERLY"),
    BI_ANNUALLY("BI_ANNUALLY"),
    ANNUALLY("ANNUALLY"),
    ;

    private final String value;

    private RedemptionFrequencyEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
