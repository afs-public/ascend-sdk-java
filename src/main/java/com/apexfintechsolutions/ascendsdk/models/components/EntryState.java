/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * EntryState
 *
 * <p>Set to be NEW for BookEntries, other statuses will be assigned via ModifyActivities; the state
 * of the entry
 */
@JsonDeserialize(using = EntryState._Deserializer.class)
@JsonSerialize(using = EntryState._Serializer.class)
public class EntryState {

  public static final EntryState ENTRY_STATE_UNSPECIFIED =
      new EntryState("ENTRY_STATE_UNSPECIFIED");
  public static final EntryState NEW = new EntryState("NEW");
  public static final EntryState REVERSAL = new EntryState("REVERSAL");
  public static final EntryState CORRECTION = new EntryState("CORRECTION");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, EntryState> values = createValuesMap();
  private static final Map<String, EntryStateEnum> enums = createEnumsMap();

  private final String value;

  private EntryState(String value) {
    this.value = value;
  }

  /**
   * Returns a EntryState with the given value. For a specific value the returned object will always
   * be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as EntryState
   */
  public static EntryState of(String value) {
    synchronized (EntryState.class) {
      return values.computeIfAbsent(value, v -> new EntryState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<EntryStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    EntryState other = (EntryState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "EntryState [value=" + value + "]";
  }

  // return an array just like an enum
  public static EntryState[] values() {
    synchronized (EntryState.class) {
      return values.values().toArray(new EntryState[] {});
    }
  }

  private static final Map<String, EntryState> createValuesMap() {
    Map<String, EntryState> map = new LinkedHashMap<>();
    map.put("ENTRY_STATE_UNSPECIFIED", ENTRY_STATE_UNSPECIFIED);
    map.put("NEW", NEW);
    map.put("REVERSAL", REVERSAL);
    map.put("CORRECTION", CORRECTION);
    return map;
  }

  private static final Map<String, EntryStateEnum> createEnumsMap() {
    Map<String, EntryStateEnum> map = new HashMap<>();
    map.put("ENTRY_STATE_UNSPECIFIED", EntryStateEnum.ENTRY_STATE_UNSPECIFIED);
    map.put("NEW", EntryStateEnum.NEW);
    map.put("REVERSAL", EntryStateEnum.REVERSAL);
    map.put("CORRECTION", EntryStateEnum.CORRECTION);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<EntryState> {

    protected _Serializer() {
      super(EntryState.class);
    }

    @Override
    public void serialize(EntryState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<EntryState> {

    protected _Deserializer() {
      super(EntryState.class);
    }

    @Override
    public EntryState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return EntryState.of(v);
    }
  }

  public enum EntryStateEnum {
    ENTRY_STATE_UNSPECIFIED("ENTRY_STATE_UNSPECIFIED"),
    NEW("NEW"),
    REVERSAL("REVERSAL"),
    CORRECTION("CORRECTION"),
    ;

    private final String value;

    private EntryStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
