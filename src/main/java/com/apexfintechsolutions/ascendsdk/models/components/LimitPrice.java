/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * LimitPrice
 *
 * <p>The limit price for this order.
 */
public class LimitPrice {
  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 4 decimal places in the USD currency.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("price")
  private JsonNullable<? extends OrderPrice> price;

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("type")
  private Optional<? extends OrderLimitPriceType> type;

  @JsonCreator
  public LimitPrice(
      @JsonProperty("price") JsonNullable<? extends OrderPrice> price,
      @JsonProperty("type") Optional<? extends OrderLimitPriceType> type) {
    Utils.checkNotNull(price, "price");
    Utils.checkNotNull(type, "type");
    this.price = price;
    this.type = type;
  }

  public LimitPrice() {
    this(JsonNullable.undefined(), Optional.empty());
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 4 decimal places in the USD currency.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public JsonNullable<OrderPrice> price() {
    return (JsonNullable<OrderPrice>) price;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OrderLimitPriceType> type() {
    return (Optional<OrderLimitPriceType>) type;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 4 decimal places in the USD currency.
   */
  public LimitPrice withPrice(OrderPrice price) {
    Utils.checkNotNull(price, "price");
    this.price = JsonNullable.of(price);
    return this;
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 4 decimal places in the USD currency.
   */
  public LimitPrice withPrice(JsonNullable<? extends OrderPrice> price) {
    Utils.checkNotNull(price, "price");
    this.price = price;
    return this;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  public LimitPrice withType(OrderLimitPriceType type) {
    Utils.checkNotNull(type, "type");
    this.type = Optional.ofNullable(type);
    return this;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  public LimitPrice withType(Optional<? extends OrderLimitPriceType> type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LimitPrice other = (LimitPrice) o;
    return Utils.enhancedDeepEquals(this.price, other.price)
        && Utils.enhancedDeepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(price, type);
  }

  @Override
  public String toString() {
    return Utils.toString(LimitPrice.class, "price", price, "type", type);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private JsonNullable<? extends OrderPrice> price = JsonNullable.undefined();

    private Optional<? extends OrderLimitPriceType> type = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 4 decimal places in the USD currency.
     */
    public Builder price(OrderPrice price) {
      Utils.checkNotNull(price, "price");
      this.price = JsonNullable.of(price);
      return this;
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 4 decimal places in the USD currency.
     */
    public Builder price(JsonNullable<? extends OrderPrice> price) {
      Utils.checkNotNull(price, "price");
      this.price = price;
      return this;
    }

    /**
     * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
     * for fixed income orders.
     */
    public Builder type(OrderLimitPriceType type) {
      Utils.checkNotNull(type, "type");
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
     * for fixed income orders.
     */
    public Builder type(Optional<? extends OrderLimitPriceType> type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public LimitPrice build() {

      return new LimitPrice(price, type);
    }
  }
}
