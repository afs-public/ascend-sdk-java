/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * TimeUnit - The time unit used to calculate the interval between transfers. The time period
 * between transfers in a scheduled series is the unit of time times the multiplier
 */
@JsonDeserialize(using = TimeUnit._Deserializer.class)
@JsonSerialize(using = TimeUnit._Serializer.class)
public class TimeUnit {

  public static final TimeUnit DAY = new TimeUnit("DAY");
  public static final TimeUnit WEEK = new TimeUnit("WEEK");
  public static final TimeUnit MONTH = new TimeUnit("MONTH");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, TimeUnit> values = createValuesMap();
  private static final Map<String, TimeUnitEnum> enums = createEnumsMap();

  private final String value;

  private TimeUnit(String value) {
    this.value = value;
  }

  /**
   * Returns a TimeUnit with the given value. For a specific value the returned object will always
   * be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as TimeUnit
   */
  public static TimeUnit of(String value) {
    synchronized (TimeUnit.class) {
      return values.computeIfAbsent(value, v -> new TimeUnit(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<TimeUnitEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    TimeUnit other = (TimeUnit) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "TimeUnit [value=" + value + "]";
  }

  // return an array just like an enum
  public static TimeUnit[] values() {
    synchronized (TimeUnit.class) {
      return values.values().toArray(new TimeUnit[] {});
    }
  }

  private static final Map<String, TimeUnit> createValuesMap() {
    Map<String, TimeUnit> map = new LinkedHashMap<>();
    map.put("DAY", DAY);
    map.put("WEEK", WEEK);
    map.put("MONTH", MONTH);
    return map;
  }

  private static final Map<String, TimeUnitEnum> createEnumsMap() {
    Map<String, TimeUnitEnum> map = new HashMap<>();
    map.put("DAY", TimeUnitEnum.DAY);
    map.put("WEEK", TimeUnitEnum.WEEK);
    map.put("MONTH", TimeUnitEnum.MONTH);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<TimeUnit> {

    protected _Serializer() {
      super(TimeUnit.class);
    }

    @Override
    public void serialize(TimeUnit value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<TimeUnit> {

    protected _Deserializer() {
      super(TimeUnit.class);
    }

    @Override
    public TimeUnit deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return TimeUnit.of(v);
    }
  }

  public enum TimeUnitEnum {
    DAY("DAY"),
    WEEK("WEEK"),
    MONTH("MONTH"),
    ;

    private final String value;

    private TimeUnitEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
