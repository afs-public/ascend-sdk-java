/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * ActivityState
 *
 * <p>The state of the activity
 */
@JsonDeserialize(using = ActivityState._Deserializer.class)
@JsonSerialize(using = ActivityState._Serializer.class)
public class ActivityState {

  public static final ActivityState ACTIVITY_STATE_UNSPECIFIED =
      new ActivityState("ACTIVITY_STATE_UNSPECIFIED");
  public static final ActivityState PENDING = new ActivityState("PENDING");
  public static final ActivityState CURRENT = new ActivityState("CURRENT");
  public static final ActivityState CANCELED = new ActivityState("CANCELED");
  public static final ActivityState REBOOKED = new ActivityState("REBOOKED");
  public static final ActivityState REBOOK = new ActivityState("REBOOK");
  public static final ActivityState CANCEL = new ActivityState("CANCEL");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, ActivityState> values = createValuesMap();
  private static final Map<String, ActivityStateEnum> enums = createEnumsMap();

  private final String value;

  private ActivityState(String value) {
    this.value = value;
  }

  /**
   * Returns a ActivityState with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as ActivityState
   */
  public static ActivityState of(String value) {
    synchronized (ActivityState.class) {
      return values.computeIfAbsent(value, v -> new ActivityState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ActivityStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    ActivityState other = (ActivityState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "ActivityState [value=" + value + "]";
  }

  // return an array just like an enum
  public static ActivityState[] values() {
    synchronized (ActivityState.class) {
      return values.values().toArray(new ActivityState[] {});
    }
  }

  private static final Map<String, ActivityState> createValuesMap() {
    Map<String, ActivityState> map = new LinkedHashMap<>();
    map.put("ACTIVITY_STATE_UNSPECIFIED", ACTIVITY_STATE_UNSPECIFIED);
    map.put("PENDING", PENDING);
    map.put("CURRENT", CURRENT);
    map.put("CANCELED", CANCELED);
    map.put("REBOOKED", REBOOKED);
    map.put("REBOOK", REBOOK);
    map.put("CANCEL", CANCEL);
    return map;
  }

  private static final Map<String, ActivityStateEnum> createEnumsMap() {
    Map<String, ActivityStateEnum> map = new HashMap<>();
    map.put("ACTIVITY_STATE_UNSPECIFIED", ActivityStateEnum.ACTIVITY_STATE_UNSPECIFIED);
    map.put("PENDING", ActivityStateEnum.PENDING);
    map.put("CURRENT", ActivityStateEnum.CURRENT);
    map.put("CANCELED", ActivityStateEnum.CANCELED);
    map.put("REBOOKED", ActivityStateEnum.REBOOKED);
    map.put("REBOOK", ActivityStateEnum.REBOOK);
    map.put("CANCEL", ActivityStateEnum.CANCEL);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<ActivityState> {

    protected _Serializer() {
      super(ActivityState.class);
    }

    @Override
    public void serialize(ActivityState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<ActivityState> {

    protected _Deserializer() {
      super(ActivityState.class);
    }

    @Override
    public ActivityState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return ActivityState.of(v);
    }
  }

  public enum ActivityStateEnum {
    ACTIVITY_STATE_UNSPECIFIED("ACTIVITY_STATE_UNSPECIFIED"),
    PENDING("PENDING"),
    CURRENT("CURRENT"),
    CANCELED("CANCELED"),
    REBOOKED("REBOOKED"),
    REBOOK("REBOOK"),
    CANCEL("CANCEL"),
    ;

    private final String value;

    private ActivityStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
