/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
@JsonDeserialize(using = PartyResidencyStatus._Deserializer.class)
@JsonSerialize(using = PartyResidencyStatus._Serializer.class)
public class PartyResidencyStatus {

  public static final PartyResidencyStatus RESIDENCY_STATUS_UNSPECIFIED =
      new PartyResidencyStatus("RESIDENCY_STATUS_UNSPECIFIED");
  public static final PartyResidencyStatus US_PERMANENT_RESIDENT =
      new PartyResidencyStatus("US_PERMANENT_RESIDENT");
  public static final PartyResidencyStatus US_TEMPORARY_RESIDENT =
      new PartyResidencyStatus("US_TEMPORARY_RESIDENT");
  public static final PartyResidencyStatus US_NON_RESIDENT =
      new PartyResidencyStatus("US_NON_RESIDENT");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, PartyResidencyStatus> values = createValuesMap();
  private static final Map<String, PartyResidencyStatusEnum> enums = createEnumsMap();

  private final String value;

  private PartyResidencyStatus(String value) {
    this.value = value;
  }

  /**
   * Returns a PartyResidencyStatus with the given value. For a specific value the returned object
   * will always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as PartyResidencyStatus
   */
  public static PartyResidencyStatus of(String value) {
    synchronized (PartyResidencyStatus.class) {
      return values.computeIfAbsent(value, v -> new PartyResidencyStatus(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<PartyResidencyStatusEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    PartyResidencyStatus other = (PartyResidencyStatus) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "PartyResidencyStatus [value=" + value + "]";
  }

  // return an array just like an enum
  public static PartyResidencyStatus[] values() {
    synchronized (PartyResidencyStatus.class) {
      return values.values().toArray(new PartyResidencyStatus[] {});
    }
  }

  private static final Map<String, PartyResidencyStatus> createValuesMap() {
    Map<String, PartyResidencyStatus> map = new LinkedHashMap<>();
    map.put("RESIDENCY_STATUS_UNSPECIFIED", RESIDENCY_STATUS_UNSPECIFIED);
    map.put("US_PERMANENT_RESIDENT", US_PERMANENT_RESIDENT);
    map.put("US_TEMPORARY_RESIDENT", US_TEMPORARY_RESIDENT);
    map.put("US_NON_RESIDENT", US_NON_RESIDENT);
    return map;
  }

  private static final Map<String, PartyResidencyStatusEnum> createEnumsMap() {
    Map<String, PartyResidencyStatusEnum> map = new HashMap<>();
    map.put("RESIDENCY_STATUS_UNSPECIFIED", PartyResidencyStatusEnum.RESIDENCY_STATUS_UNSPECIFIED);
    map.put("US_PERMANENT_RESIDENT", PartyResidencyStatusEnum.US_PERMANENT_RESIDENT);
    map.put("US_TEMPORARY_RESIDENT", PartyResidencyStatusEnum.US_TEMPORARY_RESIDENT);
    map.put("US_NON_RESIDENT", PartyResidencyStatusEnum.US_NON_RESIDENT);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<PartyResidencyStatus> {

    protected _Serializer() {
      super(PartyResidencyStatus.class);
    }

    @Override
    public void serialize(PartyResidencyStatus value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<PartyResidencyStatus> {

    protected _Deserializer() {
      super(PartyResidencyStatus.class);
    }

    @Override
    public PartyResidencyStatus deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return PartyResidencyStatus.of(v);
    }
  }

  public enum PartyResidencyStatusEnum {
    RESIDENCY_STATUS_UNSPECIFIED("RESIDENCY_STATUS_UNSPECIFIED"),
    US_PERMANENT_RESIDENT("US_PERMANENT_RESIDENT"),
    US_TEMPORARY_RESIDENT("US_TEMPORARY_RESIDENT"),
    US_NON_RESIDENT("US_NON_RESIDENT"),
    ;

    private final String value;

    private PartyResidencyStatusEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
