/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/** HttpCallback - The information about an HTTP target callback */
public class HttpCallback {

  /**
   * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
   * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("timeout_seconds")
  private Optional<Integer> timeoutSeconds;

  /**
   * The URL address of the client HTTP server that will receive the events via POST; URLs must be
   * in the form of https://{domain}[/{path}]
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("url")
  private Optional<String> url;

  @JsonCreator
  public HttpCallback(
      @JsonProperty("timeout_seconds") Optional<Integer> timeoutSeconds,
      @JsonProperty("url") Optional<String> url) {
    Utils.checkNotNull(timeoutSeconds, "timeoutSeconds");
    Utils.checkNotNull(url, "url");
    this.timeoutSeconds = timeoutSeconds;
    this.url = url;
  }

  public HttpCallback() {
    this(Optional.empty(), Optional.empty());
  }

  /**
   * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
   * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
   */
  @JsonIgnore
  public Optional<Integer> timeoutSeconds() {
    return timeoutSeconds;
  }

  /**
   * The URL address of the client HTTP server that will receive the events via POST; URLs must be
   * in the form of https://{domain}[/{path}]
   */
  @JsonIgnore
  public Optional<String> url() {
    return url;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
   * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
   */
  public HttpCallback withTimeoutSeconds(int timeoutSeconds) {
    Utils.checkNotNull(timeoutSeconds, "timeoutSeconds");
    this.timeoutSeconds = Optional.ofNullable(timeoutSeconds);
    return this;
  }

  /**
   * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
   * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
   */
  public HttpCallback withTimeoutSeconds(Optional<Integer> timeoutSeconds) {
    Utils.checkNotNull(timeoutSeconds, "timeoutSeconds");
    this.timeoutSeconds = timeoutSeconds;
    return this;
  }

  /**
   * The URL address of the client HTTP server that will receive the events via POST; URLs must be
   * in the form of https://{domain}[/{path}]
   */
  public HttpCallback withUrl(String url) {
    Utils.checkNotNull(url, "url");
    this.url = Optional.ofNullable(url);
    return this;
  }

  /**
   * The URL address of the client HTTP server that will receive the events via POST; URLs must be
   * in the form of https://{domain}[/{path}]
   */
  public HttpCallback withUrl(Optional<String> url) {
    Utils.checkNotNull(url, "url");
    this.url = url;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    HttpCallback other = (HttpCallback) o;
    return Objects.deepEquals(this.timeoutSeconds, other.timeoutSeconds)
        && Objects.deepEquals(this.url, other.url);
  }

  @Override
  public int hashCode() {
    return Objects.hash(timeoutSeconds, url);
  }

  @Override
  public String toString() {
    return Utils.toString(HttpCallback.class, "timeoutSeconds", timeoutSeconds, "url", url);
  }

  public static final class Builder {

    private Optional<Integer> timeoutSeconds = Optional.empty();

    private Optional<String> url = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
     * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
     */
    public Builder timeoutSeconds(int timeoutSeconds) {
      Utils.checkNotNull(timeoutSeconds, "timeoutSeconds");
      this.timeoutSeconds = Optional.ofNullable(timeoutSeconds);
      return this;
    }

    /**
     * The maximum amount of time, in seconds, the service will wait for an acknowledgement of a
     * delivery. If a value of 0 or no value is specified, the timeout will default to 10 seconds.
     */
    public Builder timeoutSeconds(Optional<Integer> timeoutSeconds) {
      Utils.checkNotNull(timeoutSeconds, "timeoutSeconds");
      this.timeoutSeconds = timeoutSeconds;
      return this;
    }

    /**
     * The URL address of the client HTTP server that will receive the events via POST; URLs must be
     * in the form of https://{domain}[/{path}]
     */
    public Builder url(String url) {
      Utils.checkNotNull(url, "url");
      this.url = Optional.ofNullable(url);
      return this;
    }

    /**
     * The URL address of the client HTTP server that will receive the events via POST; URLs must be
     * in the form of https://{domain}[/{path}]
     */
    public Builder url(Optional<String> url) {
      Utils.checkNotNull(url, "url");
      this.url = url;
      return this;
    }

    public HttpCallback build() {
      return new HttpCallback(timeoutSeconds, url);
    }
  }
}
