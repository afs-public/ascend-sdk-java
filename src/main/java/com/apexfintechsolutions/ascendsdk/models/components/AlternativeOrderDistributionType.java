/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * AlternativeOrderDistributionType
 *
 * <p>Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
 * Orders will be rejected if the alternative investment asset does not allow the requested
 * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
 * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
 * properties to know what it allows.
 */
@JsonDeserialize(using = AlternativeOrderDistributionType._Deserializer.class)
@JsonSerialize(using = AlternativeOrderDistributionType._Serializer.class)
public class AlternativeOrderDistributionType {

  public static final AlternativeOrderDistributionType DISTRIBUTION_TYPE_UNSPECIFIED =
      new AlternativeOrderDistributionType("DISTRIBUTION_TYPE_UNSPECIFIED");
  public static final AlternativeOrderDistributionType CASH =
      new AlternativeOrderDistributionType("CASH");
  public static final AlternativeOrderDistributionType REINVESTMENT =
      new AlternativeOrderDistributionType("REINVESTMENT");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, AlternativeOrderDistributionType> values = createValuesMap();
  private static final Map<String, AlternativeOrderDistributionTypeEnum> enums = createEnumsMap();

  private final String value;

  private AlternativeOrderDistributionType(String value) {
    this.value = value;
  }

  /**
   * Returns a AlternativeOrderDistributionType with the given value. For a specific value the
   * returned object will always be a singleton so reference equality is satisfied when the values
   * are the same.
   *
   * @param value value to be wrapped as AlternativeOrderDistributionType
   */
  public static AlternativeOrderDistributionType of(String value) {
    synchronized (AlternativeOrderDistributionType.class) {
      return values.computeIfAbsent(value, v -> new AlternativeOrderDistributionType(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<AlternativeOrderDistributionTypeEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    AlternativeOrderDistributionType other = (AlternativeOrderDistributionType) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "AlternativeOrderDistributionType [value=" + value + "]";
  }

  // return an array just like an enum
  public static AlternativeOrderDistributionType[] values() {
    synchronized (AlternativeOrderDistributionType.class) {
      return values.values().toArray(new AlternativeOrderDistributionType[] {});
    }
  }

  private static final Map<String, AlternativeOrderDistributionType> createValuesMap() {
    Map<String, AlternativeOrderDistributionType> map = new LinkedHashMap<>();
    map.put("DISTRIBUTION_TYPE_UNSPECIFIED", DISTRIBUTION_TYPE_UNSPECIFIED);
    map.put("CASH", CASH);
    map.put("REINVESTMENT", REINVESTMENT);
    return map;
  }

  private static final Map<String, AlternativeOrderDistributionTypeEnum> createEnumsMap() {
    Map<String, AlternativeOrderDistributionTypeEnum> map = new HashMap<>();
    map.put(
        "DISTRIBUTION_TYPE_UNSPECIFIED",
        AlternativeOrderDistributionTypeEnum.DISTRIBUTION_TYPE_UNSPECIFIED);
    map.put("CASH", AlternativeOrderDistributionTypeEnum.CASH);
    map.put("REINVESTMENT", AlternativeOrderDistributionTypeEnum.REINVESTMENT);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<AlternativeOrderDistributionType> {

    protected _Serializer() {
      super(AlternativeOrderDistributionType.class);
    }

    @Override
    public void serialize(
        AlternativeOrderDistributionType value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer
      extends StdDeserializer<AlternativeOrderDistributionType> {

    protected _Deserializer() {
      super(AlternativeOrderDistributionType.class);
    }

    @Override
    public AlternativeOrderDistributionType deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return AlternativeOrderDistributionType.of(v);
    }
  }

  public enum AlternativeOrderDistributionTypeEnum {
    DISTRIBUTION_TYPE_UNSPECIFIED("DISTRIBUTION_TYPE_UNSPECIFIED"),
    CASH("CASH"),
    REINVESTMENT("REINVESTMENT"),
    ;

    private final String value;

    private AlternativeOrderDistributionTypeEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
