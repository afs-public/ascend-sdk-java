/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * EnrollmentState - Indicates where in the enrollment is in the process; May be
 * `PENDING_AGREEMENT`, `ACTIVE`, `INACTIVE`, `PROCESSING`, or `EXPIRED`
 */
@JsonDeserialize(using = EnrollmentState._Deserializer.class)
@JsonSerialize(using = EnrollmentState._Serializer.class)
public class EnrollmentState {

  public static final EnrollmentState ENROLLMENT_STATE_UNSPECIFIED =
      new EnrollmentState("ENROLLMENT_STATE_UNSPECIFIED");
  public static final EnrollmentState ACTIVE = new EnrollmentState("ACTIVE");
  public static final EnrollmentState INACTIVE = new EnrollmentState("INACTIVE");
  public static final EnrollmentState PENDING_AGREEMENT = new EnrollmentState("PENDING_AGREEMENT");
  public static final EnrollmentState PROCESSING = new EnrollmentState("PROCESSING");
  public static final EnrollmentState EXPIRED = new EnrollmentState("EXPIRED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, EnrollmentState> values = createValuesMap();
  private static final Map<String, EnrollmentStateEnum> enums = createEnumsMap();

  private final String value;

  private EnrollmentState(String value) {
    this.value = value;
  }

  /**
   * Returns a EnrollmentState with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as EnrollmentState
   */
  public static EnrollmentState of(String value) {
    synchronized (EnrollmentState.class) {
      return values.computeIfAbsent(value, v -> new EnrollmentState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<EnrollmentStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    EnrollmentState other = (EnrollmentState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "EnrollmentState [value=" + value + "]";
  }

  // return an array just like an enum
  public static EnrollmentState[] values() {
    synchronized (EnrollmentState.class) {
      return values.values().toArray(new EnrollmentState[] {});
    }
  }

  private static final Map<String, EnrollmentState> createValuesMap() {
    Map<String, EnrollmentState> map = new LinkedHashMap<>();
    map.put("ENROLLMENT_STATE_UNSPECIFIED", ENROLLMENT_STATE_UNSPECIFIED);
    map.put("ACTIVE", ACTIVE);
    map.put("INACTIVE", INACTIVE);
    map.put("PENDING_AGREEMENT", PENDING_AGREEMENT);
    map.put("PROCESSING", PROCESSING);
    map.put("EXPIRED", EXPIRED);
    return map;
  }

  private static final Map<String, EnrollmentStateEnum> createEnumsMap() {
    Map<String, EnrollmentStateEnum> map = new HashMap<>();
    map.put("ENROLLMENT_STATE_UNSPECIFIED", EnrollmentStateEnum.ENROLLMENT_STATE_UNSPECIFIED);
    map.put("ACTIVE", EnrollmentStateEnum.ACTIVE);
    map.put("INACTIVE", EnrollmentStateEnum.INACTIVE);
    map.put("PENDING_AGREEMENT", EnrollmentStateEnum.PENDING_AGREEMENT);
    map.put("PROCESSING", EnrollmentStateEnum.PROCESSING);
    map.put("EXPIRED", EnrollmentStateEnum.EXPIRED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<EnrollmentState> {

    protected _Serializer() {
      super(EnrollmentState.class);
    }

    @Override
    public void serialize(EnrollmentState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<EnrollmentState> {

    protected _Deserializer() {
      super(EnrollmentState.class);
    }

    @Override
    public EnrollmentState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return EnrollmentState.of(v);
    }
  }

  public enum EnrollmentStateEnum {
    ENROLLMENT_STATE_UNSPECIFIED("ENROLLMENT_STATE_UNSPECIFIED"),
    ACTIVE("ACTIVE"),
    INACTIVE("INACTIVE"),
    PENDING_AGREEMENT("PENDING_AGREEMENT"),
    PROCESSING("PROCESSING"),
    EXPIRED("EXPIRED"),
    ;

    private final String value;

    private EnrollmentStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
