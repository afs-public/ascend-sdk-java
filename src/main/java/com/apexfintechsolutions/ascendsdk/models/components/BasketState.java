/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * BasketState
 *
 * <p>The processing status of the basket
 */
@JsonDeserialize(using = BasketState._Deserializer.class)
@JsonSerialize(using = BasketState._Serializer.class)
public class BasketState {

  public static final BasketState STATE_UNSPECIFIED = new BasketState("STATE_UNSPECIFIED");
  public static final BasketState NEW = new BasketState("NEW");
  public static final BasketState SUBMITTED = new BasketState("SUBMITTED");
  public static final BasketState EXECUTING = new BasketState("EXECUTING");
  public static final BasketState ALLOCATING = new BasketState("ALLOCATING");
  public static final BasketState DONE = new BasketState("DONE");
  public static final BasketState REJECTED = new BasketState("REJECTED");
  public static final BasketState PARTIALLY_FILLED = new BasketState("PARTIALLY_FILLED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, BasketState> values = createValuesMap();
  private static final Map<String, BasketStateEnum> enums = createEnumsMap();

  private final String value;

  private BasketState(String value) {
    this.value = value;
  }

  /**
   * Returns a BasketState with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as BasketState
   */
  public static BasketState of(String value) {
    synchronized (BasketState.class) {
      return values.computeIfAbsent(value, v -> new BasketState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<BasketStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    BasketState other = (BasketState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "BasketState [value=" + value + "]";
  }

  // return an array just like an enum
  public static BasketState[] values() {
    synchronized (BasketState.class) {
      return values.values().toArray(new BasketState[] {});
    }
  }

  private static final Map<String, BasketState> createValuesMap() {
    Map<String, BasketState> map = new LinkedHashMap<>();
    map.put("STATE_UNSPECIFIED", STATE_UNSPECIFIED);
    map.put("NEW", NEW);
    map.put("SUBMITTED", SUBMITTED);
    map.put("EXECUTING", EXECUTING);
    map.put("ALLOCATING", ALLOCATING);
    map.put("DONE", DONE);
    map.put("REJECTED", REJECTED);
    map.put("PARTIALLY_FILLED", PARTIALLY_FILLED);
    return map;
  }

  private static final Map<String, BasketStateEnum> createEnumsMap() {
    Map<String, BasketStateEnum> map = new HashMap<>();
    map.put("STATE_UNSPECIFIED", BasketStateEnum.STATE_UNSPECIFIED);
    map.put("NEW", BasketStateEnum.NEW);
    map.put("SUBMITTED", BasketStateEnum.SUBMITTED);
    map.put("EXECUTING", BasketStateEnum.EXECUTING);
    map.put("ALLOCATING", BasketStateEnum.ALLOCATING);
    map.put("DONE", BasketStateEnum.DONE);
    map.put("REJECTED", BasketStateEnum.REJECTED);
    map.put("PARTIALLY_FILLED", BasketStateEnum.PARTIALLY_FILLED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<BasketState> {

    protected _Serializer() {
      super(BasketState.class);
    }

    @Override
    public void serialize(BasketState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<BasketState> {

    protected _Deserializer() {
      super(BasketState.class);
    }

    @Override
    public BasketState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return BasketState.of(v);
    }
  }

  public enum BasketStateEnum {
    STATE_UNSPECIFIED("STATE_UNSPECIFIED"),
    NEW("NEW"),
    SUBMITTED("SUBMITTED"),
    EXECUTING("EXECUTING"),
    ALLOCATING("ALLOCATING"),
    DONE("DONE"),
    REJECTED("REJECTED"),
    PARTIALLY_FILLED("PARTIALLY_FILLED"),
    ;

    private final String value;

    private BasketStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
