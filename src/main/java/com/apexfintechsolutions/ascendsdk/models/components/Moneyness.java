/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * Moneyness
 *
 * <p>Indicates whether the price of the underlying was above or below the strike price of the
 * option
 */
@JsonDeserialize(using = Moneyness._Deserializer.class)
@JsonSerialize(using = Moneyness._Serializer.class)
public class Moneyness {

  public static final Moneyness MONEYNESS_UNSPECIFIED = new Moneyness("MONEYNESS_UNSPECIFIED");
  public static final Moneyness IN_THE_MONEY = new Moneyness("IN_THE_MONEY");
  public static final Moneyness OUT_OF_THE_MONEY = new Moneyness("OUT_OF_THE_MONEY");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, Moneyness> values = createValuesMap();
  private static final Map<String, MoneynessEnum> enums = createEnumsMap();

  private final String value;

  private Moneyness(String value) {
    this.value = value;
  }

  /**
   * Returns a Moneyness with the given value. For a specific value the returned object will always
   * be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as Moneyness
   */
  public static Moneyness of(String value) {
    synchronized (Moneyness.class) {
      return values.computeIfAbsent(value, v -> new Moneyness(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<MoneynessEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    Moneyness other = (Moneyness) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "Moneyness [value=" + value + "]";
  }

  // return an array just like an enum
  public static Moneyness[] values() {
    synchronized (Moneyness.class) {
      return values.values().toArray(new Moneyness[] {});
    }
  }

  private static final Map<String, Moneyness> createValuesMap() {
    Map<String, Moneyness> map = new LinkedHashMap<>();
    map.put("MONEYNESS_UNSPECIFIED", MONEYNESS_UNSPECIFIED);
    map.put("IN_THE_MONEY", IN_THE_MONEY);
    map.put("OUT_OF_THE_MONEY", OUT_OF_THE_MONEY);
    return map;
  }

  private static final Map<String, MoneynessEnum> createEnumsMap() {
    Map<String, MoneynessEnum> map = new HashMap<>();
    map.put("MONEYNESS_UNSPECIFIED", MoneynessEnum.MONEYNESS_UNSPECIFIED);
    map.put("IN_THE_MONEY", MoneynessEnum.IN_THE_MONEY);
    map.put("OUT_OF_THE_MONEY", MoneynessEnum.OUT_OF_THE_MONEY);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<Moneyness> {

    protected _Serializer() {
      super(Moneyness.class);
    }

    @Override
    public void serialize(Moneyness value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<Moneyness> {

    protected _Deserializer() {
      super(Moneyness.class);
    }

    @Override
    public Moneyness deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return Moneyness.of(v);
    }
  }

  public enum MoneynessEnum {
    MONEYNESS_UNSPECIFIED("MONEYNESS_UNSPECIFIED"),
    IN_THE_MONEY("IN_THE_MONEY"),
    OUT_OF_THE_MONEY("OUT_OF_THE_MONEY"),
    ;

    private final String value;

    private MoneynessEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
