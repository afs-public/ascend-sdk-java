/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/** LargeTraderUpdate - A large trader. */
public class LargeTraderUpdate {

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("effective_date")
  private Optional<? extends DateUpdate> effectiveDate;

  /**
   * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
   * reporting.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("large_trader_id")
  private Optional<String> largeTraderId;

  @JsonCreator
  public LargeTraderUpdate(
      @JsonProperty("effective_date") Optional<? extends DateUpdate> effectiveDate,
      @JsonProperty("large_trader_id") Optional<String> largeTraderId) {
    Utils.checkNotNull(effectiveDate, "effectiveDate");
    Utils.checkNotNull(largeTraderId, "largeTraderId");
    this.effectiveDate = effectiveDate;
    this.largeTraderId = largeTraderId;
  }

  public LargeTraderUpdate() {
    this(Optional.empty(), Optional.empty());
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DateUpdate> effectiveDate() {
    return (Optional<DateUpdate>) effectiveDate;
  }

  /**
   * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
   * reporting.
   */
  @JsonIgnore
  public Optional<String> largeTraderId() {
    return largeTraderId;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public LargeTraderUpdate withEffectiveDate(DateUpdate effectiveDate) {
    Utils.checkNotNull(effectiveDate, "effectiveDate");
    this.effectiveDate = Optional.ofNullable(effectiveDate);
    return this;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public LargeTraderUpdate withEffectiveDate(Optional<? extends DateUpdate> effectiveDate) {
    Utils.checkNotNull(effectiveDate, "effectiveDate");
    this.effectiveDate = effectiveDate;
    return this;
  }

  /**
   * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
   * reporting.
   */
  public LargeTraderUpdate withLargeTraderId(String largeTraderId) {
    Utils.checkNotNull(largeTraderId, "largeTraderId");
    this.largeTraderId = Optional.ofNullable(largeTraderId);
    return this;
  }

  /**
   * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
   * reporting.
   */
  public LargeTraderUpdate withLargeTraderId(Optional<String> largeTraderId) {
    Utils.checkNotNull(largeTraderId, "largeTraderId");
    this.largeTraderId = largeTraderId;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    LargeTraderUpdate other = (LargeTraderUpdate) o;
    return Objects.deepEquals(this.effectiveDate, other.effectiveDate)
        && Objects.deepEquals(this.largeTraderId, other.largeTraderId);
  }

  @Override
  public int hashCode() {
    return Objects.hash(effectiveDate, largeTraderId);
  }

  @Override
  public String toString() {
    return Utils.toString(
        LargeTraderUpdate.class, "effectiveDate", effectiveDate, "largeTraderId", largeTraderId);
  }

  public static final class Builder {

    private Optional<? extends DateUpdate> effectiveDate = Optional.empty();

    private Optional<String> largeTraderId = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder effectiveDate(DateUpdate effectiveDate) {
      Utils.checkNotNull(effectiveDate, "effectiveDate");
      this.effectiveDate = Optional.ofNullable(effectiveDate);
      return this;
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder effectiveDate(Optional<? extends DateUpdate> effectiveDate) {
      Utils.checkNotNull(effectiveDate, "effectiveDate");
      this.effectiveDate = effectiveDate;
      return this;
    }

    /**
     * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
     * reporting.
     */
    public Builder largeTraderId(String largeTraderId) {
      Utils.checkNotNull(largeTraderId, "largeTraderId");
      this.largeTraderId = Optional.ofNullable(largeTraderId);
      return this;
    }

    /**
     * SEC-issued ID signifying the person/entity as a large trader; Required for CAIS regulatory
     * reporting.
     */
    public Builder largeTraderId(Optional<String> largeTraderId) {
      Utils.checkNotNull(largeTraderId, "largeTraderId");
      this.largeTraderId = largeTraderId;
      return this;
    }

    public LargeTraderUpdate build() {
      return new LargeTraderUpdate(effectiveDate, largeTraderId);
    }
  }
}
