/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/**
 * PhoneNumberUpdate - An object representing a phone number, suitable as an API wire format.
 *
 * <p>This representation:
 *
 * <p>- should not be used for locale-specific formatting of a phone number, such as "+1 (650)
 * 253-0000 ext. 123"
 *
 * <p>- is not designed for efficient storage - may not be suitable for dialing - specialized
 * libraries (see references) should be used to parse the number for that purpose
 *
 * <p>To do something meaningful with this number, such as format it for various use-cases, convert
 * it to an `i18n.phonenumbers.PhoneNumber` object first.
 *
 * <p>For instance, in Java this would be:
 *
 * <p>com.google.type.PhoneNumber wireProto = com.google.type.PhoneNumber.newBuilder().build();
 * com.google.i18n.phonenumbers.Phonenumber.PhoneNumber phoneNumber =
 * PhoneNumberUtil.getInstance().parse(wireProto.getE164Number(), "ZZ"); if
 * (!wireProto.getExtension().isEmpty()) { phoneNumber.setExtension(wireProto.getExtension()); }
 *
 * <p>Reference(s): - https://github.com/google/libphonenumber
 */
public class PhoneNumberUpdate {

  /**
   * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
   * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and the
   * subscriber number, with no additional spaces or formatting, e.g.: - correct: "+15552220123" -
   * incorrect: "+1 (555) 222-01234 x123".
   *
   * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
   * respect that, so we relax that restriction here. National-only numbers are not allowed.
   *
   * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
   * https://en.wikipedia.org/wiki/E.164. -
   * https://en.wikipedia.org/wiki/List_of_country_calling_codes
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("e164_number")
  private Optional<String> e164Number;

  /**
   * The phone number's extension. The extension is not standardized in ITU recommendations, except
   * for being defined as a series of numbers with a maximum length of 40 digits. Other than digits,
   * some other dialing characters such as ',' (indicating a wait) or '#' may be stored here.
   *
   * <p>Note that no regions currently use extensions with short codes, so this field is normally
   * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
   * allow for short code extensions in the future.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("extension")
  private Optional<String> extension;

  /**
   * An object representing a short code, which is a phone number that is typically much shorter
   * than regular phone numbers and can be used to address messages in MMS and SMS systems, as well
   * as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on your
   * plan.").
   *
   * <p>Short codes are restricted to a region and are not internationally dialable, which means the
   * same short code can exist in different regions, with different usage and pricing, even if those
   * regions share the same country calling code (e.g. US and CA).
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("short_code")
  private Optional<? extends PhoneNumberShortCodeUpdate> shortCode;

  @JsonCreator
  public PhoneNumberUpdate(
      @JsonProperty("e164_number") Optional<String> e164Number,
      @JsonProperty("extension") Optional<String> extension,
      @JsonProperty("short_code") Optional<? extends PhoneNumberShortCodeUpdate> shortCode) {
    Utils.checkNotNull(e164Number, "e164Number");
    Utils.checkNotNull(extension, "extension");
    Utils.checkNotNull(shortCode, "shortCode");
    this.e164Number = e164Number;
    this.extension = extension;
    this.shortCode = shortCode;
  }

  public PhoneNumberUpdate() {
    this(Optional.empty(), Optional.empty(), Optional.empty());
  }

  /**
   * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
   * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and the
   * subscriber number, with no additional spaces or formatting, e.g.: - correct: "+15552220123" -
   * incorrect: "+1 (555) 222-01234 x123".
   *
   * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
   * respect that, so we relax that restriction here. National-only numbers are not allowed.
   *
   * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
   * https://en.wikipedia.org/wiki/E.164. -
   * https://en.wikipedia.org/wiki/List_of_country_calling_codes
   */
  @JsonIgnore
  public Optional<String> e164Number() {
    return e164Number;
  }

  /**
   * The phone number's extension. The extension is not standardized in ITU recommendations, except
   * for being defined as a series of numbers with a maximum length of 40 digits. Other than digits,
   * some other dialing characters such as ',' (indicating a wait) or '#' may be stored here.
   *
   * <p>Note that no regions currently use extensions with short codes, so this field is normally
   * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
   * allow for short code extensions in the future.
   */
  @JsonIgnore
  public Optional<String> extension() {
    return extension;
  }

  /**
   * An object representing a short code, which is a phone number that is typically much shorter
   * than regular phone numbers and can be used to address messages in MMS and SMS systems, as well
   * as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on your
   * plan.").
   *
   * <p>Short codes are restricted to a region and are not internationally dialable, which means the
   * same short code can exist in different regions, with different usage and pricing, even if those
   * regions share the same country calling code (e.g. US and CA).
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<PhoneNumberShortCodeUpdate> shortCode() {
    return (Optional<PhoneNumberShortCodeUpdate>) shortCode;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
   * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and the
   * subscriber number, with no additional spaces or formatting, e.g.: - correct: "+15552220123" -
   * incorrect: "+1 (555) 222-01234 x123".
   *
   * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
   * respect that, so we relax that restriction here. National-only numbers are not allowed.
   *
   * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
   * https://en.wikipedia.org/wiki/E.164. -
   * https://en.wikipedia.org/wiki/List_of_country_calling_codes
   */
  public PhoneNumberUpdate withE164Number(String e164Number) {
    Utils.checkNotNull(e164Number, "e164Number");
    this.e164Number = Optional.ofNullable(e164Number);
    return this;
  }

  /**
   * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
   * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and the
   * subscriber number, with no additional spaces or formatting, e.g.: - correct: "+15552220123" -
   * incorrect: "+1 (555) 222-01234 x123".
   *
   * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
   * respect that, so we relax that restriction here. National-only numbers are not allowed.
   *
   * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
   * https://en.wikipedia.org/wiki/E.164. -
   * https://en.wikipedia.org/wiki/List_of_country_calling_codes
   */
  public PhoneNumberUpdate withE164Number(Optional<String> e164Number) {
    Utils.checkNotNull(e164Number, "e164Number");
    this.e164Number = e164Number;
    return this;
  }

  /**
   * The phone number's extension. The extension is not standardized in ITU recommendations, except
   * for being defined as a series of numbers with a maximum length of 40 digits. Other than digits,
   * some other dialing characters such as ',' (indicating a wait) or '#' may be stored here.
   *
   * <p>Note that no regions currently use extensions with short codes, so this field is normally
   * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
   * allow for short code extensions in the future.
   */
  public PhoneNumberUpdate withExtension(String extension) {
    Utils.checkNotNull(extension, "extension");
    this.extension = Optional.ofNullable(extension);
    return this;
  }

  /**
   * The phone number's extension. The extension is not standardized in ITU recommendations, except
   * for being defined as a series of numbers with a maximum length of 40 digits. Other than digits,
   * some other dialing characters such as ',' (indicating a wait) or '#' may be stored here.
   *
   * <p>Note that no regions currently use extensions with short codes, so this field is normally
   * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
   * allow for short code extensions in the future.
   */
  public PhoneNumberUpdate withExtension(Optional<String> extension) {
    Utils.checkNotNull(extension, "extension");
    this.extension = extension;
    return this;
  }

  /**
   * An object representing a short code, which is a phone number that is typically much shorter
   * than regular phone numbers and can be used to address messages in MMS and SMS systems, as well
   * as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on your
   * plan.").
   *
   * <p>Short codes are restricted to a region and are not internationally dialable, which means the
   * same short code can exist in different regions, with different usage and pricing, even if those
   * regions share the same country calling code (e.g. US and CA).
   */
  public PhoneNumberUpdate withShortCode(PhoneNumberShortCodeUpdate shortCode) {
    Utils.checkNotNull(shortCode, "shortCode");
    this.shortCode = Optional.ofNullable(shortCode);
    return this;
  }

  /**
   * An object representing a short code, which is a phone number that is typically much shorter
   * than regular phone numbers and can be used to address messages in MMS and SMS systems, as well
   * as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on your
   * plan.").
   *
   * <p>Short codes are restricted to a region and are not internationally dialable, which means the
   * same short code can exist in different regions, with different usage and pricing, even if those
   * regions share the same country calling code (e.g. US and CA).
   */
  public PhoneNumberUpdate withShortCode(Optional<? extends PhoneNumberShortCodeUpdate> shortCode) {
    Utils.checkNotNull(shortCode, "shortCode");
    this.shortCode = shortCode;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    PhoneNumberUpdate other = (PhoneNumberUpdate) o;
    return Objects.deepEquals(this.e164Number, other.e164Number)
        && Objects.deepEquals(this.extension, other.extension)
        && Objects.deepEquals(this.shortCode, other.shortCode);
  }

  @Override
  public int hashCode() {
    return Objects.hash(e164Number, extension, shortCode);
  }

  @Override
  public String toString() {
    return Utils.toString(
        PhoneNumberUpdate.class,
        "e164Number",
        e164Number,
        "extension",
        extension,
        "shortCode",
        shortCode);
  }

  public static final class Builder {

    private Optional<String> e164Number = Optional.empty();

    private Optional<String> extension = Optional.empty();

    private Optional<? extends PhoneNumberShortCodeUpdate> shortCode = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
     * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and
     * the subscriber number, with no additional spaces or formatting, e.g.: - correct:
     * "+15552220123" - incorrect: "+1 (555) 222-01234 x123".
     *
     * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
     * respect that, so we relax that restriction here. National-only numbers are not allowed.
     *
     * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
     * https://en.wikipedia.org/wiki/E.164. -
     * https://en.wikipedia.org/wiki/List_of_country_calling_codes
     */
    public Builder e164Number(String e164Number) {
      Utils.checkNotNull(e164Number, "e164Number");
      this.e164Number = Optional.ofNullable(e164Number);
      return this;
    }

    /**
     * The phone number, represented as a leading plus sign ('+'), followed by a phone number that
     * uses a relaxed ITU E.164 format consisting of the country calling code (1 to 3 digits) and
     * the subscriber number, with no additional spaces or formatting, e.g.: - correct:
     * "+15552220123" - incorrect: "+1 (555) 222-01234 x123".
     *
     * <p>The ITU E.164 format limits the latter to 12 digits, but in practice not all countries
     * respect that, so we relax that restriction here. National-only numbers are not allowed.
     *
     * <p>References: - https://www.itu.int/rec/T-REC-E.164-201011-I -
     * https://en.wikipedia.org/wiki/E.164. -
     * https://en.wikipedia.org/wiki/List_of_country_calling_codes
     */
    public Builder e164Number(Optional<String> e164Number) {
      Utils.checkNotNull(e164Number, "e164Number");
      this.e164Number = e164Number;
      return this;
    }

    /**
     * The phone number's extension. The extension is not standardized in ITU recommendations,
     * except for being defined as a series of numbers with a maximum length of 40 digits. Other
     * than digits, some other dialing characters such as ',' (indicating a wait) or '#' may be
     * stored here.
     *
     * <p>Note that no regions currently use extensions with short codes, so this field is normally
     * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
     * allow for short code extensions in the future.
     */
    public Builder extension(String extension) {
      Utils.checkNotNull(extension, "extension");
      this.extension = Optional.ofNullable(extension);
      return this;
    }

    /**
     * The phone number's extension. The extension is not standardized in ITU recommendations,
     * except for being defined as a series of numbers with a maximum length of 40 digits. Other
     * than digits, some other dialing characters such as ',' (indicating a wait) or '#' may be
     * stored here.
     *
     * <p>Note that no regions currently use extensions with short codes, so this field is normally
     * only set in conjunction with an E.164 number. It is held separately from the E.164 number to
     * allow for short code extensions in the future.
     */
    public Builder extension(Optional<String> extension) {
      Utils.checkNotNull(extension, "extension");
      this.extension = extension;
      return this;
    }

    /**
     * An object representing a short code, which is a phone number that is typically much shorter
     * than regular phone numbers and can be used to address messages in MMS and SMS systems, as
     * well as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on
     * your plan.").
     *
     * <p>Short codes are restricted to a region and are not internationally dialable, which means
     * the same short code can exist in different regions, with different usage and pricing, even if
     * those regions share the same country calling code (e.g. US and CA).
     */
    public Builder shortCode(PhoneNumberShortCodeUpdate shortCode) {
      Utils.checkNotNull(shortCode, "shortCode");
      this.shortCode = Optional.ofNullable(shortCode);
      return this;
    }

    /**
     * An object representing a short code, which is a phone number that is typically much shorter
     * than regular phone numbers and can be used to address messages in MMS and SMS systems, as
     * well as for abbreviated dialing (e.g. "Text 611 to see how many minutes you have remaining on
     * your plan.").
     *
     * <p>Short codes are restricted to a region and are not internationally dialable, which means
     * the same short code can exist in different regions, with different usage and pricing, even if
     * those regions share the same country calling code (e.g. US and CA).
     */
    public Builder shortCode(Optional<? extends PhoneNumberShortCodeUpdate> shortCode) {
      Utils.checkNotNull(shortCode, "shortCode");
      this.shortCode = shortCode;
      return this;
    }

    public PhoneNumberUpdate build() {
      return new PhoneNumberUpdate(e164Number, extension, shortCode);
    }
  }
}
