/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * IdentityLookupState
 *
 * <p>Current state of the identity lookup
 */
@JsonDeserialize(using = IdentityLookupState._Deserializer.class)
@JsonSerialize(using = IdentityLookupState._Serializer.class)
public class IdentityLookupState {

  public static final IdentityLookupState STATE_UNSPECIFIED =
      new IdentityLookupState("STATE_UNSPECIFIED");
  public static final IdentityLookupState VERIFICATION_PENDING =
      new IdentityLookupState("VERIFICATION_PENDING");
  public static final IdentityLookupState VERIFICATION_EXPIRED =
      new IdentityLookupState("VERIFICATION_EXPIRED");
  public static final IdentityLookupState IDENTITY_FOUND =
      new IdentityLookupState("IDENTITY_FOUND");
  public static final IdentityLookupState NO_IDENTITY_FOUND =
      new IdentityLookupState("NO_IDENTITY_FOUND");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, IdentityLookupState> values = createValuesMap();
  private static final Map<String, IdentityLookupStateEnum> enums = createEnumsMap();

  private final String value;

  private IdentityLookupState(String value) {
    this.value = value;
  }

  /**
   * Returns a IdentityLookupState with the given value. For a specific value the returned object
   * will always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as IdentityLookupState
   */
  public static IdentityLookupState of(String value) {
    synchronized (IdentityLookupState.class) {
      return values.computeIfAbsent(value, v -> new IdentityLookupState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<IdentityLookupStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    IdentityLookupState other = (IdentityLookupState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "IdentityLookupState [value=" + value + "]";
  }

  // return an array just like an enum
  public static IdentityLookupState[] values() {
    synchronized (IdentityLookupState.class) {
      return values.values().toArray(new IdentityLookupState[] {});
    }
  }

  private static final Map<String, IdentityLookupState> createValuesMap() {
    Map<String, IdentityLookupState> map = new LinkedHashMap<>();
    map.put("STATE_UNSPECIFIED", STATE_UNSPECIFIED);
    map.put("VERIFICATION_PENDING", VERIFICATION_PENDING);
    map.put("VERIFICATION_EXPIRED", VERIFICATION_EXPIRED);
    map.put("IDENTITY_FOUND", IDENTITY_FOUND);
    map.put("NO_IDENTITY_FOUND", NO_IDENTITY_FOUND);
    return map;
  }

  private static final Map<String, IdentityLookupStateEnum> createEnumsMap() {
    Map<String, IdentityLookupStateEnum> map = new HashMap<>();
    map.put("STATE_UNSPECIFIED", IdentityLookupStateEnum.STATE_UNSPECIFIED);
    map.put("VERIFICATION_PENDING", IdentityLookupStateEnum.VERIFICATION_PENDING);
    map.put("VERIFICATION_EXPIRED", IdentityLookupStateEnum.VERIFICATION_EXPIRED);
    map.put("IDENTITY_FOUND", IdentityLookupStateEnum.IDENTITY_FOUND);
    map.put("NO_IDENTITY_FOUND", IdentityLookupStateEnum.NO_IDENTITY_FOUND);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<IdentityLookupState> {

    protected _Serializer() {
      super(IdentityLookupState.class);
    }

    @Override
    public void serialize(IdentityLookupState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<IdentityLookupState> {

    protected _Deserializer() {
      super(IdentityLookupState.class);
    }

    @Override
    public IdentityLookupState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return IdentityLookupState.of(v);
    }
  }

  public enum IdentityLookupStateEnum {
    STATE_UNSPECIFIED("STATE_UNSPECIFIED"),
    VERIFICATION_PENDING("VERIFICATION_PENDING"),
    VERIFICATION_EXPIRED("VERIFICATION_EXPIRED"),
    IDENTITY_FOUND("IDENTITY_FOUND"),
    NO_IDENTITY_FOUND("NO_IDENTITY_FOUND"),
    ;

    private final String value;

    private IdentityLookupStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
