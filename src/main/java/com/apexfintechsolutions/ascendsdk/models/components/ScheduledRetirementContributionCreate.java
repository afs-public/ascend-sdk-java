/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;

/**
 * ScheduledRetirementContributionCreate - The retirement contribution details for a scheduled
 * deposit
 */
public class ScheduledRetirementContributionCreate {

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("tax_year")
  private Optional<Integer> taxYear;

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("temporal_tax_year")
  private Optional<? extends TemporalTaxYear> temporalTaxYear;

  /** The type of retirement contribution. */
  @JsonProperty("type")
  private ScheduledRetirementContributionCreateType type;

  @JsonCreator
  public ScheduledRetirementContributionCreate(
      @JsonProperty("tax_year") Optional<Integer> taxYear,
      @JsonProperty("temporal_tax_year") Optional<? extends TemporalTaxYear> temporalTaxYear,
      @JsonProperty("type") ScheduledRetirementContributionCreateType type) {
    Utils.checkNotNull(taxYear, "taxYear");
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    Utils.checkNotNull(type, "type");
    this.taxYear = taxYear;
    this.temporalTaxYear = temporalTaxYear;
    this.type = type;
  }

  public ScheduledRetirementContributionCreate(ScheduledRetirementContributionCreateType type) {
    this(Optional.empty(), Optional.empty(), type);
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  @JsonIgnore
  public Optional<Integer> taxYear() {
    return taxYear;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<TemporalTaxYear> temporalTaxYear() {
    return (Optional<TemporalTaxYear>) temporalTaxYear;
  }

  /** The type of retirement contribution. */
  @JsonIgnore
  public ScheduledRetirementContributionCreateType type() {
    return type;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  public ScheduledRetirementContributionCreate withTaxYear(int taxYear) {
    Utils.checkNotNull(taxYear, "taxYear");
    this.taxYear = Optional.ofNullable(taxYear);
    return this;
  }

  /**
   * An explicit tax year value. The current year is always valid; and the prior year is valid only
   * before the tax deadline. Must be in "YYYY" format.
   */
  public ScheduledRetirementContributionCreate withTaxYear(Optional<Integer> taxYear) {
    Utils.checkNotNull(taxYear, "taxYear");
    this.taxYear = taxYear;
    return this;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  public ScheduledRetirementContributionCreate withTemporalTaxYear(
      TemporalTaxYear temporalTaxYear) {
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    this.temporalTaxYear = Optional.ofNullable(temporalTaxYear);
    return this;
  }

  /**
   * A temporal tax year value. This will always evaluate to a year based on the date the transfer
   * was initiated.
   */
  public ScheduledRetirementContributionCreate withTemporalTaxYear(
      Optional<? extends TemporalTaxYear> temporalTaxYear) {
    Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
    this.temporalTaxYear = temporalTaxYear;
    return this;
  }

  /** The type of retirement contribution. */
  public ScheduledRetirementContributionCreate withType(
      ScheduledRetirementContributionCreateType type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ScheduledRetirementContributionCreate other = (ScheduledRetirementContributionCreate) o;
    return Objects.deepEquals(this.taxYear, other.taxYear)
        && Objects.deepEquals(this.temporalTaxYear, other.temporalTaxYear)
        && Objects.deepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Objects.hash(taxYear, temporalTaxYear, type);
  }

  @Override
  public String toString() {
    return Utils.toString(
        ScheduledRetirementContributionCreate.class,
        "taxYear",
        taxYear,
        "temporalTaxYear",
        temporalTaxYear,
        "type",
        type);
  }

  public static final class Builder {

    private Optional<Integer> taxYear = Optional.empty();

    private Optional<? extends TemporalTaxYear> temporalTaxYear = Optional.empty();

    private ScheduledRetirementContributionCreateType type;

    private Builder() {
      // force use of static builder() method
    }

    /**
     * An explicit tax year value. The current year is always valid; and the prior year is valid
     * only before the tax deadline. Must be in "YYYY" format.
     */
    public Builder taxYear(int taxYear) {
      Utils.checkNotNull(taxYear, "taxYear");
      this.taxYear = Optional.ofNullable(taxYear);
      return this;
    }

    /**
     * An explicit tax year value. The current year is always valid; and the prior year is valid
     * only before the tax deadline. Must be in "YYYY" format.
     */
    public Builder taxYear(Optional<Integer> taxYear) {
      Utils.checkNotNull(taxYear, "taxYear");
      this.taxYear = taxYear;
      return this;
    }

    /**
     * A temporal tax year value. This will always evaluate to a year based on the date the transfer
     * was initiated.
     */
    public Builder temporalTaxYear(TemporalTaxYear temporalTaxYear) {
      Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
      this.temporalTaxYear = Optional.ofNullable(temporalTaxYear);
      return this;
    }

    /**
     * A temporal tax year value. This will always evaluate to a year based on the date the transfer
     * was initiated.
     */
    public Builder temporalTaxYear(Optional<? extends TemporalTaxYear> temporalTaxYear) {
      Utils.checkNotNull(temporalTaxYear, "temporalTaxYear");
      this.temporalTaxYear = temporalTaxYear;
      return this;
    }

    /** The type of retirement contribution. */
    public Builder type(ScheduledRetirementContributionCreateType type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public ScheduledRetirementContributionCreate build() {
      return new ScheduledRetirementContributionCreate(taxYear, temporalTaxYear, type);
    }
  }
}
