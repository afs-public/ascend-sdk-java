/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * Outcome
 *
 * <p>The determined outcome of the event
 */
@JsonDeserialize(using = Outcome._Deserializer.class)
@JsonSerialize(using = Outcome._Serializer.class)
public class Outcome {

  public static final Outcome EVENT_CONTRACT_OUTCOME_UNSPECIFIED =
      new Outcome("EVENT_CONTRACT_OUTCOME_UNSPECIFIED");
  public static final Outcome FAVORABLE = new Outcome("FAVORABLE");
  public static final Outcome UNFAVORABLE = new Outcome("UNFAVORABLE");
  public static final Outcome VOID = new Outcome("VOID");
  public static final Outcome TIE = new Outcome("TIE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, Outcome> values = createValuesMap();
  private static final Map<String, OutcomeEnum> enums = createEnumsMap();

  private final String value;

  private Outcome(String value) {
    this.value = value;
  }

  /**
   * Returns a Outcome with the given value. For a specific value the returned object will always be
   * a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as Outcome
   */
  public static Outcome of(String value) {
    synchronized (Outcome.class) {
      return values.computeIfAbsent(value, v -> new Outcome(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<OutcomeEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    Outcome other = (Outcome) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "Outcome [value=" + value + "]";
  }

  // return an array just like an enum
  public static Outcome[] values() {
    synchronized (Outcome.class) {
      return values.values().toArray(new Outcome[] {});
    }
  }

  private static final Map<String, Outcome> createValuesMap() {
    Map<String, Outcome> map = new LinkedHashMap<>();
    map.put("EVENT_CONTRACT_OUTCOME_UNSPECIFIED", EVENT_CONTRACT_OUTCOME_UNSPECIFIED);
    map.put("FAVORABLE", FAVORABLE);
    map.put("UNFAVORABLE", UNFAVORABLE);
    map.put("VOID", VOID);
    map.put("TIE", TIE);
    return map;
  }

  private static final Map<String, OutcomeEnum> createEnumsMap() {
    Map<String, OutcomeEnum> map = new HashMap<>();
    map.put("EVENT_CONTRACT_OUTCOME_UNSPECIFIED", OutcomeEnum.EVENT_CONTRACT_OUTCOME_UNSPECIFIED);
    map.put("FAVORABLE", OutcomeEnum.FAVORABLE);
    map.put("UNFAVORABLE", OutcomeEnum.UNFAVORABLE);
    map.put("VOID", OutcomeEnum.VOID);
    map.put("TIE", OutcomeEnum.TIE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<Outcome> {

    protected _Serializer() {
      super(Outcome.class);
    }

    @Override
    public void serialize(Outcome value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<Outcome> {

    protected _Deserializer() {
      super(Outcome.class);
    }

    @Override
    public Outcome deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return Outcome.of(v);
    }
  }

  public enum OutcomeEnum {
    EVENT_CONTRACT_OUTCOME_UNSPECIFIED("EVENT_CONTRACT_OUTCOME_UNSPECIFIED"),
    FAVORABLE("FAVORABLE"),
    UNFAVORABLE("UNFAVORABLE"),
    VOID("VOID"),
    TIE("TIE"),
    ;

    private final String value;

    private OutcomeEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
