/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Objects;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/** BasketTradingExecutedPrice - An average price definition */
public class BasketTradingExecutedPrice {

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("price")
  private JsonNullable<? extends BasketTradingExecutedPricePrice> price;

  /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("type")
  private Optional<? extends BasketTradingExecutedPriceType> type;

  @JsonCreator
  public BasketTradingExecutedPrice(
      @JsonProperty("price") JsonNullable<? extends BasketTradingExecutedPricePrice> price,
      @JsonProperty("type") Optional<? extends BasketTradingExecutedPriceType> type) {
    Utils.checkNotNull(price, "price");
    Utils.checkNotNull(type, "type");
    this.price = price;
    this.type = type;
  }

  public BasketTradingExecutedPrice() {
    this(JsonNullable.undefined(), Optional.empty());
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public JsonNullable<BasketTradingExecutedPricePrice> price() {
    return (JsonNullable<BasketTradingExecutedPricePrice>) price;
  }

  /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<BasketTradingExecutedPriceType> type() {
    return (Optional<BasketTradingExecutedPriceType>) type;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  public BasketTradingExecutedPrice withPrice(BasketTradingExecutedPricePrice price) {
    Utils.checkNotNull(price, "price");
    this.price = JsonNullable.of(price);
    return this;
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  public BasketTradingExecutedPrice withPrice(
      JsonNullable<? extends BasketTradingExecutedPricePrice> price) {
    Utils.checkNotNull(price, "price");
    this.price = price;
    return this;
  }

  /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
  public BasketTradingExecutedPrice withType(BasketTradingExecutedPriceType type) {
    Utils.checkNotNull(type, "type");
    this.type = Optional.ofNullable(type);
    return this;
  }

  /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
  public BasketTradingExecutedPrice withType(
      Optional<? extends BasketTradingExecutedPriceType> type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BasketTradingExecutedPrice other = (BasketTradingExecutedPrice) o;
    return Objects.deepEquals(this.price, other.price) && Objects.deepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Objects.hash(price, type);
  }

  @Override
  public String toString() {
    return Utils.toString(BasketTradingExecutedPrice.class, "price", price, "type", type);
  }

  public static final class Builder {

    private JsonNullable<? extends BasketTradingExecutedPricePrice> price =
        JsonNullable.undefined();

    private Optional<? extends BasketTradingExecutedPriceType> type = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 5 decimal places in the USD currency.
     */
    public Builder price(BasketTradingExecutedPricePrice price) {
      Utils.checkNotNull(price, "price");
      this.price = JsonNullable.of(price);
      return this;
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 5 decimal places in the USD currency.
     */
    public Builder price(JsonNullable<? extends BasketTradingExecutedPricePrice> price) {
      Utils.checkNotNull(price, "price");
      this.price = price;
      return this;
    }

    /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
    public Builder type(BasketTradingExecutedPriceType type) {
      Utils.checkNotNull(type, "type");
      this.type = Optional.ofNullable(type);
      return this;
    }

    /** The type of this price, which must be PRICE_PER_UNIT for equity orders */
    public Builder type(Optional<? extends BasketTradingExecutedPriceType> type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public BasketTradingExecutedPrice build() {
      return new BasketTradingExecutedPrice(price, type);
    }
  }
}
