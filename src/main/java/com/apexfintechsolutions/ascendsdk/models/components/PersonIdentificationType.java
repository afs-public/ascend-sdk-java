/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * PersonIdentificationType - The identification type for a person, one of: - `ID_TYPE_UNSPECIFIED`
 * - Default/Null value. - `SSN` - SSN tax type. - `ITIN` - ITIN tax type. - `FTIN` - FTIN (foreign
 * tax id) type. - `NATIONAL_ID` - National id type. - `PASSPORT` - Passport id type. -
 * `DRIVING_LICENSE` - Drivers license id type.
 */
@JsonDeserialize(using = PersonIdentificationType._Deserializer.class)
@JsonSerialize(using = PersonIdentificationType._Serializer.class)
public class PersonIdentificationType {

  public static final PersonIdentificationType ID_TYPE_UNSPECIFIED =
      new PersonIdentificationType("ID_TYPE_UNSPECIFIED");
  public static final PersonIdentificationType SSN = new PersonIdentificationType("SSN");
  public static final PersonIdentificationType ITIN = new PersonIdentificationType("ITIN");
  public static final PersonIdentificationType FTIN = new PersonIdentificationType("FTIN");
  public static final PersonIdentificationType NATIONAL_ID =
      new PersonIdentificationType("NATIONAL_ID");
  public static final PersonIdentificationType PASSPORT = new PersonIdentificationType("PASSPORT");
  public static final PersonIdentificationType DRIVING_LICENSE =
      new PersonIdentificationType("DRIVING_LICENSE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, PersonIdentificationType> values = createValuesMap();
  private static final Map<String, PersonIdentificationTypeEnum> enums = createEnumsMap();

  private final String value;

  private PersonIdentificationType(String value) {
    this.value = value;
  }

  /**
   * Returns a PersonIdentificationType with the given value. For a specific value the returned
   * object will always be a singleton so reference equality is satisfied when the values are the
   * same.
   *
   * @param value value to be wrapped as PersonIdentificationType
   */
  public static PersonIdentificationType of(String value) {
    synchronized (PersonIdentificationType.class) {
      return values.computeIfAbsent(value, v -> new PersonIdentificationType(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<PersonIdentificationTypeEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    PersonIdentificationType other = (PersonIdentificationType) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "PersonIdentificationType [value=" + value + "]";
  }

  // return an array just like an enum
  public static PersonIdentificationType[] values() {
    synchronized (PersonIdentificationType.class) {
      return values.values().toArray(new PersonIdentificationType[] {});
    }
  }

  private static final Map<String, PersonIdentificationType> createValuesMap() {
    Map<String, PersonIdentificationType> map = new LinkedHashMap<>();
    map.put("ID_TYPE_UNSPECIFIED", ID_TYPE_UNSPECIFIED);
    map.put("SSN", SSN);
    map.put("ITIN", ITIN);
    map.put("FTIN", FTIN);
    map.put("NATIONAL_ID", NATIONAL_ID);
    map.put("PASSPORT", PASSPORT);
    map.put("DRIVING_LICENSE", DRIVING_LICENSE);
    return map;
  }

  private static final Map<String, PersonIdentificationTypeEnum> createEnumsMap() {
    Map<String, PersonIdentificationTypeEnum> map = new HashMap<>();
    map.put("ID_TYPE_UNSPECIFIED", PersonIdentificationTypeEnum.ID_TYPE_UNSPECIFIED);
    map.put("SSN", PersonIdentificationTypeEnum.SSN);
    map.put("ITIN", PersonIdentificationTypeEnum.ITIN);
    map.put("FTIN", PersonIdentificationTypeEnum.FTIN);
    map.put("NATIONAL_ID", PersonIdentificationTypeEnum.NATIONAL_ID);
    map.put("PASSPORT", PersonIdentificationTypeEnum.PASSPORT);
    map.put("DRIVING_LICENSE", PersonIdentificationTypeEnum.DRIVING_LICENSE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<PersonIdentificationType> {

    protected _Serializer() {
      super(PersonIdentificationType.class);
    }

    @Override
    public void serialize(
        PersonIdentificationType value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<PersonIdentificationType> {

    protected _Deserializer() {
      super(PersonIdentificationType.class);
    }

    @Override
    public PersonIdentificationType deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return PersonIdentificationType.of(v);
    }
  }

  public enum PersonIdentificationTypeEnum {
    ID_TYPE_UNSPECIFIED("ID_TYPE_UNSPECIFIED"),
    SSN("SSN"),
    ITIN("ITIN"),
    FTIN("FTIN"),
    NATIONAL_ID("NATIONAL_ID"),
    PASSPORT("PASSPORT"),
    DRIVING_LICENSE("DRIVING_LICENSE"),
    ;

    private final String value;

    private PersonIdentificationTypeEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
