/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * RecordState
 *
 * <p>Indicates the current state of the asset record. The record state identifies which record
 * types the asset allows.
 */
@JsonDeserialize(using = RecordState._Deserializer.class)
@JsonSerialize(using = RecordState._Serializer.class)
public class RecordState {

  public static final RecordState RECORD_STATE_UNSPECIFIED =
      new RecordState("RECORD_STATE_UNSPECIFIED");
  public static final RecordState ACTIVE = new RecordState("ACTIVE");
  public static final RecordState FROZEN = new RecordState("FROZEN");
  public static final RecordState INACTIVE = new RecordState("INACTIVE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, RecordState> values = createValuesMap();
  private static final Map<String, RecordStateEnum> enums = createEnumsMap();

  private final String value;

  private RecordState(String value) {
    this.value = value;
  }

  /**
   * Returns a RecordState with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as RecordState
   */
  public static RecordState of(String value) {
    synchronized (RecordState.class) {
      return values.computeIfAbsent(value, v -> new RecordState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<RecordStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    RecordState other = (RecordState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "RecordState [value=" + value + "]";
  }

  // return an array just like an enum
  public static RecordState[] values() {
    synchronized (RecordState.class) {
      return values.values().toArray(new RecordState[] {});
    }
  }

  private static final Map<String, RecordState> createValuesMap() {
    Map<String, RecordState> map = new LinkedHashMap<>();
    map.put("RECORD_STATE_UNSPECIFIED", RECORD_STATE_UNSPECIFIED);
    map.put("ACTIVE", ACTIVE);
    map.put("FROZEN", FROZEN);
    map.put("INACTIVE", INACTIVE);
    return map;
  }

  private static final Map<String, RecordStateEnum> createEnumsMap() {
    Map<String, RecordStateEnum> map = new HashMap<>();
    map.put("RECORD_STATE_UNSPECIFIED", RecordStateEnum.RECORD_STATE_UNSPECIFIED);
    map.put("ACTIVE", RecordStateEnum.ACTIVE);
    map.put("FROZEN", RecordStateEnum.FROZEN);
    map.put("INACTIVE", RecordStateEnum.INACTIVE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<RecordState> {

    protected _Serializer() {
      super(RecordState.class);
    }

    @Override
    public void serialize(RecordState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<RecordState> {

    protected _Deserializer() {
      super(RecordState.class);
    }

    @Override
    public RecordState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return RecordState.of(v);
    }
  }

  public enum RecordStateEnum {
    RECORD_STATE_UNSPECIFIED("RECORD_STATE_UNSPECIFIED"),
    ACTIVE("ACTIVE"),
    FROZEN("FROZEN"),
    INACTIVE("INACTIVE"),
    ;

    private final String value;

    private RecordStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
