/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * EntrySideModifier
 *
 * <p>Indicates whether the trade is opening a new position or closing an existing position Should
 * be populated if possible for trades; the side modifier for the trade
 */
@JsonDeserialize(using = EntrySideModifier._Deserializer.class)
@JsonSerialize(using = EntrySideModifier._Serializer.class)
public class EntrySideModifier {

  public static final EntrySideModifier SIDE_MODIFIER_UNSPECIFIED =
      new EntrySideModifier("SIDE_MODIFIER_UNSPECIFIED");
  public static final EntrySideModifier SHORT = new EntrySideModifier("SHORT");
  public static final EntrySideModifier SHORT_EXEMPT = new EntrySideModifier("SHORT_EXEMPT");
  public static final EntrySideModifier SHORT_COVER = new EntrySideModifier("SHORT_COVER");
  public static final EntrySideModifier OPEN = new EntrySideModifier("OPEN");
  public static final EntrySideModifier CLOSE = new EntrySideModifier("CLOSE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, EntrySideModifier> values = createValuesMap();
  private static final Map<String, EntrySideModifierEnum> enums = createEnumsMap();

  private final String value;

  private EntrySideModifier(String value) {
    this.value = value;
  }

  /**
   * Returns a EntrySideModifier with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as EntrySideModifier
   */
  public static EntrySideModifier of(String value) {
    synchronized (EntrySideModifier.class) {
      return values.computeIfAbsent(value, v -> new EntrySideModifier(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<EntrySideModifierEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    EntrySideModifier other = (EntrySideModifier) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "EntrySideModifier [value=" + value + "]";
  }

  // return an array just like an enum
  public static EntrySideModifier[] values() {
    synchronized (EntrySideModifier.class) {
      return values.values().toArray(new EntrySideModifier[] {});
    }
  }

  private static final Map<String, EntrySideModifier> createValuesMap() {
    Map<String, EntrySideModifier> map = new LinkedHashMap<>();
    map.put("SIDE_MODIFIER_UNSPECIFIED", SIDE_MODIFIER_UNSPECIFIED);
    map.put("SHORT", SHORT);
    map.put("SHORT_EXEMPT", SHORT_EXEMPT);
    map.put("SHORT_COVER", SHORT_COVER);
    map.put("OPEN", OPEN);
    map.put("CLOSE", CLOSE);
    return map;
  }

  private static final Map<String, EntrySideModifierEnum> createEnumsMap() {
    Map<String, EntrySideModifierEnum> map = new HashMap<>();
    map.put("SIDE_MODIFIER_UNSPECIFIED", EntrySideModifierEnum.SIDE_MODIFIER_UNSPECIFIED);
    map.put("SHORT", EntrySideModifierEnum.SHORT);
    map.put("SHORT_EXEMPT", EntrySideModifierEnum.SHORT_EXEMPT);
    map.put("SHORT_COVER", EntrySideModifierEnum.SHORT_COVER);
    map.put("OPEN", EntrySideModifierEnum.OPEN);
    map.put("CLOSE", EntrySideModifierEnum.CLOSE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<EntrySideModifier> {

    protected _Serializer() {
      super(EntrySideModifier.class);
    }

    @Override
    public void serialize(EntrySideModifier value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<EntrySideModifier> {

    protected _Deserializer() {
      super(EntrySideModifier.class);
    }

    @Override
    public EntrySideModifier deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return EntrySideModifier.of(v);
    }
  }

  public enum EntrySideModifierEnum {
    SIDE_MODIFIER_UNSPECIFIED("SIDE_MODIFIER_UNSPECIFIED"),
    SHORT("SHORT"),
    SHORT_EXEMPT("SHORT_EXEMPT"),
    SHORT_COVER("SHORT_COVER"),
    OPEN("OPEN"),
    CLOSE("CLOSE"),
    ;

    private final String value;

    private EntrySideModifierEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
