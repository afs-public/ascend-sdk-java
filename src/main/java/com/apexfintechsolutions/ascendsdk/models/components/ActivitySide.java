/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * ActivitySide
 *
 * <p>Denotes whether the trade is a buy or sell
 */
@JsonDeserialize(using = ActivitySide._Deserializer.class)
@JsonSerialize(using = ActivitySide._Serializer.class)
public class ActivitySide {

  public static final ActivitySide SIDE_UNSPECIFIED = new ActivitySide("SIDE_UNSPECIFIED");
  public static final ActivitySide BUY = new ActivitySide("BUY");
  public static final ActivitySide SELL = new ActivitySide("SELL");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, ActivitySide> values = createValuesMap();
  private static final Map<String, ActivitySideEnum> enums = createEnumsMap();

  private final String value;

  private ActivitySide(String value) {
    this.value = value;
  }

  /**
   * Returns a ActivitySide with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as ActivitySide
   */
  public static ActivitySide of(String value) {
    synchronized (ActivitySide.class) {
      return values.computeIfAbsent(value, v -> new ActivitySide(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ActivitySideEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    ActivitySide other = (ActivitySide) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "ActivitySide [value=" + value + "]";
  }

  // return an array just like an enum
  public static ActivitySide[] values() {
    synchronized (ActivitySide.class) {
      return values.values().toArray(new ActivitySide[] {});
    }
  }

  private static final Map<String, ActivitySide> createValuesMap() {
    Map<String, ActivitySide> map = new LinkedHashMap<>();
    map.put("SIDE_UNSPECIFIED", SIDE_UNSPECIFIED);
    map.put("BUY", BUY);
    map.put("SELL", SELL);
    return map;
  }

  private static final Map<String, ActivitySideEnum> createEnumsMap() {
    Map<String, ActivitySideEnum> map = new HashMap<>();
    map.put("SIDE_UNSPECIFIED", ActivitySideEnum.SIDE_UNSPECIFIED);
    map.put("BUY", ActivitySideEnum.BUY);
    map.put("SELL", ActivitySideEnum.SELL);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<ActivitySide> {

    protected _Serializer() {
      super(ActivitySide.class);
    }

    @Override
    public void serialize(ActivitySide value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<ActivitySide> {

    protected _Deserializer() {
      super(ActivitySide.class);
    }

    @Override
    public ActivitySide deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return ActivitySide.of(v);
    }
  }

  public enum ActivitySideEnum {
    SIDE_UNSPECIFIED("SIDE_UNSPECIFIED"),
    BUY("BUY"),
    SELL("SELL"),
    ;

    private final String value;

    private ActivitySideEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
