/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * MatchState
 *
 * <p>The match state for a profile, one of: - `MATCH_UNSPECIFIED` - Default/Null value. -
 * `CONFIRMED_MATCH` - Match is confirmed. - `POTENTIAL_MATCH` - Match is a potential. - `NO_MATCH`
 * - Match is confirmed not to be a match. - `INCONCLUSIVE` - Match is deemed to be inconclusive.
 */
@JsonDeserialize(using = MatchState._Deserializer.class)
@JsonSerialize(using = MatchState._Serializer.class)
public class MatchState {

  public static final MatchState MATCH_UNSPECIFIED = new MatchState("MATCH_UNSPECIFIED");
  public static final MatchState CONFIRMED_MATCH = new MatchState("CONFIRMED_MATCH");
  public static final MatchState POTENTIAL_MATCH = new MatchState("POTENTIAL_MATCH");
  public static final MatchState NO_MATCH = new MatchState("NO_MATCH");
  public static final MatchState INCONCLUSIVE = new MatchState("INCONCLUSIVE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, MatchState> values = createValuesMap();
  private static final Map<String, MatchStateEnum> enums = createEnumsMap();

  private final String value;

  private MatchState(String value) {
    this.value = value;
  }

  /**
   * Returns a MatchState with the given value. For a specific value the returned object will always
   * be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as MatchState
   */
  public static MatchState of(String value) {
    synchronized (MatchState.class) {
      return values.computeIfAbsent(value, v -> new MatchState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<MatchStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    MatchState other = (MatchState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "MatchState [value=" + value + "]";
  }

  // return an array just like an enum
  public static MatchState[] values() {
    synchronized (MatchState.class) {
      return values.values().toArray(new MatchState[] {});
    }
  }

  private static final Map<String, MatchState> createValuesMap() {
    Map<String, MatchState> map = new LinkedHashMap<>();
    map.put("MATCH_UNSPECIFIED", MATCH_UNSPECIFIED);
    map.put("CONFIRMED_MATCH", CONFIRMED_MATCH);
    map.put("POTENTIAL_MATCH", POTENTIAL_MATCH);
    map.put("NO_MATCH", NO_MATCH);
    map.put("INCONCLUSIVE", INCONCLUSIVE);
    return map;
  }

  private static final Map<String, MatchStateEnum> createEnumsMap() {
    Map<String, MatchStateEnum> map = new HashMap<>();
    map.put("MATCH_UNSPECIFIED", MatchStateEnum.MATCH_UNSPECIFIED);
    map.put("CONFIRMED_MATCH", MatchStateEnum.CONFIRMED_MATCH);
    map.put("POTENTIAL_MATCH", MatchStateEnum.POTENTIAL_MATCH);
    map.put("NO_MATCH", MatchStateEnum.NO_MATCH);
    map.put("INCONCLUSIVE", MatchStateEnum.INCONCLUSIVE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<MatchState> {

    protected _Serializer() {
      super(MatchState.class);
    }

    @Override
    public void serialize(MatchState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<MatchState> {

    protected _Deserializer() {
      super(MatchState.class);
    }

    @Override
    public MatchState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return MatchState.of(v);
    }
  }

  public enum MatchStateEnum {
    MATCH_UNSPECIFIED("MATCH_UNSPECIFIED"),
    CONFIRMED_MATCH("CONFIRMED_MATCH"),
    POTENTIAL_MATCH("POTENTIAL_MATCH"),
    NO_MATCH("NO_MATCH"),
    INCONCLUSIVE("INCONCLUSIVE"),
    ;

    private final String value;

    private MatchStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
