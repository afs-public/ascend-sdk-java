/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * Program
 *
 * <p>The name of the program the ICT deposit is associated with
 */
@JsonDeserialize(using = Program._Deserializer.class)
@JsonSerialize(using = Program._Serializer.class)
public class Program {

  public static final Program ICT_PROGRAM_UNSPECIFIED = new Program("ICT_PROGRAM_UNSPECIFIED");
  public static final Program BROKER_PARTNER = new Program("BROKER_PARTNER");
  public static final Program DEPOSIT_ONLY = new Program("DEPOSIT_ONLY");
  public static final Program BANKING_PARTNER = new Program("BANKING_PARTNER");
  public static final Program WITHDRAWAL_ONLY = new Program("WITHDRAWAL_ONLY");
  public static final Program DIGITAL_PARTNER = new Program("DIGITAL_PARTNER");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, Program> values = createValuesMap();
  private static final Map<String, ProgramEnum> enums = createEnumsMap();

  private final String value;

  private Program(String value) {
    this.value = value;
  }

  /**
   * Returns a Program with the given value. For a specific value the returned object will always be
   * a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as Program
   */
  public static Program of(String value) {
    synchronized (Program.class) {
      return values.computeIfAbsent(value, v -> new Program(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<ProgramEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    Program other = (Program) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "Program [value=" + value + "]";
  }

  // return an array just like an enum
  public static Program[] values() {
    synchronized (Program.class) {
      return values.values().toArray(new Program[] {});
    }
  }

  private static final Map<String, Program> createValuesMap() {
    Map<String, Program> map = new LinkedHashMap<>();
    map.put("ICT_PROGRAM_UNSPECIFIED", ICT_PROGRAM_UNSPECIFIED);
    map.put("BROKER_PARTNER", BROKER_PARTNER);
    map.put("DEPOSIT_ONLY", DEPOSIT_ONLY);
    map.put("BANKING_PARTNER", BANKING_PARTNER);
    map.put("WITHDRAWAL_ONLY", WITHDRAWAL_ONLY);
    map.put("DIGITAL_PARTNER", DIGITAL_PARTNER);
    return map;
  }

  private static final Map<String, ProgramEnum> createEnumsMap() {
    Map<String, ProgramEnum> map = new HashMap<>();
    map.put("ICT_PROGRAM_UNSPECIFIED", ProgramEnum.ICT_PROGRAM_UNSPECIFIED);
    map.put("BROKER_PARTNER", ProgramEnum.BROKER_PARTNER);
    map.put("DEPOSIT_ONLY", ProgramEnum.DEPOSIT_ONLY);
    map.put("BANKING_PARTNER", ProgramEnum.BANKING_PARTNER);
    map.put("WITHDRAWAL_ONLY", ProgramEnum.WITHDRAWAL_ONLY);
    map.put("DIGITAL_PARTNER", ProgramEnum.DIGITAL_PARTNER);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<Program> {

    protected _Serializer() {
      super(Program.class);
    }

    @Override
    public void serialize(Program value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<Program> {

    protected _Deserializer() {
      super(Program.class);
    }

    @Override
    public Program deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return Program.of(v);
    }
  }

  public enum ProgramEnum {
    ICT_PROGRAM_UNSPECIFIED("ICT_PROGRAM_UNSPECIFIED"),
    BROKER_PARTNER("BROKER_PARTNER"),
    DEPOSIT_ONLY("DEPOSIT_ONLY"),
    BANKING_PARTNER("BANKING_PARTNER"),
    WITHDRAWAL_ONLY("WITHDRAWAL_ONLY"),
    DIGITAL_PARTNER("DIGITAL_PARTNER"),
    ;

    private final String value;

    private ProgramEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
