/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * AgreementState - The status of an agreement which blocks an enrollment; `REQUIRED` if not yet
 * received, or `AFFIRMED` if acknowledgement has been received by AFS
 */
@JsonDeserialize(using = AgreementState._Deserializer.class)
@JsonSerialize(using = AgreementState._Serializer.class)
public class AgreementState {

  public static final AgreementState AGREEMENT_STATE_UNSPECIFIED =
      new AgreementState("AGREEMENT_STATE_UNSPECIFIED");
  public static final AgreementState REQUIRED = new AgreementState("REQUIRED");
  public static final AgreementState AFFIRMED = new AgreementState("AFFIRMED");
  public static final AgreementState VOIDED = new AgreementState("VOIDED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, AgreementState> values = createValuesMap();
  private static final Map<String, AgreementStateEnum> enums = createEnumsMap();

  private final String value;

  private AgreementState(String value) {
    this.value = value;
  }

  /**
   * Returns a AgreementState with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as AgreementState
   */
  public static AgreementState of(String value) {
    synchronized (AgreementState.class) {
      return values.computeIfAbsent(value, v -> new AgreementState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<AgreementStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    AgreementState other = (AgreementState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "AgreementState [value=" + value + "]";
  }

  // return an array just like an enum
  public static AgreementState[] values() {
    synchronized (AgreementState.class) {
      return values.values().toArray(new AgreementState[] {});
    }
  }

  private static final Map<String, AgreementState> createValuesMap() {
    Map<String, AgreementState> map = new LinkedHashMap<>();
    map.put("AGREEMENT_STATE_UNSPECIFIED", AGREEMENT_STATE_UNSPECIFIED);
    map.put("REQUIRED", REQUIRED);
    map.put("AFFIRMED", AFFIRMED);
    map.put("VOIDED", VOIDED);
    return map;
  }

  private static final Map<String, AgreementStateEnum> createEnumsMap() {
    Map<String, AgreementStateEnum> map = new HashMap<>();
    map.put("AGREEMENT_STATE_UNSPECIFIED", AgreementStateEnum.AGREEMENT_STATE_UNSPECIFIED);
    map.put("REQUIRED", AgreementStateEnum.REQUIRED);
    map.put("AFFIRMED", AgreementStateEnum.AFFIRMED);
    map.put("VOIDED", AgreementStateEnum.VOIDED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<AgreementState> {

    protected _Serializer() {
      super(AgreementState.class);
    }

    @Override
    public void serialize(AgreementState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<AgreementState> {

    protected _Deserializer() {
      super(AgreementState.class);
    }

    @Override
    public AgreementState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return AgreementState.of(v);
    }
  }

  public enum AgreementStateEnum {
    AGREEMENT_STATE_UNSPECIFIED("AGREEMENT_STATE_UNSPECIFIED"),
    REQUIRED("REQUIRED"),
    AFFIRMED("AFFIRMED"),
    VOIDED("VOIDED"),
    ;

    private final String value;

    private AgreementStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
