/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Optional;

/**
 * ForeignIdentificationCreate
 *
 * <p>Foreign identification
 */
public class ForeignIdentificationCreate {
  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("expiration_date")
  private Optional<? extends DateCreate> expirationDate;

  /** Denotes if the identification is a tax id or other */
  @JsonProperty("ftin")
  private boolean ftin;

  /** Identification number */
  @JsonProperty("identification_number")
  private String identificationNumber;

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("issue_date")
  private Optional<? extends DateCreate> issueDate;

  /** Region of issuance must be provided as a two-character CLDR country code */
  @JsonProperty("issuing_region_code")
  private String issuingRegionCode;

  /** Identification type */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("type")
  private Optional<? extends Type> type;

  @JsonCreator
  public ForeignIdentificationCreate(
      @JsonProperty("expiration_date") Optional<? extends DateCreate> expirationDate,
      @JsonProperty("ftin") boolean ftin,
      @JsonProperty("identification_number") String identificationNumber,
      @JsonProperty("issue_date") Optional<? extends DateCreate> issueDate,
      @JsonProperty("issuing_region_code") String issuingRegionCode,
      @JsonProperty("type") Optional<? extends Type> type) {
    Utils.checkNotNull(expirationDate, "expirationDate");
    Utils.checkNotNull(ftin, "ftin");
    Utils.checkNotNull(identificationNumber, "identificationNumber");
    Utils.checkNotNull(issueDate, "issueDate");
    Utils.checkNotNull(issuingRegionCode, "issuingRegionCode");
    Utils.checkNotNull(type, "type");
    this.expirationDate = expirationDate;
    this.ftin = ftin;
    this.identificationNumber = identificationNumber;
    this.issueDate = issueDate;
    this.issuingRegionCode = issuingRegionCode;
    this.type = type;
  }

  public ForeignIdentificationCreate(
      boolean ftin, String identificationNumber, String issuingRegionCode) {
    this(
        Optional.empty(),
        ftin,
        identificationNumber,
        Optional.empty(),
        issuingRegionCode,
        Optional.empty());
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DateCreate> expirationDate() {
    return (Optional<DateCreate>) expirationDate;
  }

  /** Denotes if the identification is a tax id or other */
  @JsonIgnore
  public boolean ftin() {
    return ftin;
  }

  /** Identification number */
  @JsonIgnore
  public String identificationNumber() {
    return identificationNumber;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DateCreate> issueDate() {
    return (Optional<DateCreate>) issueDate;
  }

  /** Region of issuance must be provided as a two-character CLDR country code */
  @JsonIgnore
  public String issuingRegionCode() {
    return issuingRegionCode;
  }

  /** Identification type */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<Type> type() {
    return (Optional<Type>) type;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public ForeignIdentificationCreate withExpirationDate(DateCreate expirationDate) {
    Utils.checkNotNull(expirationDate, "expirationDate");
    this.expirationDate = Optional.ofNullable(expirationDate);
    return this;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public ForeignIdentificationCreate withExpirationDate(
      Optional<? extends DateCreate> expirationDate) {
    Utils.checkNotNull(expirationDate, "expirationDate");
    this.expirationDate = expirationDate;
    return this;
  }

  /** Denotes if the identification is a tax id or other */
  public ForeignIdentificationCreate withFtin(boolean ftin) {
    Utils.checkNotNull(ftin, "ftin");
    this.ftin = ftin;
    return this;
  }

  /** Identification number */
  public ForeignIdentificationCreate withIdentificationNumber(String identificationNumber) {
    Utils.checkNotNull(identificationNumber, "identificationNumber");
    this.identificationNumber = identificationNumber;
    return this;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public ForeignIdentificationCreate withIssueDate(DateCreate issueDate) {
    Utils.checkNotNull(issueDate, "issueDate");
    this.issueDate = Optional.ofNullable(issueDate);
    return this;
  }

  /**
   * Represents a whole or partial calendar date, such as a birthday. The time of day and time zone
   * are either specified elsewhere or are insignificant. The date is relative to the Gregorian
   * Calendar. This can represent one of the following:
   *
   * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
   * zero year, such as an anniversary * A year on its own, with zero month and day values * A year
   * and month value, with a zero day, such as a credit card expiration date
   *
   * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
   * `google.protobuf.Timestamp`.
   */
  public ForeignIdentificationCreate withIssueDate(Optional<? extends DateCreate> issueDate) {
    Utils.checkNotNull(issueDate, "issueDate");
    this.issueDate = issueDate;
    return this;
  }

  /** Region of issuance must be provided as a two-character CLDR country code */
  public ForeignIdentificationCreate withIssuingRegionCode(String issuingRegionCode) {
    Utils.checkNotNull(issuingRegionCode, "issuingRegionCode");
    this.issuingRegionCode = issuingRegionCode;
    return this;
  }

  /** Identification type */
  public ForeignIdentificationCreate withType(Type type) {
    Utils.checkNotNull(type, "type");
    this.type = Optional.ofNullable(type);
    return this;
  }

  /** Identification type */
  public ForeignIdentificationCreate withType(Optional<? extends Type> type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ForeignIdentificationCreate other = (ForeignIdentificationCreate) o;
    return Utils.enhancedDeepEquals(this.expirationDate, other.expirationDate)
        && Utils.enhancedDeepEquals(this.ftin, other.ftin)
        && Utils.enhancedDeepEquals(this.identificationNumber, other.identificationNumber)
        && Utils.enhancedDeepEquals(this.issueDate, other.issueDate)
        && Utils.enhancedDeepEquals(this.issuingRegionCode, other.issuingRegionCode)
        && Utils.enhancedDeepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(
        expirationDate, ftin, identificationNumber, issueDate, issuingRegionCode, type);
  }

  @Override
  public String toString() {
    return Utils.toString(
        ForeignIdentificationCreate.class,
        "expirationDate",
        expirationDate,
        "ftin",
        ftin,
        "identificationNumber",
        identificationNumber,
        "issueDate",
        issueDate,
        "issuingRegionCode",
        issuingRegionCode,
        "type",
        type);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private Optional<? extends DateCreate> expirationDate = Optional.empty();

    private Boolean ftin;

    private String identificationNumber;

    private Optional<? extends DateCreate> issueDate = Optional.empty();

    private String issuingRegionCode;

    private Optional<? extends Type> type = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder expirationDate(DateCreate expirationDate) {
      Utils.checkNotNull(expirationDate, "expirationDate");
      this.expirationDate = Optional.ofNullable(expirationDate);
      return this;
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder expirationDate(Optional<? extends DateCreate> expirationDate) {
      Utils.checkNotNull(expirationDate, "expirationDate");
      this.expirationDate = expirationDate;
      return this;
    }

    /** Denotes if the identification is a tax id or other */
    public Builder ftin(boolean ftin) {
      Utils.checkNotNull(ftin, "ftin");
      this.ftin = ftin;
      return this;
    }

    /** Identification number */
    public Builder identificationNumber(String identificationNumber) {
      Utils.checkNotNull(identificationNumber, "identificationNumber");
      this.identificationNumber = identificationNumber;
      return this;
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder issueDate(DateCreate issueDate) {
      Utils.checkNotNull(issueDate, "issueDate");
      this.issueDate = Optional.ofNullable(issueDate);
      return this;
    }

    /**
     * Represents a whole or partial calendar date, such as a birthday. The time of day and time
     * zone are either specified elsewhere or are insignificant. The date is relative to the
     * Gregorian Calendar. This can represent one of the following:
     *
     * <p>* A full date, with non-zero year, month, and day values * A month and day value, with a
     * zero year, such as an anniversary * A year on its own, with zero month and day values * A
     * year and month value, with a zero day, such as a credit card expiration date
     *
     * <p>Related types are [google.type.TimeOfDay][google.type.TimeOfDay] and
     * `google.protobuf.Timestamp`.
     */
    public Builder issueDate(Optional<? extends DateCreate> issueDate) {
      Utils.checkNotNull(issueDate, "issueDate");
      this.issueDate = issueDate;
      return this;
    }

    /** Region of issuance must be provided as a two-character CLDR country code */
    public Builder issuingRegionCode(String issuingRegionCode) {
      Utils.checkNotNull(issuingRegionCode, "issuingRegionCode");
      this.issuingRegionCode = issuingRegionCode;
      return this;
    }

    /** Identification type */
    public Builder type(Type type) {
      Utils.checkNotNull(type, "type");
      this.type = Optional.ofNullable(type);
      return this;
    }

    /** Identification type */
    public Builder type(Optional<? extends Type> type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public ForeignIdentificationCreate build() {

      return new ForeignIdentificationCreate(
          expirationDate, ftin, identificationNumber, issueDate, issuingRegionCode, type);
    }
  }
}
