/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * WatchlistScreen
 *
 * <p>The screen state of one screening within an investigation, one of: -
 * `SCREEN_STATE_UNSPECIFIED` - Default/Null value. - `PENDING` - Screen result is pending. -
 * `PASSED` - Screen result has passed. - `FAILED` - Screen result has failed. - `NEEDS_REVIEW` -
 * Screen result needs manual review. - `DEFERRED_REVIEW` - Screen result is deferred for review at
 * a later date. - `OUT_OF_SCOPE` - Screen state is out of scope for this investigation type.
 */
@JsonDeserialize(using = WatchlistScreen._Deserializer.class)
@JsonSerialize(using = WatchlistScreen._Serializer.class)
public class WatchlistScreen {

  public static final WatchlistScreen SCREEN_STATE_UNSPECIFIED =
      new WatchlistScreen("SCREEN_STATE_UNSPECIFIED");
  public static final WatchlistScreen PENDING = new WatchlistScreen("PENDING");
  public static final WatchlistScreen PASSED = new WatchlistScreen("PASSED");
  public static final WatchlistScreen FAILED = new WatchlistScreen("FAILED");
  public static final WatchlistScreen NEEDS_REVIEW = new WatchlistScreen("NEEDS_REVIEW");
  public static final WatchlistScreen DEFERRED_REVIEW = new WatchlistScreen("DEFERRED_REVIEW");
  public static final WatchlistScreen OUT_OF_SCOPE = new WatchlistScreen("OUT_OF_SCOPE");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, WatchlistScreen> values = createValuesMap();
  private static final Map<String, WatchlistScreenEnum> enums = createEnumsMap();

  private final String value;

  private WatchlistScreen(String value) {
    this.value = value;
  }

  /**
   * Returns a WatchlistScreen with the given value. For a specific value the returned object will
   * always be a singleton so reference equality is satisfied when the values are the same.
   *
   * @param value value to be wrapped as WatchlistScreen
   */
  public static WatchlistScreen of(String value) {
    synchronized (WatchlistScreen.class) {
      return values.computeIfAbsent(value, v -> new WatchlistScreen(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<WatchlistScreenEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    WatchlistScreen other = (WatchlistScreen) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "WatchlistScreen [value=" + value + "]";
  }

  // return an array just like an enum
  public static WatchlistScreen[] values() {
    synchronized (WatchlistScreen.class) {
      return values.values().toArray(new WatchlistScreen[] {});
    }
  }

  private static final Map<String, WatchlistScreen> createValuesMap() {
    Map<String, WatchlistScreen> map = new LinkedHashMap<>();
    map.put("SCREEN_STATE_UNSPECIFIED", SCREEN_STATE_UNSPECIFIED);
    map.put("PENDING", PENDING);
    map.put("PASSED", PASSED);
    map.put("FAILED", FAILED);
    map.put("NEEDS_REVIEW", NEEDS_REVIEW);
    map.put("DEFERRED_REVIEW", DEFERRED_REVIEW);
    map.put("OUT_OF_SCOPE", OUT_OF_SCOPE);
    return map;
  }

  private static final Map<String, WatchlistScreenEnum> createEnumsMap() {
    Map<String, WatchlistScreenEnum> map = new HashMap<>();
    map.put("SCREEN_STATE_UNSPECIFIED", WatchlistScreenEnum.SCREEN_STATE_UNSPECIFIED);
    map.put("PENDING", WatchlistScreenEnum.PENDING);
    map.put("PASSED", WatchlistScreenEnum.PASSED);
    map.put("FAILED", WatchlistScreenEnum.FAILED);
    map.put("NEEDS_REVIEW", WatchlistScreenEnum.NEEDS_REVIEW);
    map.put("DEFERRED_REVIEW", WatchlistScreenEnum.DEFERRED_REVIEW);
    map.put("OUT_OF_SCOPE", WatchlistScreenEnum.OUT_OF_SCOPE);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<WatchlistScreen> {

    protected _Serializer() {
      super(WatchlistScreen.class);
    }

    @Override
    public void serialize(WatchlistScreen value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<WatchlistScreen> {

    protected _Deserializer() {
      super(WatchlistScreen.class);
    }

    @Override
    public WatchlistScreen deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return WatchlistScreen.of(v);
    }
  }

  public enum WatchlistScreenEnum {
    SCREEN_STATE_UNSPECIFIED("SCREEN_STATE_UNSPECIFIED"),
    PENDING("PENDING"),
    PASSED("PASSED"),
    FAILED("FAILED"),
    NEEDS_REVIEW("NEEDS_REVIEW"),
    DEFERRED_REVIEW("DEFERRED_REVIEW"),
    OUT_OF_SCOPE("OUT_OF_SCOPE"),
    ;

    private final String value;

    private WatchlistScreenEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
