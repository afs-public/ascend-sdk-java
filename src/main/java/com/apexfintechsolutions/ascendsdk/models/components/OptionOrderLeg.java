/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * OptionOrderLeg
 *
 * <p>The message describing an option order leg.
 */
public class OptionOrderLeg {
  /**
   * The unique identifier of the asset in the Apex system. If the requested identifier does not
   * match any known Apex asset, then this field will not be set.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("asset_id")
  private Optional<String> assetId;

  /**
   * The type of the asset in this option order leg. Orders with only a single leg may only contain
   * an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg, and no
   * more than one EQUITY leg.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("asset_type")
  private Optional<? extends OptionOrderLegAssetType> assetType;

  /**
   * The computed average price for this leg, which is a weighted average (by quantity) of the
   * executed prices.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("average_price")
  private JsonNullable<? extends AveragePrice> averagePrice;

  /**
   * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When `side`
   * is SELL, `direction` will be CREDIT.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("direction")
  private Optional<? extends OptionOrderLegDirection> direction;

  /** The list of executions contributing to the fill of this leg of the option order. */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("executions")
  private Optional<? extends List<OptionOrderLegExecution>> executions;

  /**
   * The identifier of the asset, in the format specified by `identifier_type`. When
   * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and 21
   * characters. (This value must not contain any spaces between the symbol and expiry date.) When
   * `identifier_type` is SYMBOL, this field must contain the ticker value of the security, which is
   * between 1 and 6 characters and must conform the same input constraints called out in the
   * non-options single option order API.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("identifier")
  private Optional<String> identifier;

  /**
   * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
   * the identifier_type must be OSI.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("identifier_type")
  private Optional<? extends OptionOrderLegIdentifierType> identifierType;

  /**
   * This specifies the quantity of security to be ordered in this leg. For a single-leg order, this
   * value must be 1 (one). For a multi-leg order, this value must be greater than zero, and the
   * total number of shares or contracts ordered at the market will be `order.quantity *
   * leg.ratio_quantity`.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("ratio_quantity")
  private Optional<Integer> ratioQuantity;

  /** The side of the option order to be sent to the market. */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("side")
  private Optional<? extends OptionOrderLegSide> side;

  /**
   * The open/close modification of the side, signaling whether this option order leg intends to
   * OPEN a new position, or to CLOSE an existing one.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("side_modifier")
  private Optional<? extends OptionOrderLegSideModifier> sideModifier;

  @JsonCreator
  public OptionOrderLeg(
      @JsonProperty("asset_id") Optional<String> assetId,
      @JsonProperty("asset_type") Optional<? extends OptionOrderLegAssetType> assetType,
      @JsonProperty("average_price") JsonNullable<? extends AveragePrice> averagePrice,
      @JsonProperty("direction") Optional<? extends OptionOrderLegDirection> direction,
      @JsonProperty("executions") Optional<? extends List<OptionOrderLegExecution>> executions,
      @JsonProperty("identifier") Optional<String> identifier,
      @JsonProperty("identifier_type")
          Optional<? extends OptionOrderLegIdentifierType> identifierType,
      @JsonProperty("ratio_quantity") Optional<Integer> ratioQuantity,
      @JsonProperty("side") Optional<? extends OptionOrderLegSide> side,
      @JsonProperty("side_modifier") Optional<? extends OptionOrderLegSideModifier> sideModifier) {
    Utils.checkNotNull(assetId, "assetId");
    Utils.checkNotNull(assetType, "assetType");
    Utils.checkNotNull(averagePrice, "averagePrice");
    Utils.checkNotNull(direction, "direction");
    Utils.checkNotNull(executions, "executions");
    Utils.checkNotNull(identifier, "identifier");
    Utils.checkNotNull(identifierType, "identifierType");
    Utils.checkNotNull(ratioQuantity, "ratioQuantity");
    Utils.checkNotNull(side, "side");
    Utils.checkNotNull(sideModifier, "sideModifier");
    this.assetId = assetId;
    this.assetType = assetType;
    this.averagePrice = averagePrice;
    this.direction = direction;
    this.executions = executions;
    this.identifier = identifier;
    this.identifierType = identifierType;
    this.ratioQuantity = ratioQuantity;
    this.side = side;
    this.sideModifier = sideModifier;
  }

  public OptionOrderLeg() {
    this(
        Optional.empty(),
        Optional.empty(),
        JsonNullable.undefined(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        Optional.empty());
  }

  /**
   * The unique identifier of the asset in the Apex system. If the requested identifier does not
   * match any known Apex asset, then this field will not be set.
   */
  @JsonIgnore
  public Optional<String> assetId() {
    return assetId;
  }

  /**
   * The type of the asset in this option order leg. Orders with only a single leg may only contain
   * an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg, and no
   * more than one EQUITY leg.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OptionOrderLegAssetType> assetType() {
    return (Optional<OptionOrderLegAssetType>) assetType;
  }

  /**
   * The computed average price for this leg, which is a weighted average (by quantity) of the
   * executed prices.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public JsonNullable<AveragePrice> averagePrice() {
    return (JsonNullable<AveragePrice>) averagePrice;
  }

  /**
   * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When `side`
   * is SELL, `direction` will be CREDIT.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OptionOrderLegDirection> direction() {
    return (Optional<OptionOrderLegDirection>) direction;
  }

  /** The list of executions contributing to the fill of this leg of the option order. */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<List<OptionOrderLegExecution>> executions() {
    return (Optional<List<OptionOrderLegExecution>>) executions;
  }

  /**
   * The identifier of the asset, in the format specified by `identifier_type`. When
   * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and 21
   * characters. (This value must not contain any spaces between the symbol and expiry date.) When
   * `identifier_type` is SYMBOL, this field must contain the ticker value of the security, which is
   * between 1 and 6 characters and must conform the same input constraints called out in the
   * non-options single option order API.
   */
  @JsonIgnore
  public Optional<String> identifier() {
    return identifier;
  }

  /**
   * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
   * the identifier_type must be OSI.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OptionOrderLegIdentifierType> identifierType() {
    return (Optional<OptionOrderLegIdentifierType>) identifierType;
  }

  /**
   * This specifies the quantity of security to be ordered in this leg. For a single-leg order, this
   * value must be 1 (one). For a multi-leg order, this value must be greater than zero, and the
   * total number of shares or contracts ordered at the market will be `order.quantity *
   * leg.ratio_quantity`.
   */
  @JsonIgnore
  public Optional<Integer> ratioQuantity() {
    return ratioQuantity;
  }

  /** The side of the option order to be sent to the market. */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OptionOrderLegSide> side() {
    return (Optional<OptionOrderLegSide>) side;
  }

  /**
   * The open/close modification of the side, signaling whether this option order leg intends to
   * OPEN a new position, or to CLOSE an existing one.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<OptionOrderLegSideModifier> sideModifier() {
    return (Optional<OptionOrderLegSideModifier>) sideModifier;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * The unique identifier of the asset in the Apex system. If the requested identifier does not
   * match any known Apex asset, then this field will not be set.
   */
  public OptionOrderLeg withAssetId(String assetId) {
    Utils.checkNotNull(assetId, "assetId");
    this.assetId = Optional.ofNullable(assetId);
    return this;
  }

  /**
   * The unique identifier of the asset in the Apex system. If the requested identifier does not
   * match any known Apex asset, then this field will not be set.
   */
  public OptionOrderLeg withAssetId(Optional<String> assetId) {
    Utils.checkNotNull(assetId, "assetId");
    this.assetId = assetId;
    return this;
  }

  /**
   * The type of the asset in this option order leg. Orders with only a single leg may only contain
   * an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg, and no
   * more than one EQUITY leg.
   */
  public OptionOrderLeg withAssetType(OptionOrderLegAssetType assetType) {
    Utils.checkNotNull(assetType, "assetType");
    this.assetType = Optional.ofNullable(assetType);
    return this;
  }

  /**
   * The type of the asset in this option order leg. Orders with only a single leg may only contain
   * an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg, and no
   * more than one EQUITY leg.
   */
  public OptionOrderLeg withAssetType(Optional<? extends OptionOrderLegAssetType> assetType) {
    Utils.checkNotNull(assetType, "assetType");
    this.assetType = assetType;
    return this;
  }

  /**
   * The computed average price for this leg, which is a weighted average (by quantity) of the
   * executed prices.
   */
  public OptionOrderLeg withAveragePrice(AveragePrice averagePrice) {
    Utils.checkNotNull(averagePrice, "averagePrice");
    this.averagePrice = JsonNullable.of(averagePrice);
    return this;
  }

  /**
   * The computed average price for this leg, which is a weighted average (by quantity) of the
   * executed prices.
   */
  public OptionOrderLeg withAveragePrice(JsonNullable<? extends AveragePrice> averagePrice) {
    Utils.checkNotNull(averagePrice, "averagePrice");
    this.averagePrice = averagePrice;
    return this;
  }

  /**
   * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When `side`
   * is SELL, `direction` will be CREDIT.
   */
  public OptionOrderLeg withDirection(OptionOrderLegDirection direction) {
    Utils.checkNotNull(direction, "direction");
    this.direction = Optional.ofNullable(direction);
    return this;
  }

  /**
   * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When `side`
   * is SELL, `direction` will be CREDIT.
   */
  public OptionOrderLeg withDirection(Optional<? extends OptionOrderLegDirection> direction) {
    Utils.checkNotNull(direction, "direction");
    this.direction = direction;
    return this;
  }

  /** The list of executions contributing to the fill of this leg of the option order. */
  public OptionOrderLeg withExecutions(List<OptionOrderLegExecution> executions) {
    Utils.checkNotNull(executions, "executions");
    this.executions = Optional.ofNullable(executions);
    return this;
  }

  /** The list of executions contributing to the fill of this leg of the option order. */
  public OptionOrderLeg withExecutions(
      Optional<? extends List<OptionOrderLegExecution>> executions) {
    Utils.checkNotNull(executions, "executions");
    this.executions = executions;
    return this;
  }

  /**
   * The identifier of the asset, in the format specified by `identifier_type`. When
   * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and 21
   * characters. (This value must not contain any spaces between the symbol and expiry date.) When
   * `identifier_type` is SYMBOL, this field must contain the ticker value of the security, which is
   * between 1 and 6 characters and must conform the same input constraints called out in the
   * non-options single option order API.
   */
  public OptionOrderLeg withIdentifier(String identifier) {
    Utils.checkNotNull(identifier, "identifier");
    this.identifier = Optional.ofNullable(identifier);
    return this;
  }

  /**
   * The identifier of the asset, in the format specified by `identifier_type`. When
   * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and 21
   * characters. (This value must not contain any spaces between the symbol and expiry date.) When
   * `identifier_type` is SYMBOL, this field must contain the ticker value of the security, which is
   * between 1 and 6 characters and must conform the same input constraints called out in the
   * non-options single option order API.
   */
  public OptionOrderLeg withIdentifier(Optional<String> identifier) {
    Utils.checkNotNull(identifier, "identifier");
    this.identifier = identifier;
    return this;
  }

  /**
   * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
   * the identifier_type must be OSI.
   */
  public OptionOrderLeg withIdentifierType(OptionOrderLegIdentifierType identifierType) {
    Utils.checkNotNull(identifierType, "identifierType");
    this.identifierType = Optional.ofNullable(identifierType);
    return this;
  }

  /**
   * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
   * the identifier_type must be OSI.
   */
  public OptionOrderLeg withIdentifierType(
      Optional<? extends OptionOrderLegIdentifierType> identifierType) {
    Utils.checkNotNull(identifierType, "identifierType");
    this.identifierType = identifierType;
    return this;
  }

  /**
   * This specifies the quantity of security to be ordered in this leg. For a single-leg order, this
   * value must be 1 (one). For a multi-leg order, this value must be greater than zero, and the
   * total number of shares or contracts ordered at the market will be `order.quantity *
   * leg.ratio_quantity`.
   */
  public OptionOrderLeg withRatioQuantity(int ratioQuantity) {
    Utils.checkNotNull(ratioQuantity, "ratioQuantity");
    this.ratioQuantity = Optional.ofNullable(ratioQuantity);
    return this;
  }

  /**
   * This specifies the quantity of security to be ordered in this leg. For a single-leg order, this
   * value must be 1 (one). For a multi-leg order, this value must be greater than zero, and the
   * total number of shares or contracts ordered at the market will be `order.quantity *
   * leg.ratio_quantity`.
   */
  public OptionOrderLeg withRatioQuantity(Optional<Integer> ratioQuantity) {
    Utils.checkNotNull(ratioQuantity, "ratioQuantity");
    this.ratioQuantity = ratioQuantity;
    return this;
  }

  /** The side of the option order to be sent to the market. */
  public OptionOrderLeg withSide(OptionOrderLegSide side) {
    Utils.checkNotNull(side, "side");
    this.side = Optional.ofNullable(side);
    return this;
  }

  /** The side of the option order to be sent to the market. */
  public OptionOrderLeg withSide(Optional<? extends OptionOrderLegSide> side) {
    Utils.checkNotNull(side, "side");
    this.side = side;
    return this;
  }

  /**
   * The open/close modification of the side, signaling whether this option order leg intends to
   * OPEN a new position, or to CLOSE an existing one.
   */
  public OptionOrderLeg withSideModifier(OptionOrderLegSideModifier sideModifier) {
    Utils.checkNotNull(sideModifier, "sideModifier");
    this.sideModifier = Optional.ofNullable(sideModifier);
    return this;
  }

  /**
   * The open/close modification of the side, signaling whether this option order leg intends to
   * OPEN a new position, or to CLOSE an existing one.
   */
  public OptionOrderLeg withSideModifier(
      Optional<? extends OptionOrderLegSideModifier> sideModifier) {
    Utils.checkNotNull(sideModifier, "sideModifier");
    this.sideModifier = sideModifier;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    OptionOrderLeg other = (OptionOrderLeg) o;
    return Utils.enhancedDeepEquals(this.assetId, other.assetId)
        && Utils.enhancedDeepEquals(this.assetType, other.assetType)
        && Utils.enhancedDeepEquals(this.averagePrice, other.averagePrice)
        && Utils.enhancedDeepEquals(this.direction, other.direction)
        && Utils.enhancedDeepEquals(this.executions, other.executions)
        && Utils.enhancedDeepEquals(this.identifier, other.identifier)
        && Utils.enhancedDeepEquals(this.identifierType, other.identifierType)
        && Utils.enhancedDeepEquals(this.ratioQuantity, other.ratioQuantity)
        && Utils.enhancedDeepEquals(this.side, other.side)
        && Utils.enhancedDeepEquals(this.sideModifier, other.sideModifier);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(
        assetId,
        assetType,
        averagePrice,
        direction,
        executions,
        identifier,
        identifierType,
        ratioQuantity,
        side,
        sideModifier);
  }

  @Override
  public String toString() {
    return Utils.toString(
        OptionOrderLeg.class,
        "assetId",
        assetId,
        "assetType",
        assetType,
        "averagePrice",
        averagePrice,
        "direction",
        direction,
        "executions",
        executions,
        "identifier",
        identifier,
        "identifierType",
        identifierType,
        "ratioQuantity",
        ratioQuantity,
        "side",
        side,
        "sideModifier",
        sideModifier);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private Optional<String> assetId = Optional.empty();

    private Optional<? extends OptionOrderLegAssetType> assetType = Optional.empty();

    private JsonNullable<? extends AveragePrice> averagePrice = JsonNullable.undefined();

    private Optional<? extends OptionOrderLegDirection> direction = Optional.empty();

    private Optional<? extends List<OptionOrderLegExecution>> executions = Optional.empty();

    private Optional<String> identifier = Optional.empty();

    private Optional<? extends OptionOrderLegIdentifierType> identifierType = Optional.empty();

    private Optional<Integer> ratioQuantity = Optional.empty();

    private Optional<? extends OptionOrderLegSide> side = Optional.empty();

    private Optional<? extends OptionOrderLegSideModifier> sideModifier = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The unique identifier of the asset in the Apex system. If the requested identifier does not
     * match any known Apex asset, then this field will not be set.
     */
    public Builder assetId(String assetId) {
      Utils.checkNotNull(assetId, "assetId");
      this.assetId = Optional.ofNullable(assetId);
      return this;
    }

    /**
     * The unique identifier of the asset in the Apex system. If the requested identifier does not
     * match any known Apex asset, then this field will not be set.
     */
    public Builder assetId(Optional<String> assetId) {
      Utils.checkNotNull(assetId, "assetId");
      this.assetId = assetId;
      return this;
    }

    /**
     * The type of the asset in this option order leg. Orders with only a single leg may only
     * contain an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg,
     * and no more than one EQUITY leg.
     */
    public Builder assetType(OptionOrderLegAssetType assetType) {
      Utils.checkNotNull(assetType, "assetType");
      this.assetType = Optional.ofNullable(assetType);
      return this;
    }

    /**
     * The type of the asset in this option order leg. Orders with only a single leg may only
     * contain an OPTION asset type. Orders with multiple legs must contain at least one OPTION leg,
     * and no more than one EQUITY leg.
     */
    public Builder assetType(Optional<? extends OptionOrderLegAssetType> assetType) {
      Utils.checkNotNull(assetType, "assetType");
      this.assetType = assetType;
      return this;
    }

    /**
     * The computed average price for this leg, which is a weighted average (by quantity) of the
     * executed prices.
     */
    public Builder averagePrice(AveragePrice averagePrice) {
      Utils.checkNotNull(averagePrice, "averagePrice");
      this.averagePrice = JsonNullable.of(averagePrice);
      return this;
    }

    /**
     * The computed average price for this leg, which is a weighted average (by quantity) of the
     * executed prices.
     */
    public Builder averagePrice(JsonNullable<? extends AveragePrice> averagePrice) {
      Utils.checkNotNull(averagePrice, "averagePrice");
      this.averagePrice = averagePrice;
      return this;
    }

    /**
     * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When
     * `side` is SELL, `direction` will be CREDIT.
     */
    public Builder direction(OptionOrderLegDirection direction) {
      Utils.checkNotNull(direction, "direction");
      this.direction = Optional.ofNullable(direction);
      return this;
    }

    /**
     * The computed direction for this leg. When `side` is BUY, `direction` will be DEBIT. When
     * `side` is SELL, `direction` will be CREDIT.
     */
    public Builder direction(Optional<? extends OptionOrderLegDirection> direction) {
      Utils.checkNotNull(direction, "direction");
      this.direction = direction;
      return this;
    }

    /** The list of executions contributing to the fill of this leg of the option order. */
    public Builder executions(List<OptionOrderLegExecution> executions) {
      Utils.checkNotNull(executions, "executions");
      this.executions = Optional.ofNullable(executions);
      return this;
    }

    /** The list of executions contributing to the fill of this leg of the option order. */
    public Builder executions(Optional<? extends List<OptionOrderLegExecution>> executions) {
      Utils.checkNotNull(executions, "executions");
      this.executions = executions;
      return this;
    }

    /**
     * The identifier of the asset, in the format specified by `identifier_type`. When
     * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and
     * 21 characters. (This value must not contain any spaces between the symbol and expiry date.)
     * When `identifier_type` is SYMBOL, this field must contain the ticker value of the security,
     * which is between 1 and 6 characters and must conform the same input constraints called out in
     * the non-options single option order API.
     */
    public Builder identifier(String identifier) {
      Utils.checkNotNull(identifier, "identifier");
      this.identifier = Optional.ofNullable(identifier);
      return this;
    }

    /**
     * The identifier of the asset, in the format specified by `identifier_type`. When
     * `identifier_type` is OSI, this field must be a valid OSI identifier, which is between 16 and
     * 21 characters. (This value must not contain any spaces between the symbol and expiry date.)
     * When `identifier_type` is SYMBOL, this field must contain the ticker value of the security,
     * which is between 1 and 6 characters and must conform the same input constraints called out in
     * the non-options single option order API.
     */
    public Builder identifier(Optional<String> identifier) {
      Utils.checkNotNull(identifier, "identifier");
      this.identifier = identifier;
      return this;
    }

    /**
     * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
     * the identifier_type must be OSI.
     */
    public Builder identifierType(OptionOrderLegIdentifierType identifierType) {
      Utils.checkNotNull(identifierType, "identifierType");
      this.identifierType = Optional.ofNullable(identifierType);
      return this;
    }

    /**
     * The format of the identifier value used to look up the security. When `asset_type` is OPTION,
     * the identifier_type must be OSI.
     */
    public Builder identifierType(Optional<? extends OptionOrderLegIdentifierType> identifierType) {
      Utils.checkNotNull(identifierType, "identifierType");
      this.identifierType = identifierType;
      return this;
    }

    /**
     * This specifies the quantity of security to be ordered in this leg. For a single-leg order,
     * this value must be 1 (one). For a multi-leg order, this value must be greater than zero, and
     * the total number of shares or contracts ordered at the market will be `order.quantity *
     * leg.ratio_quantity`.
     */
    public Builder ratioQuantity(int ratioQuantity) {
      Utils.checkNotNull(ratioQuantity, "ratioQuantity");
      this.ratioQuantity = Optional.ofNullable(ratioQuantity);
      return this;
    }

    /**
     * This specifies the quantity of security to be ordered in this leg. For a single-leg order,
     * this value must be 1 (one). For a multi-leg order, this value must be greater than zero, and
     * the total number of shares or contracts ordered at the market will be `order.quantity *
     * leg.ratio_quantity`.
     */
    public Builder ratioQuantity(Optional<Integer> ratioQuantity) {
      Utils.checkNotNull(ratioQuantity, "ratioQuantity");
      this.ratioQuantity = ratioQuantity;
      return this;
    }

    /** The side of the option order to be sent to the market. */
    public Builder side(OptionOrderLegSide side) {
      Utils.checkNotNull(side, "side");
      this.side = Optional.ofNullable(side);
      return this;
    }

    /** The side of the option order to be sent to the market. */
    public Builder side(Optional<? extends OptionOrderLegSide> side) {
      Utils.checkNotNull(side, "side");
      this.side = side;
      return this;
    }

    /**
     * The open/close modification of the side, signaling whether this option order leg intends to
     * OPEN a new position, or to CLOSE an existing one.
     */
    public Builder sideModifier(OptionOrderLegSideModifier sideModifier) {
      Utils.checkNotNull(sideModifier, "sideModifier");
      this.sideModifier = Optional.ofNullable(sideModifier);
      return this;
    }

    /**
     * The open/close modification of the side, signaling whether this option order leg intends to
     * OPEN a new position, or to CLOSE an existing one.
     */
    public Builder sideModifier(Optional<? extends OptionOrderLegSideModifier> sideModifier) {
      Utils.checkNotNull(sideModifier, "sideModifier");
      this.sideModifier = sideModifier;
      return this;
    }

    public OptionOrderLeg build() {

      return new OptionOrderLeg(
          assetId,
          assetType,
          averagePrice,
          direction,
          executions,
          identifier,
          identifierType,
          ratioQuantity,
          side,
          sideModifier);
    }
  }
}
