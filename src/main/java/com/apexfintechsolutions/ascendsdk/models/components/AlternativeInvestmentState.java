/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper class for an "open" enum. "Open" enums are those that are expected to evolve
 * (particularly with the addition of enum members over time). If an open enum is used then the
 * appearance of unexpected enum values (say in a response from an updated an API) will not bring
 * about a runtime error thus ensuring that non-updated client versions can continue to work without
 * error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe cache is
 * maintained to ensure that). As a consequence instances created with the same value will satisfy
 * reference equality (via {@code ==}).
 *
 * <p>This class is intended to emulate an enum (in terms of common usage and with reference
 * equality) but with the ability to carry unknown values. Unfortunately Java does not permit the
 * use of an instance in a switch expression but you can use the {@code asEnum()} method (after
 * dealing with the `Optional` appropriately).
 */
/**
 * AlternativeInvestmentState
 *
 * <p>The asset’s state in the Apex alternative investment platform.
 */
@JsonDeserialize(using = AlternativeInvestmentState._Deserializer.class)
@JsonSerialize(using = AlternativeInvestmentState._Serializer.class)
public class AlternativeInvestmentState {

  public static final AlternativeInvestmentState STATE_UNSPECIFIED =
      new AlternativeInvestmentState("STATE_UNSPECIFIED");
  public static final AlternativeInvestmentState OPEN = new AlternativeInvestmentState("OPEN");
  public static final AlternativeInvestmentState CLOSED = new AlternativeInvestmentState("CLOSED");

  // This map will grow whenever a Color gets created with a new
  // unrecognized value (a potential memory leak if the user is not
  // careful). Keep this field lower case to avoid clashing with
  // generated member names which will always be upper cased (Java
  // convention)
  private static final Map<String, AlternativeInvestmentState> values = createValuesMap();
  private static final Map<String, AlternativeInvestmentStateEnum> enums = createEnumsMap();

  private final String value;

  private AlternativeInvestmentState(String value) {
    this.value = value;
  }

  /**
   * Returns a AlternativeInvestmentState with the given value. For a specific value the returned
   * object will always be a singleton so reference equality is satisfied when the values are the
   * same.
   *
   * @param value value to be wrapped as AlternativeInvestmentState
   */
  public static AlternativeInvestmentState of(String value) {
    synchronized (AlternativeInvestmentState.class) {
      return values.computeIfAbsent(value, v -> new AlternativeInvestmentState(v));
    }
  }

  public String value() {
    return value;
  }

  public Optional<AlternativeInvestmentStateEnum> asEnum() {
    return Optional.ofNullable(enums.getOrDefault(value, null));
  }

  public boolean isKnown() {
    return asEnum().isPresent();
  }

  @Override
  public int hashCode() {
    return Objects.hash(value);
  }

  @Override
  public boolean equals(java.lang.Object obj) {
    if (this == obj) return true;
    if (obj == null) return false;
    if (getClass() != obj.getClass()) return false;
    AlternativeInvestmentState other = (AlternativeInvestmentState) obj;
    return Objects.equals(value, other.value);
  }

  @Override
  public String toString() {
    return "AlternativeInvestmentState [value=" + value + "]";
  }

  // return an array just like an enum
  public static AlternativeInvestmentState[] values() {
    synchronized (AlternativeInvestmentState.class) {
      return values.values().toArray(new AlternativeInvestmentState[] {});
    }
  }

  private static final Map<String, AlternativeInvestmentState> createValuesMap() {
    Map<String, AlternativeInvestmentState> map = new LinkedHashMap<>();
    map.put("STATE_UNSPECIFIED", STATE_UNSPECIFIED);
    map.put("OPEN", OPEN);
    map.put("CLOSED", CLOSED);
    return map;
  }

  private static final Map<String, AlternativeInvestmentStateEnum> createEnumsMap() {
    Map<String, AlternativeInvestmentStateEnum> map = new HashMap<>();
    map.put("STATE_UNSPECIFIED", AlternativeInvestmentStateEnum.STATE_UNSPECIFIED);
    map.put("OPEN", AlternativeInvestmentStateEnum.OPEN);
    map.put("CLOSED", AlternativeInvestmentStateEnum.CLOSED);
    return map;
  }

  @SuppressWarnings("serial")
  public static final class _Serializer extends StdSerializer<AlternativeInvestmentState> {

    protected _Serializer() {
      super(AlternativeInvestmentState.class);
    }

    @Override
    public void serialize(
        AlternativeInvestmentState value, JsonGenerator g, SerializerProvider provider)
        throws IOException, JsonProcessingException {
      g.writeObject(value.value);
    }
  }

  @SuppressWarnings("serial")
  public static final class _Deserializer extends StdDeserializer<AlternativeInvestmentState> {

    protected _Deserializer() {
      super(AlternativeInvestmentState.class);
    }

    @Override
    public AlternativeInvestmentState deserialize(JsonParser p, DeserializationContext ctxt)
        throws IOException, JacksonException {
      String v = p.readValueAs(new TypeReference<String>() {});
      // use the factory method to ensure we get singletons
      return AlternativeInvestmentState.of(v);
    }
  }

  public enum AlternativeInvestmentStateEnum {
    STATE_UNSPECIFIED("STATE_UNSPECIFIED"),
    OPEN("OPEN"),
    CLOSED("CLOSED"),
    ;

    private final String value;

    private AlternativeInvestmentStateEnum(String value) {
      this.value = value;
    }

    public String value() {
      return value;
    }
  }
}
