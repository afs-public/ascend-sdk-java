/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * TradingExecutedPrice
 *
 * <p>An average price definition
 */
public class TradingExecutedPrice {
  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("price")
  private JsonNullable<? extends Price> price;

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("type")
  private Optional<? extends TradingExecutedPriceType> type;

  @JsonCreator
  public TradingExecutedPrice(
      @JsonProperty("price") JsonNullable<? extends Price> price,
      @JsonProperty("type") Optional<? extends TradingExecutedPriceType> type) {
    Utils.checkNotNull(price, "price");
    Utils.checkNotNull(type, "type");
    this.price = price;
    this.type = type;
  }

  public TradingExecutedPrice() {
    this(JsonNullable.undefined(), Optional.empty());
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public JsonNullable<Price> price() {
    return (JsonNullable<Price>) price;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<TradingExecutedPriceType> type() {
    return (Optional<TradingExecutedPriceType>) type;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  public TradingExecutedPrice withPrice(Price price) {
    Utils.checkNotNull(price, "price");
    this.price = JsonNullable.of(price);
    return this;
  }

  /**
   * The limit price which must be greater than zero if provided. For equity orders in the USD
   * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places for
   * prices at or below $1. For fixed income orders this is expressed as a percentage of par, which
   * allows up to 5 decimal places in the USD currency.
   */
  public TradingExecutedPrice withPrice(JsonNullable<? extends Price> price) {
    Utils.checkNotNull(price, "price");
    this.price = price;
    return this;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  public TradingExecutedPrice withType(TradingExecutedPriceType type) {
    Utils.checkNotNull(type, "type");
    this.type = Optional.ofNullable(type);
    return this;
  }

  /**
   * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
   * for fixed income orders.
   */
  public TradingExecutedPrice withType(Optional<? extends TradingExecutedPriceType> type) {
    Utils.checkNotNull(type, "type");
    this.type = type;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TradingExecutedPrice other = (TradingExecutedPrice) o;
    return Utils.enhancedDeepEquals(this.price, other.price)
        && Utils.enhancedDeepEquals(this.type, other.type);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(price, type);
  }

  @Override
  public String toString() {
    return Utils.toString(TradingExecutedPrice.class, "price", price, "type", type);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private JsonNullable<? extends Price> price = JsonNullable.undefined();

    private Optional<? extends TradingExecutedPriceType> type = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 5 decimal places in the USD currency.
     */
    public Builder price(Price price) {
      Utils.checkNotNull(price, "price");
      this.price = JsonNullable.of(price);
      return this;
    }

    /**
     * The limit price which must be greater than zero if provided. For equity orders in the USD
     * currency, up to 2 decimal places are allowed for prices above $1 and up to 4 decimal places
     * for prices at or below $1. For fixed income orders this is expressed as a percentage of par,
     * which allows up to 5 decimal places in the USD currency.
     */
    public Builder price(JsonNullable<? extends Price> price) {
      Utils.checkNotNull(price, "price");
      this.price = price;
      return this;
    }

    /**
     * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
     * for fixed income orders.
     */
    public Builder type(TradingExecutedPriceType type) {
      Utils.checkNotNull(type, "type");
      this.type = Optional.ofNullable(type);
      return this;
    }

    /**
     * The type of this price, which must be PRICE_PER_UNIT for equity orders, or PERCENTAGE_OF_PAR
     * for fixed income orders.
     */
    public Builder type(Optional<? extends TradingExecutedPriceType> type) {
      Utils.checkNotNull(type, "type");
      this.type = type;
      return this;
    }

    public TradingExecutedPrice build() {

      return new TradingExecutedPrice(price, type);
    }
  }
}
