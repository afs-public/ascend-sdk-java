/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.components;

import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.Optional;

/**
 * AlternativeOrderCreate
 *
 * <p>An order for an alternative investment.
 */
public class AlternativeOrderCreate {
  /**
   * A client-supplied unique ID to enforce idempotency. Each ID must be unique within the account.
   *
   * <p>If a create request includes a `client_order_id` that matches an existing order’s
   * `client_order_id`, the service will respond with `ALREADY_EXISTS` and will not create a new
   * order resource.
   */
  @JsonProperty("client_order_id")
  private String clientOrderId;

  /**
   * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified in
   * the request.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("currency_code")
  private Optional<String> currencyCode;

  /**
   * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
   * Orders will be rejected if the alternative investment asset does not allow the requested
   * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
   * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
   * properties to know what it allows.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("distribution_type")
  private Optional<? extends DistributionType> distributionType;

  /**
   * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
   * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
   * allow only partial redemption. - Not relevant to subscription orders.
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("full")
  private Optional<Boolean> full;

  /**
   * The asset identifier value, dependent on enum value set for `identifier_type`.
   *
   * <p>For example, if `identifier_type` = `ASSET_ID`, then the value for `identifier` is the
   * `asset_id` value.
   */
  @JsonProperty("identifier")
  private String identifier;

  /**
   * Identifies which parameter value is used to identify the asset in the order. The value for that
   * parameter is used in the `identifier` field.
   */
  @JsonProperty("identifier_type")
  private AlternativeOrderCreateIdentifierType identifierType;

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("notional_value")
  private Optional<? extends DecimalCreate> notionalValue;

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  @JsonInclude(Include.NON_ABSENT)
  @JsonProperty("quantity")
  private Optional<? extends DecimalCreate> quantity;

  /** The side of the order. */
  @JsonProperty("side")
  private AlternativeOrderCreateSide side;

  @JsonCreator
  public AlternativeOrderCreate(
      @JsonProperty("client_order_id") String clientOrderId,
      @JsonProperty("currency_code") Optional<String> currencyCode,
      @JsonProperty("distribution_type") Optional<? extends DistributionType> distributionType,
      @JsonProperty("full") Optional<Boolean> full,
      @JsonProperty("identifier") String identifier,
      @JsonProperty("identifier_type") AlternativeOrderCreateIdentifierType identifierType,
      @JsonProperty("notional_value") Optional<? extends DecimalCreate> notionalValue,
      @JsonProperty("quantity") Optional<? extends DecimalCreate> quantity,
      @JsonProperty("side") AlternativeOrderCreateSide side) {
    Utils.checkNotNull(clientOrderId, "clientOrderId");
    Utils.checkNotNull(currencyCode, "currencyCode");
    Utils.checkNotNull(distributionType, "distributionType");
    Utils.checkNotNull(full, "full");
    Utils.checkNotNull(identifier, "identifier");
    Utils.checkNotNull(identifierType, "identifierType");
    Utils.checkNotNull(notionalValue, "notionalValue");
    Utils.checkNotNull(quantity, "quantity");
    Utils.checkNotNull(side, "side");
    this.clientOrderId = clientOrderId;
    this.currencyCode = currencyCode;
    this.distributionType = distributionType;
    this.full = full;
    this.identifier = identifier;
    this.identifierType = identifierType;
    this.notionalValue = notionalValue;
    this.quantity = quantity;
    this.side = side;
  }

  public AlternativeOrderCreate(
      String clientOrderId,
      String identifier,
      AlternativeOrderCreateIdentifierType identifierType,
      AlternativeOrderCreateSide side) {
    this(
        clientOrderId,
        Optional.empty(),
        Optional.empty(),
        Optional.empty(),
        identifier,
        identifierType,
        Optional.empty(),
        Optional.empty(),
        side);
  }

  /**
   * A client-supplied unique ID to enforce idempotency. Each ID must be unique within the account.
   *
   * <p>If a create request includes a `client_order_id` that matches an existing order’s
   * `client_order_id`, the service will respond with `ALREADY_EXISTS` and will not create a new
   * order resource.
   */
  @JsonIgnore
  public String clientOrderId() {
    return clientOrderId;
  }

  /**
   * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified in
   * the request.
   */
  @JsonIgnore
  public Optional<String> currencyCode() {
    return currencyCode;
  }

  /**
   * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
   * Orders will be rejected if the alternative investment asset does not allow the requested
   * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
   * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
   * properties to know what it allows.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DistributionType> distributionType() {
    return (Optional<DistributionType>) distributionType;
  }

  /**
   * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
   * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
   * allow only partial redemption. - Not relevant to subscription orders.
   */
  @JsonIgnore
  public Optional<Boolean> full() {
    return full;
  }

  /**
   * The asset identifier value, dependent on enum value set for `identifier_type`.
   *
   * <p>For example, if `identifier_type` = `ASSET_ID`, then the value for `identifier` is the
   * `asset_id` value.
   */
  @JsonIgnore
  public String identifier() {
    return identifier;
  }

  /**
   * Identifies which parameter value is used to identify the asset in the order. The value for that
   * parameter is used in the `identifier` field.
   */
  @JsonIgnore
  public AlternativeOrderCreateIdentifierType identifierType() {
    return identifierType;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DecimalCreate> notionalValue() {
    return (Optional<DecimalCreate>) notionalValue;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<DecimalCreate> quantity() {
    return (Optional<DecimalCreate>) quantity;
  }

  /** The side of the order. */
  @JsonIgnore
  public AlternativeOrderCreateSide side() {
    return side;
  }

  public static Builder builder() {
    return new Builder();
  }

  /**
   * A client-supplied unique ID to enforce idempotency. Each ID must be unique within the account.
   *
   * <p>If a create request includes a `client_order_id` that matches an existing order’s
   * `client_order_id`, the service will respond with `ALREADY_EXISTS` and will not create a new
   * order resource.
   */
  public AlternativeOrderCreate withClientOrderId(String clientOrderId) {
    Utils.checkNotNull(clientOrderId, "clientOrderId");
    this.clientOrderId = clientOrderId;
    return this;
  }

  /**
   * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified in
   * the request.
   */
  public AlternativeOrderCreate withCurrencyCode(String currencyCode) {
    Utils.checkNotNull(currencyCode, "currencyCode");
    this.currencyCode = Optional.ofNullable(currencyCode);
    return this;
  }

  /**
   * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified in
   * the request.
   */
  public AlternativeOrderCreate withCurrencyCode(Optional<String> currencyCode) {
    Utils.checkNotNull(currencyCode, "currencyCode");
    this.currencyCode = currencyCode;
    return this;
  }

  /**
   * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
   * Orders will be rejected if the alternative investment asset does not allow the requested
   * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
   * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
   * properties to know what it allows.
   */
  public AlternativeOrderCreate withDistributionType(DistributionType distributionType) {
    Utils.checkNotNull(distributionType, "distributionType");
    this.distributionType = Optional.ofNullable(distributionType);
    return this;
  }

  /**
   * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
   * Orders will be rejected if the alternative investment asset does not allow the requested
   * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
   * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
   * properties to know what it allows.
   */
  public AlternativeOrderCreate withDistributionType(
      Optional<? extends DistributionType> distributionType) {
    Utils.checkNotNull(distributionType, "distributionType");
    this.distributionType = distributionType;
    return this;
  }

  /**
   * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
   * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
   * allow only partial redemption. - Not relevant to subscription orders.
   */
  public AlternativeOrderCreate withFull(boolean full) {
    Utils.checkNotNull(full, "full");
    this.full = Optional.ofNullable(full);
    return this;
  }

  /**
   * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
   * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
   * allow only partial redemption. - Not relevant to subscription orders.
   */
  public AlternativeOrderCreate withFull(Optional<Boolean> full) {
    Utils.checkNotNull(full, "full");
    this.full = full;
    return this;
  }

  /**
   * The asset identifier value, dependent on enum value set for `identifier_type`.
   *
   * <p>For example, if `identifier_type` = `ASSET_ID`, then the value for `identifier` is the
   * `asset_id` value.
   */
  public AlternativeOrderCreate withIdentifier(String identifier) {
    Utils.checkNotNull(identifier, "identifier");
    this.identifier = identifier;
    return this;
  }

  /**
   * Identifies which parameter value is used to identify the asset in the order. The value for that
   * parameter is used in the `identifier` field.
   */
  public AlternativeOrderCreate withIdentifierType(
      AlternativeOrderCreateIdentifierType identifierType) {
    Utils.checkNotNull(identifierType, "identifierType");
    this.identifierType = identifierType;
    return this;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  public AlternativeOrderCreate withNotionalValue(DecimalCreate notionalValue) {
    Utils.checkNotNull(notionalValue, "notionalValue");
    this.notionalValue = Optional.ofNullable(notionalValue);
    return this;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  public AlternativeOrderCreate withNotionalValue(Optional<? extends DecimalCreate> notionalValue) {
    Utils.checkNotNull(notionalValue, "notionalValue");
    this.notionalValue = notionalValue;
    return this;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  public AlternativeOrderCreate withQuantity(DecimalCreate quantity) {
    Utils.checkNotNull(quantity, "quantity");
    this.quantity = Optional.ofNullable(quantity);
    return this;
  }

  /**
   * A representation of a decimal value, such as 2.5. Clients may convert values into
   * language-native decimal formats, such as Java's [BigDecimal][] or Python's [decimal.Decimal][].
   *
   * <p>[BigDecimal]:
   * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
   * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
   */
  public AlternativeOrderCreate withQuantity(Optional<? extends DecimalCreate> quantity) {
    Utils.checkNotNull(quantity, "quantity");
    this.quantity = quantity;
    return this;
  }

  /** The side of the order. */
  public AlternativeOrderCreate withSide(AlternativeOrderCreateSide side) {
    Utils.checkNotNull(side, "side");
    this.side = side;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AlternativeOrderCreate other = (AlternativeOrderCreate) o;
    return Utils.enhancedDeepEquals(this.clientOrderId, other.clientOrderId)
        && Utils.enhancedDeepEquals(this.currencyCode, other.currencyCode)
        && Utils.enhancedDeepEquals(this.distributionType, other.distributionType)
        && Utils.enhancedDeepEquals(this.full, other.full)
        && Utils.enhancedDeepEquals(this.identifier, other.identifier)
        && Utils.enhancedDeepEquals(this.identifierType, other.identifierType)
        && Utils.enhancedDeepEquals(this.notionalValue, other.notionalValue)
        && Utils.enhancedDeepEquals(this.quantity, other.quantity)
        && Utils.enhancedDeepEquals(this.side, other.side);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(
        clientOrderId,
        currencyCode,
        distributionType,
        full,
        identifier,
        identifierType,
        notionalValue,
        quantity,
        side);
  }

  @Override
  public String toString() {
    return Utils.toString(
        AlternativeOrderCreate.class,
        "clientOrderId",
        clientOrderId,
        "currencyCode",
        currencyCode,
        "distributionType",
        distributionType,
        "full",
        full,
        "identifier",
        identifier,
        "identifierType",
        identifierType,
        "notionalValue",
        notionalValue,
        "quantity",
        quantity,
        "side",
        side);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private String clientOrderId;

    private Optional<String> currencyCode = Optional.empty();

    private Optional<? extends DistributionType> distributionType = Optional.empty();

    private Optional<Boolean> full = Optional.empty();

    private String identifier;

    private AlternativeOrderCreateIdentifierType identifierType;

    private Optional<? extends DecimalCreate> notionalValue = Optional.empty();

    private Optional<? extends DecimalCreate> quantity = Optional.empty();

    private AlternativeOrderCreateSide side;

    private Builder() {
      // force use of static builder() method
    }

    /**
     * A client-supplied unique ID to enforce idempotency. Each ID must be unique within the
     * account.
     *
     * <p>If a create request includes a `client_order_id` that matches an existing order’s
     * `client_order_id`, the service will respond with `ALREADY_EXISTS` and will not create a new
     * order resource.
     */
    public Builder clientOrderId(String clientOrderId) {
      Utils.checkNotNull(clientOrderId, "clientOrderId");
      this.clientOrderId = clientOrderId;
      return this;
    }

    /**
     * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified
     * in the request.
     */
    public Builder currencyCode(String currencyCode) {
      Utils.checkNotNull(currencyCode, "currencyCode");
      this.currencyCode = Optional.ofNullable(currencyCode);
      return this;
    }

    /**
     * The currency code of the order. Supports only "USD" and will default to "USD" if unspecified
     * in the request.
     */
    public Builder currencyCode(Optional<String> currencyCode) {
      Utils.checkNotNull(currencyCode, "currencyCode");
      this.currencyCode = currencyCode;
      return this;
    }

    /**
     * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
     * Orders will be rejected if the alternative investment asset does not allow the requested
     * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
     * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
     * properties to know what it allows.
     */
    public Builder distributionType(DistributionType distributionType) {
      Utils.checkNotNull(distributionType, "distributionType");
      this.distributionType = Optional.ofNullable(distributionType);
      return this;
    }

    /**
     * Identifies the distribution type for buy side orders (that is, when `side` value = `BUY`). -
     * Orders will be rejected if the alternative investment asset does not allow the requested
     * distribution type. - Not relevant for sell orders (that is, when `side` value = `SELL`). -
     * Confirm the asset’s `cash_distribution_allowed` and `reinvestment_distribution_allowed`
     * properties to know what it allows.
     */
    public Builder distributionType(Optional<? extends DistributionType> distributionType) {
      Utils.checkNotNull(distributionType, "distributionType");
      this.distributionType = distributionType;
      return this;
    }

    /**
     * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
     * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
     * allow only partial redemption. - Not relevant to subscription orders.
     */
    public Builder full(boolean full) {
      Utils.checkNotNull(full, "full");
      this.full = Optional.ofNullable(full);
      return this;
    }

    /**
     * Indicates whether this order is for a full redemption (as in, all shares redeemed). - Orders
     * can specify only one of `full`, `notional_value`, or `quantity`. - Note that the issuer may
     * allow only partial redemption. - Not relevant to subscription orders.
     */
    public Builder full(Optional<Boolean> full) {
      Utils.checkNotNull(full, "full");
      this.full = full;
      return this;
    }

    /**
     * The asset identifier value, dependent on enum value set for `identifier_type`.
     *
     * <p>For example, if `identifier_type` = `ASSET_ID`, then the value for `identifier` is the
     * `asset_id` value.
     */
    public Builder identifier(String identifier) {
      Utils.checkNotNull(identifier, "identifier");
      this.identifier = identifier;
      return this;
    }

    /**
     * Identifies which parameter value is used to identify the asset in the order. The value for
     * that parameter is used in the `identifier` field.
     */
    public Builder identifierType(AlternativeOrderCreateIdentifierType identifierType) {
      Utils.checkNotNull(identifierType, "identifierType");
      this.identifierType = identifierType;
      return this;
    }

    /**
     * A representation of a decimal value, such as 2.5. Clients may convert values into
     * language-native decimal formats, such as Java's [BigDecimal][] or Python's
     * [decimal.Decimal][].
     *
     * <p>[BigDecimal]:
     * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
     * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
     */
    public Builder notionalValue(DecimalCreate notionalValue) {
      Utils.checkNotNull(notionalValue, "notionalValue");
      this.notionalValue = Optional.ofNullable(notionalValue);
      return this;
    }

    /**
     * A representation of a decimal value, such as 2.5. Clients may convert values into
     * language-native decimal formats, such as Java's [BigDecimal][] or Python's
     * [decimal.Decimal][].
     *
     * <p>[BigDecimal]:
     * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
     * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
     */
    public Builder notionalValue(Optional<? extends DecimalCreate> notionalValue) {
      Utils.checkNotNull(notionalValue, "notionalValue");
      this.notionalValue = notionalValue;
      return this;
    }

    /**
     * A representation of a decimal value, such as 2.5. Clients may convert values into
     * language-native decimal formats, such as Java's [BigDecimal][] or Python's
     * [decimal.Decimal][].
     *
     * <p>[BigDecimal]:
     * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
     * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
     */
    public Builder quantity(DecimalCreate quantity) {
      Utils.checkNotNull(quantity, "quantity");
      this.quantity = Optional.ofNullable(quantity);
      return this;
    }

    /**
     * A representation of a decimal value, such as 2.5. Clients may convert values into
     * language-native decimal formats, such as Java's [BigDecimal][] or Python's
     * [decimal.Decimal][].
     *
     * <p>[BigDecimal]:
     * https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/math/BigDecimal.html
     * [decimal.Decimal]: https://docs.python.org/3/library/decimal.html
     */
    public Builder quantity(Optional<? extends DecimalCreate> quantity) {
      Utils.checkNotNull(quantity, "quantity");
      this.quantity = quantity;
      return this;
    }

    /** The side of the order. */
    public Builder side(AlternativeOrderCreateSide side) {
      Utils.checkNotNull(side, "side");
      this.side = side;
      return this;
    }

    public AlternativeOrderCreate build() {

      return new AlternativeOrderCreate(
          clientOrderId,
          currencyCode,
          distributionType,
          full,
          identifier,
          identifierType,
          notionalValue,
          quantity,
          side);
    }
  }
}
