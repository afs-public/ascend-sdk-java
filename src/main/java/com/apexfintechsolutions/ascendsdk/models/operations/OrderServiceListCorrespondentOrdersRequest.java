/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.operations;

import com.apexfintechsolutions.ascendsdk.utils.SpeakeasyMetadata;
import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import java.util.Optional;

public class OrderServiceListCorrespondentOrdersRequest {
  /** The correspondent id. */
  @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=correspondent_id")
  private String correspondentId;

  /**
   * CEL filter string expressing what orders should be listed. The only properties available for
   * filtering are the boolean `open` and `order_date`. Each of these represent fields on the Orders
   * object, and more details about each can be found attached to the properties.
   *
   * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
   * `order_date` searches are limited to orders within the most recent 365 days. If no `order_date`
   * is specified, the default will search between now and 365 days ago.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=filter")
  private Optional<String> filter;

  /**
   * The number of records to return in a single page. The maximum page size is 100. If a value is
   * not provided, the default of 100 will be used. If a value less than one, or greater than the
   * maximum, is provided, the default value will be used.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_size")
  private Optional<Integer> pageSize;

  /**
   * The token for the next page of content to fetch. When paginating, all other parameters provided
   * to `ListOrders` must match the call that provided the page token.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_token")
  private Optional<String> pageToken;

  @JsonCreator
  public OrderServiceListCorrespondentOrdersRequest(
      String correspondentId,
      Optional<String> filter,
      Optional<Integer> pageSize,
      Optional<String> pageToken) {
    Utils.checkNotNull(correspondentId, "correspondentId");
    Utils.checkNotNull(filter, "filter");
    Utils.checkNotNull(pageSize, "pageSize");
    Utils.checkNotNull(pageToken, "pageToken");
    this.correspondentId = correspondentId;
    this.filter = filter;
    this.pageSize = pageSize;
    this.pageToken = pageToken;
  }

  public OrderServiceListCorrespondentOrdersRequest(String correspondentId) {
    this(correspondentId, Optional.empty(), Optional.empty(), Optional.empty());
  }

  /** The correspondent id. */
  @JsonIgnore
  public String correspondentId() {
    return correspondentId;
  }

  /**
   * CEL filter string expressing what orders should be listed. The only properties available for
   * filtering are the boolean `open` and `order_date`. Each of these represent fields on the Orders
   * object, and more details about each can be found attached to the properties.
   *
   * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
   * `order_date` searches are limited to orders within the most recent 365 days. If no `order_date`
   * is specified, the default will search between now and 365 days ago.
   */
  @JsonIgnore
  public Optional<String> filter() {
    return filter;
  }

  /**
   * The number of records to return in a single page. The maximum page size is 100. If a value is
   * not provided, the default of 100 will be used. If a value less than one, or greater than the
   * maximum, is provided, the default value will be used.
   */
  @JsonIgnore
  public Optional<Integer> pageSize() {
    return pageSize;
  }

  /**
   * The token for the next page of content to fetch. When paginating, all other parameters provided
   * to `ListOrders` must match the call that provided the page token.
   */
  @JsonIgnore
  public Optional<String> pageToken() {
    return pageToken;
  }

  public static Builder builder() {
    return new Builder();
  }

  /** The correspondent id. */
  public OrderServiceListCorrespondentOrdersRequest withCorrespondentId(String correspondentId) {
    Utils.checkNotNull(correspondentId, "correspondentId");
    this.correspondentId = correspondentId;
    return this;
  }

  /**
   * CEL filter string expressing what orders should be listed. The only properties available for
   * filtering are the boolean `open` and `order_date`. Each of these represent fields on the Orders
   * object, and more details about each can be found attached to the properties.
   *
   * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
   * `order_date` searches are limited to orders within the most recent 365 days. If no `order_date`
   * is specified, the default will search between now and 365 days ago.
   */
  public OrderServiceListCorrespondentOrdersRequest withFilter(String filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = Optional.ofNullable(filter);
    return this;
  }

  /**
   * CEL filter string expressing what orders should be listed. The only properties available for
   * filtering are the boolean `open` and `order_date`. Each of these represent fields on the Orders
   * object, and more details about each can be found attached to the properties.
   *
   * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
   * `order_date` searches are limited to orders within the most recent 365 days. If no `order_date`
   * is specified, the default will search between now and 365 days ago.
   */
  public OrderServiceListCorrespondentOrdersRequest withFilter(Optional<String> filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = filter;
    return this;
  }

  /**
   * The number of records to return in a single page. The maximum page size is 100. If a value is
   * not provided, the default of 100 will be used. If a value less than one, or greater than the
   * maximum, is provided, the default value will be used.
   */
  public OrderServiceListCorrespondentOrdersRequest withPageSize(int pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = Optional.ofNullable(pageSize);
    return this;
  }

  /**
   * The number of records to return in a single page. The maximum page size is 100. If a value is
   * not provided, the default of 100 will be used. If a value less than one, or greater than the
   * maximum, is provided, the default value will be used.
   */
  public OrderServiceListCorrespondentOrdersRequest withPageSize(Optional<Integer> pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = pageSize;
    return this;
  }

  /**
   * The token for the next page of content to fetch. When paginating, all other parameters provided
   * to `ListOrders` must match the call that provided the page token.
   */
  public OrderServiceListCorrespondentOrdersRequest withPageToken(String pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = Optional.ofNullable(pageToken);
    return this;
  }

  /**
   * The token for the next page of content to fetch. When paginating, all other parameters provided
   * to `ListOrders` must match the call that provided the page token.
   */
  public OrderServiceListCorrespondentOrdersRequest withPageToken(Optional<String> pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = pageToken;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    OrderServiceListCorrespondentOrdersRequest other =
        (OrderServiceListCorrespondentOrdersRequest) o;
    return Utils.enhancedDeepEquals(this.correspondentId, other.correspondentId)
        && Utils.enhancedDeepEquals(this.filter, other.filter)
        && Utils.enhancedDeepEquals(this.pageSize, other.pageSize)
        && Utils.enhancedDeepEquals(this.pageToken, other.pageToken);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(correspondentId, filter, pageSize, pageToken);
  }

  @Override
  public String toString() {
    return Utils.toString(
        OrderServiceListCorrespondentOrdersRequest.class,
        "correspondentId",
        correspondentId,
        "filter",
        filter,
        "pageSize",
        pageSize,
        "pageToken",
        pageToken);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private String correspondentId;

    private Optional<String> filter = Optional.empty();

    private Optional<Integer> pageSize = Optional.empty();

    private Optional<String> pageToken = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /** The correspondent id. */
    public Builder correspondentId(String correspondentId) {
      Utils.checkNotNull(correspondentId, "correspondentId");
      this.correspondentId = correspondentId;
      return this;
    }

    /**
     * CEL filter string expressing what orders should be listed. The only properties available for
     * filtering are the boolean `open` and `order_date`. Each of these represent fields on the
     * Orders object, and more details about each can be found attached to the properties.
     *
     * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
     * `order_date` searches are limited to orders within the most recent 365 days. If no
     * `order_date` is specified, the default will search between now and 365 days ago.
     */
    public Builder filter(String filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = Optional.ofNullable(filter);
      return this;
    }

    /**
     * CEL filter string expressing what orders should be listed. The only properties available for
     * filtering are the boolean `open` and `order_date`. Each of these represent fields on the
     * Orders object, and more details about each can be found attached to the properties.
     *
     * <p>If `open` is not provided, both "open" and "not open" orders will be returned. All
     * `order_date` searches are limited to orders within the most recent 365 days. If no
     * `order_date` is specified, the default will search between now and 365 days ago.
     */
    public Builder filter(Optional<String> filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = filter;
      return this;
    }

    /**
     * The number of records to return in a single page. The maximum page size is 100. If a value is
     * not provided, the default of 100 will be used. If a value less than one, or greater than the
     * maximum, is provided, the default value will be used.
     */
    public Builder pageSize(int pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = Optional.ofNullable(pageSize);
      return this;
    }

    /**
     * The number of records to return in a single page. The maximum page size is 100. If a value is
     * not provided, the default of 100 will be used. If a value less than one, or greater than the
     * maximum, is provided, the default value will be used.
     */
    public Builder pageSize(Optional<Integer> pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = pageSize;
      return this;
    }

    /**
     * The token for the next page of content to fetch. When paginating, all other parameters
     * provided to `ListOrders` must match the call that provided the page token.
     */
    public Builder pageToken(String pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = Optional.ofNullable(pageToken);
      return this;
    }

    /**
     * The token for the next page of content to fetch. When paginating, all other parameters
     * provided to `ListOrders` must match the call that provided the page token.
     */
    public Builder pageToken(Optional<String> pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = pageToken;
      return this;
    }

    public OrderServiceListCorrespondentOrdersRequest build() {

      return new OrderServiceListCorrespondentOrdersRequest(
          correspondentId, filter, pageSize, pageToken);
    }
  }
}
