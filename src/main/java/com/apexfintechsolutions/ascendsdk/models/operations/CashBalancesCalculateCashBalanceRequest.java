/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.operations;

import com.apexfintechsolutions.ascendsdk.utils.SpeakeasyMetadata;
import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import java.util.Objects;
import java.util.Optional;

public class CashBalancesCalculateCashBalanceRequest {

  /** The account id. */
  @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=account_id")
  private String accountId;

  /**
   * The withdraw mechanism to calculate the balance for. The mechanism determines what account
   * activity will affect the balance.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=mechanism")
  private Optional<? extends Mechanism> mechanism;

  @JsonCreator
  public CashBalancesCalculateCashBalanceRequest(
      String accountId, Optional<? extends Mechanism> mechanism) {
    Utils.checkNotNull(accountId, "accountId");
    Utils.checkNotNull(mechanism, "mechanism");
    this.accountId = accountId;
    this.mechanism = mechanism;
  }

  public CashBalancesCalculateCashBalanceRequest(String accountId) {
    this(accountId, Optional.empty());
  }

  /** The account id. */
  @JsonIgnore
  public String accountId() {
    return accountId;
  }

  /**
   * The withdraw mechanism to calculate the balance for. The mechanism determines what account
   * activity will affect the balance.
   */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<Mechanism> mechanism() {
    return (Optional<Mechanism>) mechanism;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /** The account id. */
  public CashBalancesCalculateCashBalanceRequest withAccountId(String accountId) {
    Utils.checkNotNull(accountId, "accountId");
    this.accountId = accountId;
    return this;
  }

  /**
   * The withdraw mechanism to calculate the balance for. The mechanism determines what account
   * activity will affect the balance.
   */
  public CashBalancesCalculateCashBalanceRequest withMechanism(Mechanism mechanism) {
    Utils.checkNotNull(mechanism, "mechanism");
    this.mechanism = Optional.ofNullable(mechanism);
    return this;
  }

  /**
   * The withdraw mechanism to calculate the balance for. The mechanism determines what account
   * activity will affect the balance.
   */
  public CashBalancesCalculateCashBalanceRequest withMechanism(
      Optional<? extends Mechanism> mechanism) {
    Utils.checkNotNull(mechanism, "mechanism");
    this.mechanism = mechanism;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CashBalancesCalculateCashBalanceRequest other = (CashBalancesCalculateCashBalanceRequest) o;
    return Objects.deepEquals(this.accountId, other.accountId)
        && Objects.deepEquals(this.mechanism, other.mechanism);
  }

  @Override
  public int hashCode() {
    return Objects.hash(accountId, mechanism);
  }

  @Override
  public String toString() {
    return Utils.toString(
        CashBalancesCalculateCashBalanceRequest.class,
        "accountId",
        accountId,
        "mechanism",
        mechanism);
  }

  public static final class Builder {

    private String accountId;

    private Optional<? extends Mechanism> mechanism = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /** The account id. */
    public Builder accountId(String accountId) {
      Utils.checkNotNull(accountId, "accountId");
      this.accountId = accountId;
      return this;
    }

    /**
     * The withdraw mechanism to calculate the balance for. The mechanism determines what account
     * activity will affect the balance.
     */
    public Builder mechanism(Mechanism mechanism) {
      Utils.checkNotNull(mechanism, "mechanism");
      this.mechanism = Optional.ofNullable(mechanism);
      return this;
    }

    /**
     * The withdraw mechanism to calculate the balance for. The mechanism determines what account
     * activity will affect the balance.
     */
    public Builder mechanism(Optional<? extends Mechanism> mechanism) {
      Utils.checkNotNull(mechanism, "mechanism");
      this.mechanism = mechanism;
      return this;
    }

    public CashBalancesCalculateCashBalanceRequest build() {
      return new CashBalancesCalculateCashBalanceRequest(accountId, mechanism);
    }
  }
}
