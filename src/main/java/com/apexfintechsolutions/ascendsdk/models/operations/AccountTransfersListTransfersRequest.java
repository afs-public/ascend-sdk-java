/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk.models.operations;

import com.apexfintechsolutions.ascendsdk.utils.SpeakeasyMetadata;
import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import java.util.Optional;

public class AccountTransfersListTransfersRequest {
  /** The correspondent id. */
  @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=correspondent_id")
  private String correspondentId;

  /** The account id. */
  @SpeakeasyMetadata("pathParam:style=simple,explode=false,name=account_id")
  private String accountId;

  /**
   * The maximum number of transfers to return. The service may return fewer than this value. If
   * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above 200
   * will be coerced to 200.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_size")
  private Optional<Integer> pageSize;

  /**
   * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
   * provided the page token.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_token")
  private Optional<String> pageToken;

  /**
   * A CEL string to filter results; See the [CEL
   * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
   * Guides for more information;
   *
   * <p>Currently supported CEL filters: -------------------------------- * acat_control_number (add
   * to queries for better performance) * state == State.* * nscc_status == NsccStatus.* * direction
   * == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* * create_time *
   * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
   * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
   * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
   * deliverer.correspondent_code * deliverer.correspondent_id
   *
   * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
   * practice this is not performant and should be avoided if possible
   *
   * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
   *
   * <p>- The following CEL operators are NOT allowed, using them will result in an INVALID_ARGUMENT
   * being returned: string.matches(substr) + - / *
   *
   * <p>- Queries using acat_control_number will result in increased performance
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=filter")
  private Optional<String> filter;

  @JsonCreator
  public AccountTransfersListTransfersRequest(
      String correspondentId,
      String accountId,
      Optional<Integer> pageSize,
      Optional<String> pageToken,
      Optional<String> filter) {
    Utils.checkNotNull(correspondentId, "correspondentId");
    Utils.checkNotNull(accountId, "accountId");
    Utils.checkNotNull(pageSize, "pageSize");
    Utils.checkNotNull(pageToken, "pageToken");
    Utils.checkNotNull(filter, "filter");
    this.correspondentId = correspondentId;
    this.accountId = accountId;
    this.pageSize = pageSize;
    this.pageToken = pageToken;
    this.filter = filter;
  }

  public AccountTransfersListTransfersRequest(String correspondentId, String accountId) {
    this(correspondentId, accountId, Optional.empty(), Optional.empty(), Optional.empty());
  }

  /** The correspondent id. */
  @JsonIgnore
  public String correspondentId() {
    return correspondentId;
  }

  /** The account id. */
  @JsonIgnore
  public String accountId() {
    return accountId;
  }

  /**
   * The maximum number of transfers to return. The service may return fewer than this value. If
   * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above 200
   * will be coerced to 200.
   */
  @JsonIgnore
  public Optional<Integer> pageSize() {
    return pageSize;
  }

  /**
   * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
   * provided the page token.
   */
  @JsonIgnore
  public Optional<String> pageToken() {
    return pageToken;
  }

  /**
   * A CEL string to filter results; See the [CEL
   * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
   * Guides for more information;
   *
   * <p>Currently supported CEL filters: -------------------------------- * acat_control_number (add
   * to queries for better performance) * state == State.* * nscc_status == NsccStatus.* * direction
   * == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* * create_time *
   * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
   * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
   * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
   * deliverer.correspondent_code * deliverer.correspondent_id
   *
   * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
   * practice this is not performant and should be avoided if possible
   *
   * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
   *
   * <p>- The following CEL operators are NOT allowed, using them will result in an INVALID_ARGUMENT
   * being returned: string.matches(substr) + - / *
   *
   * <p>- Queries using acat_control_number will result in increased performance
   */
  @JsonIgnore
  public Optional<String> filter() {
    return filter;
  }

  public static Builder builder() {
    return new Builder();
  }

  /** The correspondent id. */
  public AccountTransfersListTransfersRequest withCorrespondentId(String correspondentId) {
    Utils.checkNotNull(correspondentId, "correspondentId");
    this.correspondentId = correspondentId;
    return this;
  }

  /** The account id. */
  public AccountTransfersListTransfersRequest withAccountId(String accountId) {
    Utils.checkNotNull(accountId, "accountId");
    this.accountId = accountId;
    return this;
  }

  /**
   * The maximum number of transfers to return. The service may return fewer than this value. If
   * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above 200
   * will be coerced to 200.
   */
  public AccountTransfersListTransfersRequest withPageSize(int pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = Optional.ofNullable(pageSize);
    return this;
  }

  /**
   * The maximum number of transfers to return. The service may return fewer than this value. If
   * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above 200
   * will be coerced to 200.
   */
  public AccountTransfersListTransfersRequest withPageSize(Optional<Integer> pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = pageSize;
    return this;
  }

  /**
   * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
   * provided the page token.
   */
  public AccountTransfersListTransfersRequest withPageToken(String pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = Optional.ofNullable(pageToken);
    return this;
  }

  /**
   * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
   * provided the page token.
   */
  public AccountTransfersListTransfersRequest withPageToken(Optional<String> pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = pageToken;
    return this;
  }

  /**
   * A CEL string to filter results; See the [CEL
   * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
   * Guides for more information;
   *
   * <p>Currently supported CEL filters: -------------------------------- * acat_control_number (add
   * to queries for better performance) * state == State.* * nscc_status == NsccStatus.* * direction
   * == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* * create_time *
   * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
   * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
   * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
   * deliverer.correspondent_code * deliverer.correspondent_id
   *
   * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
   * practice this is not performant and should be avoided if possible
   *
   * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
   *
   * <p>- The following CEL operators are NOT allowed, using them will result in an INVALID_ARGUMENT
   * being returned: string.matches(substr) + - / *
   *
   * <p>- Queries using acat_control_number will result in increased performance
   */
  public AccountTransfersListTransfersRequest withFilter(String filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = Optional.ofNullable(filter);
    return this;
  }

  /**
   * A CEL string to filter results; See the [CEL
   * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
   * Guides for more information;
   *
   * <p>Currently supported CEL filters: -------------------------------- * acat_control_number (add
   * to queries for better performance) * state == State.* * nscc_status == NsccStatus.* * direction
   * == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* * create_time *
   * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
   * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
   * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
   * deliverer.correspondent_code * deliverer.correspondent_id
   *
   * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
   * practice this is not performant and should be avoided if possible
   *
   * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
   *
   * <p>- The following CEL operators are NOT allowed, using them will result in an INVALID_ARGUMENT
   * being returned: string.matches(substr) + - / *
   *
   * <p>- Queries using acat_control_number will result in increased performance
   */
  public AccountTransfersListTransfersRequest withFilter(Optional<String> filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = filter;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AccountTransfersListTransfersRequest other = (AccountTransfersListTransfersRequest) o;
    return Utils.enhancedDeepEquals(this.correspondentId, other.correspondentId)
        && Utils.enhancedDeepEquals(this.accountId, other.accountId)
        && Utils.enhancedDeepEquals(this.pageSize, other.pageSize)
        && Utils.enhancedDeepEquals(this.pageToken, other.pageToken)
        && Utils.enhancedDeepEquals(this.filter, other.filter);
  }

  @Override
  public int hashCode() {
    return Utils.enhancedHash(correspondentId, accountId, pageSize, pageToken, filter);
  }

  @Override
  public String toString() {
    return Utils.toString(
        AccountTransfersListTransfersRequest.class,
        "correspondentId",
        correspondentId,
        "accountId",
        accountId,
        "pageSize",
        pageSize,
        "pageToken",
        pageToken,
        "filter",
        filter);
  }

  @SuppressWarnings("UnusedReturnValue")
  public static final class Builder {

    private String correspondentId;

    private String accountId;

    private Optional<Integer> pageSize = Optional.empty();

    private Optional<String> pageToken = Optional.empty();

    private Optional<String> filter = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /** The correspondent id. */
    public Builder correspondentId(String correspondentId) {
      Utils.checkNotNull(correspondentId, "correspondentId");
      this.correspondentId = correspondentId;
      return this;
    }

    /** The account id. */
    public Builder accountId(String accountId) {
      Utils.checkNotNull(accountId, "accountId");
      this.accountId = accountId;
      return this;
    }

    /**
     * The maximum number of transfers to return. The service may return fewer than this value. If
     * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above
     * 200 will be coerced to 200.
     */
    public Builder pageSize(int pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = Optional.ofNullable(pageSize);
      return this;
    }

    /**
     * The maximum number of transfers to return. The service may return fewer than this value. If
     * unspecified, at most 100 transfers will be returned. The maximum value is 200; values above
     * 200 will be coerced to 200.
     */
    public Builder pageSize(Optional<Integer> pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = pageSize;
      return this;
    }

    /**
     * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
     * subsequent page.
     *
     * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
     * provided the page token.
     */
    public Builder pageToken(String pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = Optional.ofNullable(pageToken);
      return this;
    }

    /**
     * A page token, received from a previous `ListTransfers` call. Provide this to retrieve the
     * subsequent page.
     *
     * <p>When paginating, all other parameters provided to `ListTransfers` must match the call that
     * provided the page token.
     */
    public Builder pageToken(Optional<String> pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = pageToken;
      return this;
    }

    /**
     * A CEL string to filter results; See the [CEL
     * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
     * Guides for more information;
     *
     * <p>Currently supported CEL filters: -------------------------------- * acat_control_number
     * (add to queries for better performance) * state == State.* * nscc_status == NsccStatus.* *
     * direction == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* *
     * create_time * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
     * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
     * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
     * deliverer.correspondent_code * deliverer.correspondent_id
     *
     * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
     * practice this is not performant and should be avoided if possible
     *
     * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
     *
     * <p>- The following CEL operators are NOT allowed, using them will result in an
     * INVALID_ARGUMENT being returned: string.matches(substr) + - / *
     *
     * <p>- Queries using acat_control_number will result in increased performance
     */
    public Builder filter(String filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = Optional.ofNullable(filter);
      return this;
    }

    /**
     * A CEL string to filter results; See the [CEL
     * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
     * Guides for more information;
     *
     * <p>Currently supported CEL filters: -------------------------------- * acat_control_number
     * (add to queries for better performance) * state == State.* * nscc_status == NsccStatus.* *
     * direction == Direction.* * transfer_type == TransferType.* * reject_code == RejectCode.* *
     * create_time * last_nscc_status_updated_time * receiver.account_id * receiver.account_number *
     * receiver.participant_number * receiver.correspondent_code * receiver.correspondent_id *
     * deliverer.account_id * deliverer.account_number * deliverer.participant_number *
     * deliverer.correspondent_code * deliverer.correspondent_id
     *
     * <p>- Empty filters are allowed, which return the most recent page_size worth of transfers, in
     * practice this is not performant and should be avoided if possible
     *
     * <p>- Macros are NOT allowed, using them will result in an INVALID_ARGUMENT being returned
     *
     * <p>- The following CEL operators are NOT allowed, using them will result in an
     * INVALID_ARGUMENT being returned: string.matches(substr) + - / *
     *
     * <p>- Queries using acat_control_number will result in increased performance
     */
    public Builder filter(Optional<String> filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = filter;
      return this;
    }

    public AccountTransfersListTransfersRequest build() {

      return new AccountTransfersListTransfersRequest(
          correspondentId, accountId, pageSize, pageToken, filter);
    }
  }
}
