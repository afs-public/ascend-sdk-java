/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.apexfintechsolutions.ascendsdk.models.operations;

import com.apexfintechsolutions.ascendsdk.utils.SpeakeasyMetadata;
import com.apexfintechsolutions.ascendsdk.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import java.util.Objects;
import java.util.Optional;

public class AccountsListAccountsRequest {

  /**
   * The maximum number of accounts to return. The service may return fewer than this value. If
   * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
   * will be coerced to 100.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_size")
  private Optional<Integer> pageSize;

  /**
   * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
   * provided the page token.
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=page_token")
  private Optional<String> pageToken;

  /**
   * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
   * `state` `account_group_id`
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=order_by")
  private Optional<String> orderBy;

  /**
   * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
   * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
   * See the [CEL Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search)
   * page in Guides for more information; Filter options include: `account_id` `account_number`
   * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
   * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
   * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
   * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
   * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
   * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
   * `parties.legal_natural_person.foreign_identification.identification_number`
   * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
   * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
   */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=filter")
  private Optional<String> filter;

  /** The view to return. Defaults to `BASIC`. */
  @SpeakeasyMetadata("queryParam:style=form,explode=true,name=view")
  private Optional<? extends View> view;

  @JsonCreator
  public AccountsListAccountsRequest(
      Optional<Integer> pageSize,
      Optional<String> pageToken,
      Optional<String> orderBy,
      Optional<String> filter,
      Optional<? extends View> view) {
    Utils.checkNotNull(pageSize, "pageSize");
    Utils.checkNotNull(pageToken, "pageToken");
    Utils.checkNotNull(orderBy, "orderBy");
    Utils.checkNotNull(filter, "filter");
    Utils.checkNotNull(view, "view");
    this.pageSize = pageSize;
    this.pageToken = pageToken;
    this.orderBy = orderBy;
    this.filter = filter;
    this.view = view;
  }

  public AccountsListAccountsRequest() {
    this(Optional.empty(), Optional.empty(), Optional.empty(), Optional.empty(), Optional.empty());
  }

  /**
   * The maximum number of accounts to return. The service may return fewer than this value. If
   * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
   * will be coerced to 100.
   */
  @JsonIgnore
  public Optional<Integer> pageSize() {
    return pageSize;
  }

  /**
   * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
   * provided the page token.
   */
  @JsonIgnore
  public Optional<String> pageToken() {
    return pageToken;
  }

  /**
   * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
   * `state` `account_group_id`
   */
  @JsonIgnore
  public Optional<String> orderBy() {
    return orderBy;
  }

  /**
   * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
   * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
   * See the [CEL Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search)
   * page in Guides for more information; Filter options include: `account_id` `account_number`
   * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
   * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
   * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
   * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
   * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
   * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
   * `parties.legal_natural_person.foreign_identification.identification_number`
   * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
   * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
   */
  @JsonIgnore
  public Optional<String> filter() {
    return filter;
  }

  /** The view to return. Defaults to `BASIC`. */
  @SuppressWarnings("unchecked")
  @JsonIgnore
  public Optional<View> view() {
    return (Optional<View>) view;
  }

  public static final Builder builder() {
    return new Builder();
  }

  /**
   * The maximum number of accounts to return. The service may return fewer than this value. If
   * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
   * will be coerced to 100.
   */
  public AccountsListAccountsRequest withPageSize(int pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = Optional.ofNullable(pageSize);
    return this;
  }

  /**
   * The maximum number of accounts to return. The service may return fewer than this value. If
   * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
   * will be coerced to 100.
   */
  public AccountsListAccountsRequest withPageSize(Optional<Integer> pageSize) {
    Utils.checkNotNull(pageSize, "pageSize");
    this.pageSize = pageSize;
    return this;
  }

  /**
   * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
   * provided the page token.
   */
  public AccountsListAccountsRequest withPageToken(String pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = Optional.ofNullable(pageToken);
    return this;
  }

  /**
   * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
   * subsequent page.
   *
   * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
   * provided the page token.
   */
  public AccountsListAccountsRequest withPageToken(Optional<String> pageToken) {
    Utils.checkNotNull(pageToken, "pageToken");
    this.pageToken = pageToken;
    return this;
  }

  /**
   * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
   * `state` `account_group_id`
   */
  public AccountsListAccountsRequest withOrderBy(String orderBy) {
    Utils.checkNotNull(orderBy, "orderBy");
    this.orderBy = Optional.ofNullable(orderBy);
    return this;
  }

  /**
   * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
   * `state` `account_group_id`
   */
  public AccountsListAccountsRequest withOrderBy(Optional<String> orderBy) {
    Utils.checkNotNull(orderBy, "orderBy");
    this.orderBy = orderBy;
    return this;
  }

  /**
   * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
   * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
   * See the [CEL Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search)
   * page in Guides for more information; Filter options include: `account_id` `account_number`
   * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
   * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
   * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
   * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
   * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
   * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
   * `parties.legal_natural_person.foreign_identification.identification_number`
   * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
   * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
   */
  public AccountsListAccountsRequest withFilter(String filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = Optional.ofNullable(filter);
    return this;
  }

  /**
   * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
   * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
   * See the [CEL Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search)
   * page in Guides for more information; Filter options include: `account_id` `account_number`
   * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
   * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
   * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
   * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
   * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
   * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
   * `parties.legal_natural_person.foreign_identification.identification_number`
   * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
   * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
   */
  public AccountsListAccountsRequest withFilter(Optional<String> filter) {
    Utils.checkNotNull(filter, "filter");
    this.filter = filter;
    return this;
  }

  /** The view to return. Defaults to `BASIC`. */
  public AccountsListAccountsRequest withView(View view) {
    Utils.checkNotNull(view, "view");
    this.view = Optional.ofNullable(view);
    return this;
  }

  /** The view to return. Defaults to `BASIC`. */
  public AccountsListAccountsRequest withView(Optional<? extends View> view) {
    Utils.checkNotNull(view, "view");
    this.view = view;
    return this;
  }

  @Override
  public boolean equals(java.lang.Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AccountsListAccountsRequest other = (AccountsListAccountsRequest) o;
    return Objects.deepEquals(this.pageSize, other.pageSize)
        && Objects.deepEquals(this.pageToken, other.pageToken)
        && Objects.deepEquals(this.orderBy, other.orderBy)
        && Objects.deepEquals(this.filter, other.filter)
        && Objects.deepEquals(this.view, other.view);
  }

  @Override
  public int hashCode() {
    return Objects.hash(pageSize, pageToken, orderBy, filter, view);
  }

  @Override
  public String toString() {
    return Utils.toString(
        AccountsListAccountsRequest.class,
        "pageSize",
        pageSize,
        "pageToken",
        pageToken,
        "orderBy",
        orderBy,
        "filter",
        filter,
        "view",
        view);
  }

  public static final class Builder {

    private Optional<Integer> pageSize = Optional.empty();

    private Optional<String> pageToken = Optional.empty();

    private Optional<String> orderBy = Optional.empty();

    private Optional<String> filter = Optional.empty();

    private Optional<? extends View> view = Optional.empty();

    private Builder() {
      // force use of static builder() method
    }

    /**
     * The maximum number of accounts to return. The service may return fewer than this value. If
     * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
     * will be coerced to 100.
     */
    public Builder pageSize(int pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = Optional.ofNullable(pageSize);
      return this;
    }

    /**
     * The maximum number of accounts to return. The service may return fewer than this value. If
     * unspecified, at most 25 accounts will be returned. The maximum value is 100; values above 100
     * will be coerced to 100.
     */
    public Builder pageSize(Optional<Integer> pageSize) {
      Utils.checkNotNull(pageSize, "pageSize");
      this.pageSize = pageSize;
      return this;
    }

    /**
     * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
     * subsequent page.
     *
     * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
     * provided the page token.
     */
    public Builder pageToken(String pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = Optional.ofNullable(pageToken);
      return this;
    }

    /**
     * A page token, received from a previous `ListAccounts` call. Provide this to retrieve the
     * subsequent page.
     *
     * <p>When paginating, all other parameters provided to `ListAccounts` must match the call that
     * provided the page token.
     */
    public Builder pageToken(Optional<String> pageToken) {
      Utils.checkNotNull(pageToken, "pageToken");
      this.pageToken = pageToken;
      return this;
    }

    /**
     * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
     * `state` `account_group_id`
     */
    public Builder orderBy(String orderBy) {
      Utils.checkNotNull(orderBy, "orderBy");
      this.orderBy = Optional.ofNullable(orderBy);
      return this;
    }

    /**
     * The order in which accounts are listed. Can order by: `account_id` `account_number` `title`
     * `state` `account_group_id`
     */
    public Builder orderBy(Optional<String> orderBy) {
      Utils.checkNotNull(orderBy, "orderBy");
      this.orderBy = orderBy;
      return this;
    }

    /**
     * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
     * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
     * See the [CEL
     * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
     * Guides for more information; Filter options include: `account_id` `account_number`
     * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
     * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
     * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
     * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
     * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
     * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
     * `parties.legal_natural_person.foreign_identification.identification_number`
     * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
     * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
     */
    public Builder filter(String filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = Optional.ofNullable(filter);
      return this;
    }

    /**
     * A CEL string to filter results; Use `upperAscii()` for case-insensitive searches; E.g.
     * `parties.exists(parties,parties.legal_natural_person.family_name.upperAscii()=="dOuGh".upperAscii())`;
     * See the [CEL
     * Search](https://developer.apexclearing.com/apex-fintech-solutions/docs/cel-search) page in
     * Guides for more information; Filter options include: `account_id` `account_number`
     * `correspondent_id` `title` `state` `margin_group_id` `account_group_id` `registration_type`
     * `reserve_class` `parties.legal_natural_person.legal_natural_person_id`
     * `parties.legal_natural_person.given_name` `parties.legal_natural_person.family_name`
     * `parties.legal_natural_person.tax_id_type` `parties.legal_natural_person.tax_id`
     * `parties.legal_entity.legal_entity_id` `parties.legal_entity.entity_name`
     * `parties.legal_entity.tax_id_type` `parties.legal_entity.tax_id`
     * `parties.legal_natural_person.foreign_identification.identification_number`
     * `parties.legal_natural_person.foreign_identification.type` `enrollments.enrollment_id`
     * `enrollments.type` `enrollments.state` `identifiers.type` `identifiers.value`
     */
    public Builder filter(Optional<String> filter) {
      Utils.checkNotNull(filter, "filter");
      this.filter = filter;
      return this;
    }

    /** The view to return. Defaults to `BASIC`. */
    public Builder view(View view) {
      Utils.checkNotNull(view, "view");
      this.view = Optional.ofNullable(view);
      return this;
    }

    /** The view to return. Defaults to `BASIC`. */
    public Builder view(Optional<? extends View> view) {
      Utils.checkNotNull(view, "view");
      this.view = view;
      return this;
    }

    public AccountsListAccountsRequest build() {
      return new AccountsListAccountsRequest(pageSize, pageToken, orderBy, filter, view);
    }
  }
}
