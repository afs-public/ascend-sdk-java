/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.apexfintechsolutions.ascendsdk;

import static org.junit.jupiter.api.Assertions.assertEquals;

import com.apexfintechsolutions.ascendsdk.models.components.Security;
import com.apexfintechsolutions.ascendsdk.models.components.ServiceAccountCreds;
import com.apexfintechsolutions.ascendsdk.models.operations.AccountsListAccountsRequest;
import com.apexfintechsolutions.ascendsdk.models.operations.AccountsListAccountsResponse;
import com.apexfintechsolutions.ascendsdk.models.operations.AccountsListLegalEntitiesResponse;
import com.apexfintechsolutions.ascendsdk.models.operations.AccountsListLegalNaturalPersonsResponse;
import com.apexfintechsolutions.ascendsdk.models.operations.View;
import com.apexfintechsolutions.ascendsdk.utils.Utils;
import org.junit.jupiter.api.Test;

public class PersonManagementTests {

  @Test
  public void testPersonManagement_AccountsListLegalNaturalPersons() throws Exception {

    var testHttpClient = Utils.createTestHTTPClient("Accounts_ListLegalNaturalPersons");
    SDK sdk =
        SDK.builder()
            .serverURL(Utils.environmentVariable("SERVICE_ACCOUNT_CREDS_URL", ""))
            .security(
                Security.builder()
                    .apiKey(System.getenv().getOrDefault("API_KEY", ""))
                    .serviceAccountCreds(
                        ServiceAccountCreds.builder()
                            .privateKey(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_PRIVATE_KEY", ""))
                            .name(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_NAME", ""))
                            .organization(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_ORGANIZATION", ""))
                            .type(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_TYPE", ""))
                            .build())
                    .build())
            .client(testHttpClient)
            .build();

    AccountsListLegalNaturalPersonsResponse res =
        sdk.personManagement()
            .listLegalNaturalPersons()
            .pageSize(25)
            .pageToken("")
            .filter("")
            .call();
    assertEquals(200, res.statusCode());
  }

  @Test
  public void testPersonManagement_AccountsListLegalEntities() throws Exception {

    var testHttpClient = Utils.createTestHTTPClient("Accounts_ListLegalEntities");
    SDK sdk =
        SDK.builder()
            .serverURL(Utils.environmentVariable("SERVICE_ACCOUNT_CREDS_URL", ""))
            .security(
                Security.builder()
                    .apiKey(System.getenv().getOrDefault("API_KEY", ""))
                    .serviceAccountCreds(
                        ServiceAccountCreds.builder()
                            .privateKey(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_PRIVATE_KEY", ""))
                            .name(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_NAME", ""))
                            .organization(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_ORGANIZATION", ""))
                            .type(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_TYPE", ""))
                            .build())
                    .build())
            .client(testHttpClient)
            .build();

    AccountsListLegalEntitiesResponse res =
        sdk.personManagement()
            .listLegalEntities()
            .pageSize(25)
            .pageToken("")
            .orderBy("")
            .filter("")
            .call();
    assertEquals(200, res.statusCode());
  }

  @Test
  public void testPersonManagement_AccountsListAccounts() throws Exception {

    var testHttpClient = Utils.createTestHTTPClient("Accounts_ListAccounts");
    SDK sdk =
        SDK.builder()
            .serverURL(Utils.environmentVariable("SERVICE_ACCOUNT_CREDS_URL", ""))
            .security(
                Security.builder()
                    .apiKey(System.getenv().getOrDefault("API_KEY", ""))
                    .serviceAccountCreds(
                        ServiceAccountCreds.builder()
                            .privateKey(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_PRIVATE_KEY", ""))
                            .name(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_NAME", ""))
                            .organization(
                                System.getenv()
                                    .getOrDefault("SERVICE_ACCOUNT_CREDS_ORGANIZATION", ""))
                            .type(System.getenv().getOrDefault("SERVICE_ACCOUNT_CREDS_TYPE", ""))
                            .build())
                    .build())
            .client(testHttpClient)
            .build();

    AccountsListAccountsRequest req =
        AccountsListAccountsRequest.builder()
            .pageSize(25)
            .pageToken("")
            .orderBy("")
            .filter("")
            .view(View.BASIC)
            .build();

    AccountsListAccountsResponse res = sdk.accountManagement().listAccounts().request(req).call();
    assertEquals(200, res.statusCode());
  }
}
